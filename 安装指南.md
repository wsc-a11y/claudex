前置： Node 20+、pnpm 9+、claude CLI 已安装并登录

1. 如果装了claude code cli版，或者用过npm/npx命令的，那应该有node

2. - 安装pnpm可以从npm渠道(npm install -g pnpm)
   - 或者单独渠道(iwr https://get.pnpm.io/install.ps1 -useb | iex)
   - 参考：https://www.runoob.com/nodejs/nodejs-pnpm.html

---
前置条件确认后，在这个文件夹，打开cmd，运行pnpm install执行下载依赖包
- pnpm可以设置镜像源
- 有一个包我下载有问题，因为node版本比作者的新，deepseek说用一个新版本的包可以兼容

---
依赖包安装后，打开cmd，运行pnpm run init --username=用户名 --password='密码（要单引号）'

`首次 init 会打印你的 TOTP 密钥（二维码 + 手输字符串）和 10 条恢复码——只显示一次，过后再也看不到。请务必保存。把二维码扫进任意 TOTP 应用（1Password / Authy / Aegis / Google Authenticator 均可）。`

---
运行用pnpm serve , 终端打印内容有可以跳转的网址，直接ctrl+点击即可

实际运行可以用“启动dev管理器.vbs”，封装了启动，缩放到状态栏，退出时杀后台进程等等这些操作，因为直接终端执行pnpm serve后，直接结束进程，claudex是不会停止的

'''参考了 ./REA.md '''