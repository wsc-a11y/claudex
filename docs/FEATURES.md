# claudex · Features

> Living ledger of what actually ships. Updated in the **same commit** as the
> feature that changes it. If a behavior exists in code but isn't reflected
> here, that's a bug — either the doc or the code is wrong.

Three status tiers:

- ✅ **Ready** — end-to-end working, accessible from the UI a user sees.
- 🟡 **Partial** — backend or scaffold exists, UI is missing or thin. Safe to
  use from the API; users won't see it yet.
- ⬜ **Planned** — listed so nobody re-plans it from scratch, but not started.

Last updated: see the git log of this file. Row counts drift fast. Source of
truth is `git grep -c '^|' docs/FEATURES.md` for ✅ / 🟡 / ⬜ totals and
`pnpm --filter @claudex/server test` for the test count.

---

## Install & bootstrap

| Status | Feature | Where |
|---|---|---|
| ✅ | First-run admin creation via `pnpm run init`, interactive or env-driven (`CLAUDEX_INIT_USERNAME` / `CLAUDEX_INIT_PASSWORD` / `--username=` / `--password=`). Bootstrap order: TOTP secret → QR + manual-entry string → recovery codes printed under a `⚠ shown once, never again` banner → (interactive TTY only) Enter-to-confirm prompt → recovery-code hashes persisted. Persist happens strictly **after** print so a Ctrl-C before confirmation leaves TOTP complete but recovery codes unwritten (documented escape hatch: log in and regenerate from Settings → Security). Non-interactive / piped stdout paths skip the prompt but still print-then-flush-then-persist in the same order | `server/src/bin/init.ts` |
| ✅ | Optional 2FA at first install — `--skip-totp` flag (or `CLAUDEX_INIT_SKIP_TOTP=1`) creates the admin row with `totp_enabled=0` and an empty `totp_secret`, skipping the QR code + recovery-codes prompts entirely. Login then short-circuits to a session cookie after bcrypt for that account. The user can opt back in any time from Settings → Security → Enable 2FA | `server/src/bin/init.ts`, `server/src/db/index.ts` (migration 28), `server/src/auth/index.ts`, `server/src/auth/routes.ts` |
| ✅ | Generates TOTP secret, prints ASCII-art QR code and the secret string, sets the Issuer/Account to `claudex / <username>` | same |
| ✅ | Refuses to re-run if a user already exists (manual DB delete required to reset) | same |
| ✅ | `pnpm reset-credentials` — rotate username and/or password in place while keeping the TOTP secret (so the authenticator entry keeps working). Flags: `--username=`, `--password=`, `--match=<current-username>` for multi-user disambiguation; env vars `CLAUDEX_RESET_USERNAME` / `CLAUDEX_RESET_PASSWORD` / `CLAUDEX_RESET_MATCH` work too | `server/src/bin/reset-credentials.ts` |
| ✅ | All runtime state under `~/.claudex/` (DB, logs, JWT secret); override via `CLAUDEX_STATE_DIR` | `server/src/lib/config.ts` |
| ✅ | SQLite DB with hand-rolled migrations, WAL mode, foreign keys on, cascade on session delete | `server/src/db/index.ts` |
| ✅ | Refuses to bind anything other than `127.0.0.1` / `::1` / `localhost` — public exposure is the user's responsibility (frp / Cloudflare Tunnel / Tailscale / Caddy) | `server/src/lib/config.ts` |
| ✅ | `pnpm claudex:status` — read-only diagnostic snapshot for operators who don't want to open the web UI. Probes port 5179 over TCP (500ms connect timeout) to decide running/stopped, opens the DB with `readonly:true` and prints: server state + log path, DB size + migration count, session totals grouped by status (running/awaiting/idle/archived/error), event total and last-message age, batch-queue counts (queued/running/failed-in-last-24h), push-device count with most-recently-used device label, and a best-effort `frpc` PID via `pgrep -f "frpc -c"`. Never starts the server, never takes a write lock, never touches `~/.claude`. Respects `NO_COLOR` and non-TTY stdout | `server/src/bin/status.ts` |

## Server shape

| Status | Feature | Where |
|---|---|---|
| ✅ | Fastify 5 + `@fastify/cookie` + `@fastify/websocket` | `server/src/transport/app.ts` |
| ✅ | `GET /api/health` — liveness probe | same |
| ✅ | Single-port mode: server hosts the built web bundle at `/` alongside `/api` and `/ws`. `/assets/*` gets `Cache-Control: immutable`, `index.html` gets `no-cache` — including when it's served via the SPA fallback for any React Router path (`/session/:id`, `/routines`, `/alerts`, …) so deep-linking into a route doesn't poison the HTTP cache with a long-lived index.html copy | same |
| ✅ | SPA fallback: any non-`/api`, non-`/ws` GET falls through to `index.html` so React Router survives a refresh on deep links | same |
| ✅ | `/api/*` and `/ws` 404s return JSON (not swallowed by the HTML fallback) | same |
| ✅ | `CLAUDEX_WEB_DIST=<path>` override; `CLAUDEX_WEB_DIST=none` disables the static mount (use for Vite dev on 5173) | `server/src/index.ts` |
| ✅ | pino logger to `~/.claudex/logs/server.log`, pretty to stdout in dev | `server/src/lib/logger.ts` |
| ✅ | Graceful shutdown on SIGINT/SIGTERM — disposes session runners, closes app, closes DB | `server/src/index.ts` |
| ✅ | **Self-restart** — `POST /api/admin/restart` (login-gated). Spawns `scripts/restart-worker.mjs` as a detached child (Node's `child_process.spawn({detached:true})` handles the platform work: `setsid(2)` on Unix, `DETACHED_PROCESS + CREATE_NEW_PROCESS_GROUP` on Windows), replies 200 with `{restarterPid, port, log}`, then SIGTERMs itself so the standard shutdown handler runs. Worker polls the listen port via `net.createServer().listen()` (portable — no `lsof`/`netstat`/`ss`), then execs `pnpm exec tsx src/index.ts` once the port is free. 30s ceiling on the port-drain wait. Audit event `server_restart`. Also runnable manually as `node scripts/restart.mjs [port]`. Restart logs appended to `~/.claudex/server-stdout.log`. Designed so the Claude CLI — a subprocess of the server — can trigger its own host's restart without being killed mid-command when the server dies, which is the failure mode plain `kill && nohup ... & disown` has. UI surface: a **Force reload (clear cache)** icon button lives directly in the Home header, and a **Restart server** action lives inside the WebSocket-diagnostics panel that opens from the live/offline chip — a two-step inline confirmation (`Restart server` → `Yes, restart now` / `Cancel`, no `window.confirm`) kicks off the full-screen blocking overlay that polls `/api/health` and hard-reloads on success. Both are backed by shared helpers in `web/src/lib/admin-actions.ts` so the Settings → Advanced tab and the Home header share the same cache-busting + health-poll logic | `server/src/admin/routes.ts`, `scripts/restart.mjs`, `scripts/restart-worker.mjs`, `web/src/lib/admin-actions.ts`, `web/src/screens/Home.tsx` |
| ✅ | **Restart tool-call UX** — when `POST /api/admin/restart` carries `{sessionId, toolUseId}` in the body, the server writes a row into `pending_restart_results` (migration 19) BEFORE SIGTERMing itself. On the next boot a sweep (`resolvePendingRestartResults` in `server/src/transport/pending-restart-sweep.ts`) converts each row into a synthetic `tool_result` event with `isError=false` ("claudex restarted successfully."), force-idles the session via `SessionManager.forceIdle`, broadcasts a transcript refresh, and deletes the row. Rows older than 24h are pruned at the top of the sweep. Result: when Claude triggers a restart mid-tool-call, the chat UI renders a green tool result instead of a dangling "failed" tool_use. Back-compat: the body-less legacy call still works (just no synthetic result). Bash-path helper: `scripts/restart-self.mjs` wraps the HTTP call, reads the cookie from `~/.claudex/cookies.txt`, falls back to plain `restart.mjs` on HTTP failure | `server/src/admin/pending-restarts.ts`, `server/src/admin/routes.ts`, `server/src/transport/pending-restart-sweep.ts`, `server/src/db/index.ts`, `scripts/restart-self.mjs` |

## Auth

| Status | Feature | Where |
|---|---|---|
| ✅ | `POST /api/auth/login` — username + password, returns a short-lived `challengeId` (not yet logged in). Case-insensitive username. Uniform timing regardless of whether the user exists (always runs bcrypt). When the matched user has `totp_enabled = 0` (e.g. installed via `claudex init --skip-totp` or disabled from Settings) the route skips the challenge and issues the session cookie immediately, returning `{requireTotp:false, challengeId:null}` | `server/src/auth/routes.ts` |
| ✅ | `POST /api/auth/verify-totp` — six-digit TOTP with ±1 step tolerance (accounts for clock skew). On success, issues a JWT (HS256, 30-day TTL) in an httpOnly session cookie | same |
| ✅ | **TOTP enable / rebind / disable** — login-gated. `POST /api/auth/totp/begin` mints a fresh `secret + otpauth URI + inline SVG QR` without persisting; the client echoes that secret back through `POST /api/auth/totp/confirm` with a current 6-digit code from the new pairing **plus** one of `currentTotp` (when 2FA is already on — proves they still hold the OLD authenticator, blocking a rebind by anyone with only a stolen cookie) or `password` (when 2FA is currently off — first-time enable). `POST /api/auth/totp/disable` is always password-gated, wipes the secret, flips `totp_enabled = 0`, and clears every `recovery_codes` row for the user (no recovery target = recovery codes have nothing to recover) | `server/src/auth/routes.ts`, `server/src/auth/index.ts` (`UserStore.setTotpState / clearRecoveryCodes`), `web/src/screens/Settings.tsx` (`TwoFactorCard`, `TwoFactorEnrollModal`, `TwoFactorDisableModal`) |
| ✅ | Peek-then-consume: a wrong TOTP **does not** burn the challenge — the user can retry without going back to the password screen | same |
| ✅ | **2FA recovery codes** — 10 single-use fallbacks issued at `claudex init` (printed once to stdout) and regeneratable from Settings → Security. Code shape `xxxx-xxxx-xxxx-xxxx`, 16 alphanumeric chars from a confusion-free alphabet (no `0/1/o/l/i`), bcrypt-hashed at rounds=10. `POST /api/auth/verify-recovery-code` (body `{challengeId, code}`) mirrors the TOTP flow: shares the per-challenge sliding-window limiter, peek-then-consume on the challenge, flips `used_at` on match, and issues the same session cookie. Response body also reports `remaining` so UIs can nag. `POST /api/auth/recovery-codes/regenerate` (login-gated) wipes the prior batch (used or not) and returns the 10 plaintext codes exactly once. `GET /api/auth/recovery-codes/state` (login-gated) returns `{remaining, generatedAt?}` — never plaintext. Audit events: `recovery_codes_regenerated`, `recovery_code_used`, `recovery_code_failed`. Stored in migration id=12 `recovery_codes(user_id, code_hash, used_at, created_at)` with index on `(user_id, used_at)` | `server/src/auth/recovery-codes.ts`, `server/src/auth/routes.ts`, `server/src/auth/index.ts` (`UserStore.setRecoveryCodeHashes / consumeRecoveryCode / countRemainingRecoveryCodes`), `server/src/bin/init.ts`, `server/src/db/index.ts` (migration 12), `web/src/screens/Settings.tsx` (`RecoveryCodesCard`, `RecoveryCodesModal`), `web/src/screens/Login.tsx` (`RecoveryCodeInput` toggle) |
| ✅ | Cookie `Secure` attribute auto-detected from request protocol and `X-Forwarded-Proto`; override with `CLAUDEX_COOKIE_SECURE=1/0`. `SameSite=Lax`, `HttpOnly`, `Path=/` | same |
| ✅ | `GET /api/auth/whoami` — returns the logged-in user or 401 | same |
| ✅ | `POST /api/auth/logout` — clears the session cookie | same |
| ✅ | `POST /api/auth/change-password` — logged-in only. Requires the caller's current password (401 `invalid_credentials` on mismatch), min-8 on new password (400 `bad_request`), rejects identical-to-current (400 `same_password`). On success rotates the bcrypt hash and issues a fresh session cookie so the caller's tab keeps working. The JWT secret is not rotated, so other tabs' old cookies remain valid until their own `exp` — we don't maintain a revocation list yet | `server/src/auth/routes.ts`, `server/src/auth/index.ts` (`UserStore.setPasswordHash`) |
| ✅ | `GET /api/user/env` — logged-in only. Read-only reflection of the user's Claude CLI environment: the session user, `claudeDir` (absolute `~/.claude`), `settingsReadable`, and a merged plugin list keyed by `<plugin>@<marketplace>` — union of `~/.claude/settings.json` `enabledPlugins` and `~/.claude/plugins/installed_plugins.json`. Each entry carries `name`, `marketplace`, `version`, `installPath`, `enabled`. Missing or malformed files degrade gracefully rather than erroring | `server/src/sessions/user-env.ts` |
| ✅ | JWT signed with a 48-byte random secret persisted at `~/.claudex/jwt.secret` (mode 0600) | `server/src/auth/index.ts` |
| ✅ | Bcrypt password hashing (12 rounds); rejects passwords under 8 chars | same |
| ✅ | `otplib` TOTP with issuer `claudex`; current-code helper exposed for tests | same |
| ✅ | Short-lived login challenge store (5 min TTL) with peek + consume semantics | same |
| ✅ | **Rate limiting** on the auth endpoints — in-memory sliding-window limiter (`SlidingWindowLimiter`). `POST /api/auth/login` keyed by `req.ip`, max **5 failed attempts per 5 min**; `POST /api/auth/verify-totp` keyed by `challengeId`, max **10 failed attempts per 15 min** (stricter because the TOTP space is only 10^6). Successful credentials reset the login counter; a successful TOTP resets the challenge counter. Over-limit responses are `429 {"error":"rate_limited","retryAfterSec":N}` with a `Retry-After` header. Rate-limit trips audit as `login_rate_limited` / `totp_rate_limited` alongside the existing `login_failed` / `totp_failed` rows | `server/src/auth/rate-limit.ts`, `server/src/auth/routes.ts` |

## Projects

| Status | Feature | Where |
|---|---|---|
| ✅ | Login-gated REST CRUD | `server/src/sessions/routes.ts` |
| ✅ | `GET /api/projects` — list. Each row is enriched with `isGitRepo: boolean` (from `fs.lstat(<path>/.git)` at list time; per-row errors swallowed so one unreadable path doesn't blank the list) so NewSessionSheet and Chat rail quick-create can pre-gate their worktree toggle without a probe round-trip | same |
| ✅ | `POST /api/projects` — add. Rejects non-existent paths (400) and duplicate paths (409). Paths are resolved to absolute and stored verbatim | same |
| ✅ | `PATCH /api/projects/:id` — rename (only `name` is mutable; `path` changes would be a different project and are rejected at the schema level) | same |
| ✅ | `DELETE /api/projects/:id` — delete. Returns `409 has_sessions` with `{sessionCount}` if the project still owns any session (archived included) — FK is `ON DELETE RESTRICT` | same |
| ✅ | `POST /api/projects/cleanup-empty` — bulk-remove every project whose `countSessions` returns 0 (archived included). Wrapped in a single SQLite transaction so a concurrent session insert either lands before the sweep (project survives) or after (project's gone, FK is too) — never half-way. Returns `{removed, removedNames}` so the UI can toast a precise summary; idempotent — re-running with no empty projects returns `{removed: 0, removedNames: []}`. ProjectsSheet exposes a "Clean up empty" button (Sparkles icon) in the header next to the close X — only rendered when at least one project is in the list, fires a `confirm()` before calling, then refreshes + toasts the count. Sessions are unaffected; only project rows with zero sessions are deleted | `server/src/sessions/routes.ts`, `server/src/sessions/projects.ts` (`cleanupEmpty`), `web/src/screens/Home.tsx` (`ProjectsSheet`), `web/src/api/client.ts` |
| ✅ | `ProjectStore.setTrusted` + `.setName` + `.countSessions` + `.delete` + `.cleanupEmpty` | `server/src/sessions/projects.ts` |
| ✅ | `POST /api/projects/:id/trust` — flip trust bit (`{trusted: boolean}`). Idempotent, audited (`project_trusted` / `project_untrusted`). New projects default to `trusted=0` via HTTP; CLI-adopted projects auto-trust. `POST /api/sessions` returns `409 project_not_trusted` against an untrusted project. NewSessionSheet shows a "Trust this folder?" confirm card before proceeding; Settings → Security → Trusted projects lists every project with a pill + toggle | same, `web/src/screens/Home.tsx`, `web/src/screens/Settings.tsx` |
| ✅ | `projects.trusted` defaults to 0 — SQLite migration id=11 (`projects_trusted_default_zero`) is a marker-only migration (body is `SELECT 1`) paired with an application-layer change in `ProjectStore.create` so new rows created via the HTTP path no longer bypass the gate by unconditionally writing `trusted:true`. Pre-existing rows are left as-is (the user implicitly trusted them by running sessions there); only newly-created projects after the migration have to pass the `POST /api/projects/:id/trust` confirmation before a session can spawn | `server/src/db/index.ts` (migration 11), `server/src/projects/store.ts` |

## Filesystem browse

| Status | Feature | Where |
|---|---|---|
| ✅ | `GET /api/browse?path=<abs>` — lists immediate children. Entries are `{name, path, isDir, isHidden, size?, mtimeMs?}` sorted dirs-first then by name. Hidden (leading-dot) entries are returned with `isHidden: true` so the UI chooses visibility. Symlinks are classified via `lstat` and never followed; dangling symlinks show up as non-dirs instead of crashing the listing. `size`/`mtimeMs` added for the general Files browser; older consumers (FolderPicker, @-file mention sheet) ignore them | `server/src/sessions/browse.ts` |
| ✅ | `GET /api/browse/home` — returns `{path: os.homedir()}` for a "back to home" shortcut | same |
| ✅ | `GET /api/browse/read?path=<abs>` — read UTF-8 text contents of an arbitrary absolute-path file, 1 MB cap (`truncated: true`), binary detection (extension blocklist + 512-byte null-byte sniff → `415 binary_file`). Used by the general Files browser; the project-scoped `/api/files/read` is the sibling that also returns git annotations | same |
| ✅ | `GET /api/browse/raw?path=<abs>&download=<0\|1>` — streams raw bytes of any absolute-path file with an extension-based `Content-Type` (images, pdf, html, office MIMEs, audio/video, `application/octet-stream` fallback). Default `Content-Disposition: inline` for `<img>`/`<iframe>`/`<audio>`/`<video>` previews; `download=1` flips to `attachment` so the Office "Download" button actually saves. 50 MB cap → `413 file_too_large`; shares the same `400 not_absolute` / `404 not_found` / `400 is_a_directory` / `403 permission_denied` error surface. Cookie-auth-gated | `server/src/sessions/browse.ts` |
| ✅ | Errors: `400 not_absolute`, `404 not_found`, `403 not_a_directory`, `400 is_a_directory`, `415 binary_file`, `403 permission_denied` (EACCES/EPERM), `413 file_too_large` | same |
| ✅ | Login-gated alongside the rest of `/api/*` | same |

## Files browser

General-purpose read-only host-filesystem viewer (mockup s-14). Entered from the mobile tab bar (Sessions / Routines / Files / Alerts / Settings) and from the desktop sidebar at `/files`. Defaults to the user's `$HOME` and supports browsing anywhere on the host via Home / Up / Root toolbar + a clickable breadcrumb that also flips into an inline path input (pencil icon) so the user can paste/type an absolute path and jump straight there. The side listing and the file preview now share a single location: opening a file syncs the listing to the file's parent, and navigating the listing away from that parent drops the preview — no more stale preview from a different directory. A "Jump to project…" dropdown is still offered as a shortcut to registered project roots, but the browser is no longer project-scoped — you can view files outside any project.

| Status | Feature | Where |
|---|---|---|
| ✅ | Web `/files` screen — path-based general file browser. Defaults to `os.homedir()` via `GET /api/browse/home`; toolbar has Home (user home), Up (`data.parent`), Root (`/` on POSIX; current drive's root such as `D:\` on Windows), and a project-jump dropdown. Breadcrumb segments are individually clickable and are split via a cross-platform helper (`web/src/lib/path.ts`) so Windows paths (`D:\Code\AI\claudex`, UNC `\\server\share\...`) render as separate segments instead of one giant blob. A dedicated **Show hidden** pill (Eye / EyeOff) is always visible on the right side of the toolbar row — enables viewing dotfile-prefixed entries like `.git`, `.claude`, `.env`. Off-state shows a count badge so users can see hidden entries exist without toggling. Preference persists across navigations/reloads in `localStorage` (`claudex:files:showHidden`). Mobile: breadcrumb + toolbar + filter + flat listing + fullscreen preview sheet. Desktop: 3-col `[300px | 1fr | 240px]` with line-numbered preview + meta panel. Preview header has **Copy** (file contents) and **Path** (absolute path) buttons — both show a Check/`Copied` affordance for ~1.2s after click. HTTP-compatible clipboard fallback via hidden textarea + `execCommand("copy")` for non-secure contexts. Last successfully-opened file path is stashed in `sessionStorage` (`claudex:files:lastOpenedFile`) and auto-restored on next mount within the same browser session; if the file has since been deleted (`not_found`), the stash is silently cleared. Explicitly closing the preview also clears the stash | `web/src/screens/Files.tsx`, `web/src/lib/path.ts` |
| ✅ | **Typed file previews** in the preview pane — extension-driven renderer so common file types don't just show "binary_file". Kinds: `text` (existing line-numbered view), `image` (inline `<img>` via `/api/browse/raw` with `onError` fallback), `pdf` (inline `<iframe>` + always-visible Download button for mobile Safari fallback), `html` (default: source view with a **Render** toggle in the header that swaps to an `<iframe src="/api/browse/raw?path=…">` with `sandbox="allow-scripts"` — scripts execute so Tailwind Play CDN / Alpine / jQuery / inline init code all render the way they would in a real browser tab, and the real URL lets the document resolve relative asset / font / stylesheet references properly, but the iframe runs in a unique opaque origin so JS inside can't touch the parent or the claudex auth cookie. Previously rendered via `<iframe srcDoc sandbox="">` which blocked every script and had no base URL for relative paths, making CDN-driven pages and anything with `./foo.png`-style references render incorrectly. Toggle state resets on file change), `markdown` (`.md` / `.markdown` / `.mdown` / `.mkd` / `.mkdn`: default source view with the same **Render** toggle, which swaps to the shared `<Markdown>` component — react-markdown + remark-gfm, no HTML passthrough — so README-style files look like rendered docs instead of raw CommonMark), `audio`/`video` (native controls pointed at the raw endpoint), `office` (`doc/docx/xls/xlsx/ppt/pptx/odt/ods/odp`: friendly "browser can't render this" card with a prominent Download CTA — no in-browser office renderer bundled, too heavy). Copy-content button is disabled for non-text kinds; Copy-path remains available. Desktop right-rail meta panel shows Kind + Size for binary selections instead of the empty-state text. Image/pdf/audio/video skip `/api/browse/read` entirely — the tag loads bytes itself | `web/src/screens/Files.tsx` |
| 🟡 | Project-scoped `/api/files/tree` + `/api/files/read` + `/api/files/status` still exist with git-status + per-file +/- annotations. No UI surfaces them anymore — the Files screen now uses `/api/browse` + `/api/browse/read` so it can browse outside any project. The project endpoints remain for potential re-introduction of per-project git-annotated views (e.g. a diff-oriented tab) | `server/src/files/routes.ts` |
| ✅ | SECURITY: project-scoped `/api/files/*` rejects traversal via `resolveRelPath`. General `/api/browse*` accepts any absolute path — by design, this is a host-machine service and the user knows their own paths | `server/src/files/routes.ts`, `server/src/sessions/browse.ts` |
| ✅ | **Editable breadcrumb + unified location.** The Files breadcrumb has a small pencil icon that swaps the whole row into an `<input>` preloaded with the current absolute path — Enter submits, Esc / blur cancels. Submission is dispatched by `resolvePath`: try the entered path as a directory via `/api/browse`; if the server returns `not_a_directory`, re-route to `openFile(path)` so a pasted file path opens the preview. Non-absolute input is rejected inline with `not_absolute` on the listing error channel — same UX as clicking a now-missing folder. `currentPath` (side listing) and `selectedFilePath` (preview) share one location: `openFile` syncs `currentPath` to the file's parent, and a guard effect closes the preview whenever the listing navigates away from that parent. The initial `/api/browse/home` fetch no longer clobbers a `currentPath` set by the sessionStorage restore of the last-opened file | `web/src/screens/Files.tsx` |
| ✅ | Mobile tab bar now 5 tabs — Sessions / Routines / Files / Alerts / Settings. Queue drops to desktop-only sidebar (like Usage). `AppShell.MOBILE_NAV` excludes `usage` + `queue` | `web/src/components/AppShell.tsx` |
| ✅ | **主站 → 文件站 corner link** — the main-site Files tab's top bar ends with a small "文件站 ↗" link (mobile: right end of the breadcrumb row; desktop: right end of the left-panel first row), opening the standalone `/files.html` entry in a new tab. It is the mirror of the "主站 ↗" link in the file station's own top bar (`web/src/files-main.tsx`): that one escapes the chrome-less file station back to the main app; this one escapes the chrome-heavy main tab to the shareable full-screen station. Dev maps vite 5173 → 5174 (`fileStationHref`, mirroring `mainSiteHref`); built app is same-origin `/files.html`. Rendered only in embedded mode — `FilesScreen standalone` never links to itself | `web/src/screens/Files.tsx` |

## Slash commands

| Status | Feature | Where |
|---|---|---|
| ✅ | `GET /api/slash-commands?projectId=<id>` — returns the merged list that powers the composer's `/` picker: curated CLI built-ins, then `~/.claude/commands/*.md` (kind `user`), then `<project>/.claude/commands/*.md` (kind `project`, only when `projectId` is given). Each entry is `{name, description, kind, source?}`. Descriptions are parsed from YAML frontmatter (`description:`), a leading `# Heading`, or the first non-empty line — whichever lands first in the first 1 KB / 10 lines. Top-level `.md` only; dotfiles skipped; unreadable files are quietly skipped rather than 500-ing. Unknown `projectId` is soft-ignored (still returns built-in + user) | `server/src/sessions/slash-commands.ts` |
| ✅ | Built-ins are a curated list (`add-dir`, `bug`, `clear`, `compact`, `config`, `continue`, `cost`, `doctor`, `help`, `init`, `login`, `logout`, `mcp`, `model`, `plugin`, `pr-comments`, `resume`, `review`, `status`) — the `claude` CLI owns the real behavior, we just surface the token so the picker isn't empty. Each built-in carries a `behavior` field that tells the composer what happens when the user picks it: `native` (send the token through to the SDK), `claudex-action` (intercept client-side and open the mapped claudex UI), or `unsupported` (REPL-only, dim the row and block sends). Today's triage: **native** — `compact`; **claudex-action** — `model`→open-model-picker, `config`/`status`→open-session-settings, `cost`→open-usage, `mcp`/`plugin`→open-plugins-settings, `clear`→clear-transcript (stub), `help`→open-slash-help (stub); **unsupported** — `add-dir`, `bug`, `continue`, `doctor`, `init`, `login`, `logout`, `pr-comments`, `resume`, `review`. User/project/plugin custom commands are always `native` (the SDK resolves them from disk) | same |
| ✅ | Plugin commands — driven by `~/.claude/plugins/installed_plugins.json`. For each installed plugin we scan `<installPath>/commands/*.md`; multiple installed versions of the same plugin de-duplicate to the most recently updated install (by `lastUpdated`, else `installedAt`). Missing or malformed manifest is swallowed silently rather than 500-ing. Tagged `kind: "plugin"`; `source` points at the absolute `.md` path | `server/src/sessions/slash-commands.ts` (`scanPluginCommands`) |

## Sessions

| Status | Feature | Where |
|---|---|---|
| ✅ | `POST /api/sessions` — create. Body: `projectId`, `model`, `mode`, `title?`, `worktree`. When `worktree: true` the server creates a `<project>/.claude/worktrees/<sessionId>` worktree on a fresh `claude/<slug>` branch; see the "worktree lifecycle" row for the full round-trip | `server/src/sessions/routes.ts` |
| ✅ | `GET /api/sessions` — list. `?project=<id>` scopes to one project; `?archived=1` includes archived | same |
| ✅ | `GET /api/sessions/:id` — fetch one | same |
| ✅ | `GET /api/sessions/:id/events?sinceSeq=N` — replay persisted events. Also accepts `?limit=<N>` (capped 1..1000, default 200) and `?beforeSeq=<M>` for descending pagination, returning `{events, hasMore, oldestSeq}`. `limit` alone returns the last N events ASC; `limit + beforeSeq` pages older. Callers that pass no pagination params get the full history ASC with `hasMore:false` (back-compat for existing clients) | same |
| ✅ | `GET /api/sessions/:id/usage-summary` — lightweight per-session rollup (`{totalInput, totalOutput, lastTurnInput, lastTurnContextKnown, turnCount, perModel}`) computed server-side from `session_events WHERE kind='turn_end'`. Replaces the old "refetch all events just to compute last-turn context" path on the UsagePanel, ChatTasksRail, and Chat header ring — ~1 KB instead of the full transcript payload. **Accumulation aligns with Claude Code CLI's statusline math**: `lastTurnInput` (context ring numerator) follows the most recent non-empty `turn_end` even mid-turn, while `totalInput` / `totalOutput` / `turnCount` only accumulate when the turn actually ended (`stopReason` is neither `"tool_use"` nor `"unknown"`). The CLI JSONL importer (a) skips sidechain / subagent records entirely — their usage belongs to the child agent's context, not the parent session — and (b) preserves the raw `stop_reason` on each turn_end (`"tool_use"` for intermediate chunks, `"unknown"` when the source record had no stop_reason), so the shared accumulator doesn't triple-count the warm cache block across a turn's tool calls. Migration 25 clears turn_end rows that were written under the old buggy rules for any session previously resynced from a CLI JSONL (`cli_jsonl_seq > 0`) and resets `cli_jsonl_seq` to 0; the resync-on-open path re-imports those sessions losslessly from the original `~/.claude/projects/<slug>/<uuid>.jsonl` under the new importer | `server/src/sessions/routes.ts`, `server/src/sessions/usage-summary.ts`, `shared/src/usage.ts`, `server/src/sessions/cli-events-import.ts` |
| ✅ | **CLI resync on session open** — when `GET /api/sessions/:id` is called for a session with `sdkSessionId`, the server kicks off `resyncCliSession` in the background (never blocks the HTTP response). The resync locates the matching `~/.claude/projects/<slug>/<uuid>.jsonl`, counts lines, and if the file has grown past the session's persisted `cli_jsonl_seq` the new lines are mapped to `session_events` and appended. Deduped per-session so two parallel opens don't double-fetch. When new events land a `refresh_transcript` WS frame is broadcast; the web client reacts by refetching `/events?limit=200` and merging with any lazily-loaded older pages. Fire-and-forget: failures log-and-continue rather than failing the GET. **Gate: native claudex sessions are exempt** — `resyncCliSession` / `triggerCliResync` / the cli-sync watcher all early-return unless `session.adoptedFromCli === true`. Native rows have their own `<sdk-id>.jsonl` on disk (the SDK writes it as a side effect of spawning `claude`), but re-importing from that file was lossy — the CLI transcript format carries only the rendered `@<upload-path>` prompt, not the original attachment metadata, so resync would silently rebuild `user_message` payloads without their `attachments` field and the composer capsules would vanish. Migration 26 adds the `adopted_from_cli` column (default 0 for existing rows); `cli-import.ts` sets it to 1 on adoption. Migration 27 repairs already-poisoned `user_message` payloads by reverse-parsing the leading `@/<abs>` lines from `payload.text`, looking up matching rows in the `attachments` table by `(session_id, path)`, and stamping `payload.attachments` back into the event JSON | `server/src/sessions/cli-resync.ts`, `server/src/sessions/routes.ts`, `server/src/sessions/cli-import.ts`, `server/src/cli-sync/watcher.ts`, `server/src/db/index.ts` (migrations 26, 27), `shared/src/models.ts` (`adoptedFromCli`) |
| ✅ | **CLI process scanner (external `claude` → `cli_running` status)** — every 5 seconds the server walks `ps -axo pid=,args=` + `lsof -p <pid> -a -d cwd -Fn` to build a map of live external `claude` CLI processes → their SDK session ids. `claude --resume <uuid>` resolves directly from argv; plain `claude` resolves via cwd → `encodeCwdToSlug` → newest-mtime `*.jsonl` under `~/.claude/projects/<slug>/`. Basename filter is strict (`exe basename === "claude"`) so our own `claudex` node process and `claude-code` variants aren't caught. Reconciliation is narrow: only rows currently `idle` whose `sdkSessionId` is present in the map get promoted to `cli_running`; only rows currently `cli_running` whose `sdkSessionId` is NO LONGER in the map get demoted back to `idle`. All other statuses (`running` / `awaiting` / `archived` / `error`) are untouched — claudex's own state wins. Transitions broadcast via `SessionManager.broadcastStatus` so every subscribed tab sees the dot move within ~5 s. Watchdog is NOT armed for `cli_running` (the external process, not claudex, owns supervision). UX contract: `cli_running` is PURELY an observability signal — the web composer is unlocked, the user can still submit a message through claudex even while an external terminal is typing into the same JSONL. Visual: klein-toned pulsing dot + `CLI · RUNNING` pill on Home rows and Chat header; no new filter chip (v1 keeps the rail uncluttered). Web `shouldNotifyCompletion` skips toast firing on `cli_running → idle` transitions so the user isn't spammed each time their external CLI exits. Both the filesystem watcher (`cli-sync/watcher.ts`) and on-boot backfill skip rows already at `cli_running` so the JSONL mtime heuristic doesn't flap with the scanner. Disabled under `NODE_ENV=test` and via `CLAUDEX_WATCH_CLI=0` (same kill switch as the filesystem watcher). Honest limits: 5 s interval means idle → cli_running can lag up to 5 s; `ps` argv truncation on macOS (~256 chars) isn't a problem for `claude --resume <uuid>`; a plain `claude` whose cwd is not under `~/.claude/projects/<slug>/` (i.e. the user hasn't made any transcript yet) won't map and stays `idle` until the first write lands | `server/src/cli-sync/process-scanner.ts`, `server/src/index.ts`, `server/src/sessions/cli-discovery.ts` (`encodeCwdToSlug`), `server/src/sessions/manager.ts` (`broadcastStatus` accepts `cli_running`), `shared/src/models.ts` (`SessionStatus`), `web/src/screens/Home.tsx`, `web/src/screens/Chat.tsx`, `web/src/components/ChatSessionsRail.tsx`, `web/src/state/sessions.ts` |
| ✅ | **CLI live sync (filesystem watch)** — on boot the server starts a single `chokidar` watcher rooted at `~/.claude/projects` with `ignoreInitial: true`, `depth: 2`, `awaitWriteFinish: { stabilityThreshold: 200, pollInterval: 50 }`. `add` of a `<uuid>.jsonl` whose stem isn't in `sessions.sdk_session_id` auto-adopts via `importCliSession` (reuses the Import-sheet path, seeds events, stamps `cli_jsonl_seq`); `add` on an already-adopted stem falls through to the resync path. `change` looks up the row by `sdkSessionId` and calls `resyncCliSession`, which broadcasts `refresh_transcript` on append. **Dupe-prevention**: `onExistingChange` first checks `SessionManager.hasRunner(sessionId)` — when claudex is itself driving the session via AgentRunner, the SDK's JSONL writes are an echo of turns we already persisted through `handleEvent`, so the resync branch is skipped and we only bump `cli_jsonl_seq` to the new line count. When the runner disposes (session closed, runner crashed, server restart) subsequent CLI-driven edits resume from the correct offset and resync runs normally. For CLI-driven sessions (`claude` in terminal, claudex observing), `hasRunner` is false and resync fires as expected. `unlink` is a no-op — user deleting JSONL shouldn't silently wipe claudex state. Per-sessionId in-flight dedup means a flurry of writes triggers at most one concurrent op. **Running-status heuristic** (`deriveCliStatus`): tails the last ~8 KB of the JSONL to parse up to 5 trailing records, then picks status by first-match decision table — (a) most recent `type:"assistant"` record has `stop_reason` set → `idle` regardless of mtime; (b) last non-empty line is `type:"user"` with no later assistant (CLI is waiting for the user or a brand-new session with no reply yet) → `idle` (we don't have a distinct "awaiting_user" status today); (c) mtime `< 20s` AND last assistant record is `< 30s` old AND lacks `stop_reason` → `running`; (d) default → `idle`. Tightened from the old "mtime < 60s + no stop_reason" heuristic which mis-classified fresh user-only tails as running and flapped on slow tool calls; new thresholds deliberately err on idle to avoid composer-lockout false positives. `timestamp` field on each record (when present) is used to age the assistant record accurately; otherwise we fall back to the file's mtime. A `session_update` frame is broadcast whenever status changes (synthesized via `SessionManager.broadcastStatus`). Initial-status backfill on boot sweeps every row with `sdkSessionId` and runs the heuristic once — cheap (stat + tail per row), doesn't import events. Disabled under `NODE_ENV=test` (tests drive their own cases) and `CLAUDEX_WATCH_CLI=0` (emergency off-switch). Graceful shutdown awaits `watcher.close()` before DB close. **Honest limitations**: read-only mirroring — we only observe "file grew recently" / "file stopped growing", not "CLI process is alive"; a turn that pauses > 30s on a slow tool will briefly show idle before the next assistant chunk flips it back. Assumes default CLI layout `~/.claude/projects/<slug>/<uuid>.jsonl`; users with a relocated CLI state dir won't be observed. JSONLs at unexpected depths (not `<root>/<slug>/<file>`) are ignored for safety. **Known issue (pre-fix data)**: users who ran claudex between the watcher landing (`c455566`) and this fix may have duplicate `session_events` rows — each claudex-driven turn was imported a second time from the SDK's JSONL echo. The rows have distinct `seq` values (seq is assigned via `MAX(seq)+1` with no UNIQUE constraint, so the DB never rejected the duplicates), so an auto-dedupe query can't safely distinguish legitimate identical events (e.g. two bash invocations running the same command) from the regression. Manual cleanup is recommended on a per-session basis: eyeball `SELECT seq, kind, substr(payload,1,80) FROM session_events WHERE session_id='…' ORDER BY seq;` for adjacent-seq pairs with identical kind+payload, then `DELETE FROM session_events WHERE id IN (…)` the confirmed duplicates. Fresh sessions started after the fix won't accumulate more duplicates | `server/src/cli-sync/watcher.ts`, `server/src/index.ts`, `server/src/sessions/manager.ts` (`broadcastStatus`, `hasRunner`) |
| ✅ | **Session silence watchdog** — `SessionManager` arms a per-session `setTimeout` whenever a runner becomes active (first `sendUserMessage`, `status: running` event, permission decision, ask-user-question resolution, or edited-message re-run). Each subsequent runner event for the session resets the timer. If no events arrive for `CLAUDEX_SESSION_WATCHDOG_MS` (default `5 * 60 * 1000` = 5 minutes) the live path is **diagnostic-only**: a single `session watchdog: runner silent past window — leaving status untouched` pino warn line fires, and nothing else. The previous behaviour force-transitioned the row to `status = error`, appended a `watchdog_timeout` event, and broadcast a synthesized `error` RunnerEvent that drove the Chat composer into its errored-lockout — but in practice that fired far more often on legitimately-running long tool calls (big `pnpm install`, slow WebFetch, model chewing a big patch) than on actually-dead runners, and locked the user out mid-work. Now Stop + Send stay available throughout; if a row is genuinely stuck the user drives the existing `POST /api/sessions/:id/force-idle` escape hatch (surfaced as "Reset to idle" in SessionSettingsSheet, auto-revealed when `status === "running" \|\| "error"` and last activity > 2 min old) to recover. Terminal transitions — `turn_end`, runner `error`, explicit `idle` status, `dispose(id)`, `disposeAll()` — clear the timer. The timer is `unref`ed so it can't keep a dying Node process alive past SIGTERM. Env-overridable for operators and tests (vitest shortens it to 1s to exercise the fake-timer path). **Restart-survivability**: watchdog timers live in memory and don't survive a server restart, so `SessionManager.sweepStuckOnBoot()` runs once after the WS layer is wired and walks every row stuck in `running` / `awaiting`. Stale rows (`now - updated_at > watchdogMs` or unparseable timestamp) are **recovered to `idle`** — the runner subprocess definitionally did not survive the restart, so `idle` is the honest status; a `watchdog_timeout` error event is appended to the transcript for forensic visibility, a `session_watchdog_swept` audit row is written, and a `status: idle` frame is broadcast so every tab's composer unlocks. (No synthesized `error` RunnerEvent — that was the frame that scared the chat UI into the errored-lockout banner even when the previous turn was salvageable.) Rows still within the window get a fresh timer armed for the remaining time so they fire at the right wall-clock moment; the re-armed live timer is itself log-only as described above. Skipped in `NODE_ENV=test` so the vitest harness can drive the sweep directly. **Status transition logs**: every status change inside the manager routes through `transitionStatus(sessionId, to, reason)`, which emits a single `session status transition` pino log line with `{ sessionId, from, to, reason }` (reasons include `turn_end`, `runner_error:<code>`, `permission_request`, `user_forced_idle`, `boot_sweep_stale`, etc.); the CLI-sync watcher does the same for its own setStatus callsites. **Manual escape hatch**: `POST /api/sessions/:id/force-idle` drops any non-archived row back to idle and writes a `session_forced_idle` audit row; the web `SessionSettingsSheet` surfaces a "Reset to idle" link when `session.status === "running" \|\| "error"` and the last activity is > 2 min old | `server/src/sessions/manager.ts`, `server/src/sessions/routes.ts`, `web/src/components/SessionSettingsSheet.tsx`, `web/src/screens/Home.tsx` |
| ✅ | `POST /api/sessions/:id/archive` — mark read-only | same |
| ✅ | `DELETE /api/sessions/:id` — hard delete: drops the session row, every `session_events` row (FK CASCADE), any `/btw` side-chat children (self-referential CASCADE), and session-scoped `tool_grants`. Tears down the live runner first via `SessionManager.dispose(id)` so no more events are appended to a vanishing row. If a worktree exists, `removeWorktree` is called best-effort — delete must not fail on git cleanup. 404 on unknown id, 204 on success | same |
| ✅ | `POST /api/sessions/:id/side` — spawn a `/btw` child session that branches off an existing session. Copies the parent's `projectId` + `model`, defaults `mode` to `plan` (so side chats can't mutate the working tree unless the user explicitly flips it), and sets `parent_session_id` on the new row. Idempotent: if the parent already has an active (non-archived) side chat the existing child is returned instead of creating a duplicate. `409 archived` if the parent is archived, `404` if it doesn't exist. Paired `GET /api/sessions/:id/side` returns the active child or `null`. Side chats are hidden from the default session list (`parent_session_id IS NOT NULL`) so they don't clutter Home | `server/src/sessions/routes.ts`, `server/src/sessions/store.ts` |
| ✅ | **Fork session from a specific event** — `POST /api/sessions/:id/fork` `{upToSeq?, title?}` creates a new top-level session under the same project that inherits `project_id` / `model` / `mode` and copies every event with `seq <= upToSeq` verbatim (payload, kind, timestamp) with fresh event ids and a normalized 1..N seq. `upToSeq` omitted → fork at the latest event. `title` omitted → `"Fork of <source.title>"`, truncated at 60 chars with `…`. Status starts `idle`; `sdk_session_id` is **not** copied — the fork is a fresh SDK conversation and the model has no memory of being forked (this is an honest limitation; resuming mid-thread with full context would require either replaying every event through the SDK or threading tool_use/tool_result pairs into the CLI's JSONL, neither of which is wired). `parent_session_id` is null (forks are top-level, unlike `/btw` side chats). The new row's `forked_from_session_id` (migration id=14) points back at the source so the chat header can render a `GitFork` "Forked" pill with a tooltip making the SDK-context-reset honest to the user — the badge shows on both mobile and desktop headers whenever `session.forkedFromSessionId` is set; `forked_from_session_id` has no FK so deleting the source leaves the fork standing. Archived sources refuse with `409 archived` because forking from a read-only session is misleading; unknown id is `404`. Web: the per-message action row exposes a `GitFork` "Branch from here" button on `user` and `assistant_text` bubbles (desktop inline + mobile popover) that calls `api.forkSession(id, {upToSeq: seq})` and navigates into the new session on success | `server/src/sessions/routes.ts`, `server/src/sessions/store.ts` (`forkSession`), `server/src/db/index.ts` (migration 14), `web/src/components/MessageActions.tsx`, `web/src/screens/Chat.tsx`, `web/src/api/client.ts`, `shared/src/models.ts` |
| ✅ | `sessions.parent_session_id` — SQLite migration id=3, `TEXT REFERENCES sessions(id) ON DELETE CASCADE`. Deleting the parent cascades through every `/btw` child. Non-null rows are excluded from `list()` / `listByProject()` by default; `listChildren(parentId)` fetches a single parent's side chats | `server/src/db/index.ts`, `server/src/sessions/store.ts` |
| ✅ | `sessions.cli_jsonl_seq` — SQLite migration id=5 (`session_cli_jsonl_seq`), `INTEGER NOT NULL DEFAULT 0`. Tracks how many lines of the adopted CLI `<uuid>.jsonl` transcript have already been imported into `session_events`. Drives the idempotent resync-on-open path in `cli-resync.ts`: new lines past the stamped offset get mapped and appended; zero means either never-imported or a pre-column row, in which case resync falls back to diffing persisted-event count against JSONL line count | `server/src/db/index.ts`, `server/src/sessions/cli-resync.ts`, `server/src/sessions/cli-events-import.ts` |
| ✅ | `PATCH /api/sessions/:id` — partial update (`title`, `model`, `mode`). `mode` changes are pushed into the live runner via `setPermissionMode`; `model` changes are DB-only and the response carries `warnings: ["model_change_applies_to_next_turn"]` when a runner is already attached. Refuses `409 archived` on archived sessions and `400 bad_request` on empty bodies (at least one field required) | same |
| ✅ | `GET /api/sessions/:id/export?format=md\|json` — per-session transcript export. Login-gated; 404 on unknown id; default format is `md`. Markdown body is a header block (`# title` + session id / project / model / mode / timestamps) followed by every `session_events` row rendered by kind: `user_message` as `**You:**`, `assistant_text` as `**Claude:**`, `assistant_thinking` as a blockquote, `tool_use` / `tool_result` as fenced code blocks (full-fidelity — no per-result cap; a prior 4000-char silent-truncation was removed to honor CLAUDE.md's no-silent-truncation rule), permission events as blockquote status lines, `turn_end` as a `---` separator with a faint token/cache usage line, and `error` as a blockquote. Unknown event kinds are skipped rather than crashing. JSON body is `{session, events, project?}` — `events` is the full array (reuses `SessionStore.listEvents(id)`; not streamed). Response carries `Content-Disposition: attachment; filename="claudex-<sessionShortId>-YYYY-MM-DD.<ext>"` so the browser's native download flow handles it — the web client's `api.exportSession(id, format)` is a one-shot anchor click, no Blob. Session settings sheet footer carries an `Export ⌄` button (left of Archive) with an inline Markdown / JSON picker; available on archived sessions too | `server/src/sessions/export.ts`, `server/src/sessions/export-routes.ts`, `web/src/components/SessionSettingsSheet.tsx`, `web/src/api/client.ts` |
| ✅ | Every event gets a monotonic per-session `seq` and is written to `session_events` (payload as JSON) | `server/src/sessions/store.ts` |
| ✅ | Aggregate stats on the session row (messages, files changed, +/− lines, contextPct) bumpable via `bumpStats`; file/line diff stats refreshed out-of-band by `StatsRefresher` (see below) | same |
| ✅ | **`stats_context_pct` is populated on every `turn_end`** — manager computes `body = inputTokens + cacheReadInputTokens + cacheCreationInputTokens`, divides by `contextWindowTokens(session.model)`, and writes via `bumpStats({ contextPct })`. Historical turns below `HISTORICAL_TURN_THRESHOLD` (500) don't overwrite — column keeps its prior value so the ring doesn't blink to a misleading 0% when an old no-cache-fields turn replays. Unblocks the session-list context ring (previously always empty because nothing ever wrote this column). `contextWindowTokens` + `HISTORICAL_TURN_THRESHOLD` moved to `@claudex/shared` so both sides agree on the denominator | `server/src/sessions/manager.ts`, `shared/src/usage.ts` |
| ✅ | **One-shot boot backfill of `stats_context_pct`** — sessions whose most recent `turn_end` predates the runtime that stamps the column would otherwise render empty rings forever. `manager.backfillContextPctOnBoot()` (wired next to `sweepStuckOnBoot` in `buildApp`) scans non-archived rows where `stats_context_pct = 0 AND stats_messages > 0`, reads only the last `turn_end` per row (LIMIT 1), computes the same body/window pct, and absolute-writes via `setContextPctRaw` — which deliberately does NOT bump `updated_at`, preserving session-list ordering | `server/src/sessions/manager.ts`, `server/src/sessions/store.ts` (`listSessionsNeedingContextBackfill`, `setContextPctRaw`), `server/src/transport/app.ts` |
| ✅ | `worktree: true` creates a `<project>/.claude/worktrees/<session>` worktree on a new `claude/<slug>-<suffix>` branch; `false` keeps cwd at project root. Non-git project rejects with 400 `not_a_git_repo`. Archive cleans up the worktree but leaves the branch for manual disposition. **Both UI entry points — Home NewSessionSheet and the Chat rail's inline `+ New session` quick-create — expose a "Use git worktree" toggle that defaults ON for git repos** (tracked via `userTouchedWorktree` so flipping projects doesn't clobber an explicit user choice) and disabled-off for non-git repos; NewSessionSheet reads `isGitRepo` straight off the `GET /api/projects` row, QuickCreateForm looks it up lazily on mount from the same endpoint (or short-circuits to `true` when the current session already carries a `worktreePath`) | `server/src/sessions/worktree.ts`, `server/src/sessions/routes.ts`, `web/src/screens/Home.tsx`, `web/src/components/ChatSessionsRail.tsx` |
| ✅ | **Non-worktree session captures project branch** — when `worktree: false`, POST /api/sessions runs `git rev-parse --abbrev-ref HEAD` against the project path (2s timeout, best-effort) and persists the result to `sessions.branch`. Non-git projects / detached HEAD / git errors all write `null`, and the Home row renders an em-dash placeholder instead of the old `?? "main"` lie | `server/src/sessions/git-branch.ts`, `server/src/sessions/routes.ts`, `web/src/screens/Home.tsx` |
| ✅ | **`StatsRefresher` background sweeper** — every 30s (configurable via `STATS_REFRESH_INTERVAL_MS`), picks up to `STATS_REFRESH_BATCH_SIZE` (default 10) sessions whose event log has moved past their `stats_computed_seq` cursor, re-aggregates via `aggregateSessionDiff`, and absolute-writes `stats_files_changed` / `stats_lines_added` / `stats_lines_removed`. Archived sessions excluded. Auto-start gated on `NODE_ENV !== 'test'` like QueueRunner. Migration id 22 adds the cursor column (default `-1` = never computed) | `server/src/sessions/stats-refresher.ts`, `server/src/sessions/store.ts` (`setStats`, `listStaleStats`), `server/src/db/index.ts` (migration 22), `server/src/transport/app.ts` |
| 🟡 | Auto-title from first user message; boot-time backfill retitles historical sessions with placeholder titles. On first `sendUserMessage` for a top-level, non-side-chat session with an empty / `"Untitled"` / ≤3-word title, `SessionManager` derives a title from the first line of the message (trim, cap at 60 chars, snap to last word boundary if feasible, single-char ellipsis `…`). A one-shot `backfillSessionTitles` pass at server start replays the same rule against every non-archived top-level session that still has a placeholder title and has already received at least one `user_message` — so sessions that predate auto-title (or were created with `"Untitled"` and kept it) pick up a real title without any user action. Logs `backfilled titles: N/M`. Live broadcast of the new title is still TBD (surfaces on next Home refresh) | `server/src/sessions/manager.ts`, `server/src/sessions/backfill-titles.ts`, `server/src/index.ts` |
| ✅ | **Session tags** — user-authored labels for filtering sessions from Home. Stored as a JSON string array on `sessions.tags` (migration id=15, `TEXT NOT NULL DEFAULT '[]'`). Validation at the HTTP surface: each tag matches `[a-z0-9-]{1,24}` (`SessionTag` in `@claudex/shared`), max 8 tags/session; violations return `400 bad_request`. `PATCH /api/sessions/:id` accepts `{tags?: string[]}` — passing the field replaces the full list; passing `[]` clears it. SessionSettingsSheet renders a "Tags" section between Memory and Approved with pill chips + an inline input; Enter commits, empty-backspace removes the last chip, client-side normalization (lowercase + strip non-`[a-z0-9-]`) means typing "Backend Bug!" turns into "backendbug" instead of surfacing a validation error. Home reads `?tag=<name>` from the URL: when set, a klein-filled `#<name>` chip appears above the filter rail with an X to clear, and the session list narrows to rows whose `tags.includes(name)`. On desktop (`lg+`) up to 3 tag chips render next to each session title; clicking a chip toggles the filter (`stopPropagation` so the click doesn't also open the session). Mobile rows have no room for chips — filter is still reachable from the session's settings sheet. Tags round-trip through the backup export/import path | `server/src/db/index.ts` (migration 15), `server/src/sessions/store.ts` (`setTags` + `parseTags`), `server/src/sessions/routes.ts` (PATCH), `server/src/backup/export.ts`, `server/src/backup/import.ts`, `shared/src/models.ts` (`Session.tags`, `SessionTag`, `UpdateSessionRequest.tags`), `web/src/components/SessionSettingsSheet.tsx`, `web/src/screens/Home.tsx` |
| ✅ | **Session pinning** — user-authored bit that floats a session to the top of Home's list regardless of activity recency. Persisted on `sessions.pinned` (migration id=16, `INTEGER NOT NULL DEFAULT 0`). `PATCH /api/sessions/:id` accepts `{pinned?: boolean}`; store helper `setPinned` bumps `updated_at` on every flip. SessionSettingsSheet renders a toggle row at the top of the sheet (above Model) that flips pinned via the shared `patch()` helper; pinned state lights the row and chip klein-wash. Home's group builder sorts each project bucket by `[pinned desc, lastMessageAt desc]` so pinned rows float inside their project; at the group level projects containing any pinned session precede the rest (tie-broken on newest session's activity). Pinned rows render a small `Pin` (lucide) icon next to the title in both mobile and desktop layouts. Round-trips through the backup export path alongside tags | `server/src/db/index.ts` (migration 16), `server/src/sessions/store.ts` (`setPinned`), `server/src/sessions/routes.ts` (PATCH), `server/src/backup/export.ts`, `shared/src/models.ts` (`Session.pinned`, `UpdateSessionRequest.pinned`), `web/src/components/SessionSettingsSheet.tsx`, `web/src/screens/Home.tsx` |

## Alerts

| Status | Feature | Where |
|---|---|---|
| ✅ | **Persistent alerts queue** (migration 20 `alerts` table) — replaces the earlier in-memory `completions` map on the sessions store that evaporated on page reload and couldn't represent "seen but still active" state. Three kinds today: `permission_pending` (session entered `awaiting`), `session_error` (session entered `error`), `session_completed` (session transitioned `running→idle`). Each row has two orthogonal state bits: `seen_at` (user looked at the alert — drives the badge count) and `resolved_at` (underlying condition cleared — drives Active filter vs archival). Foreign key `alerts.session_id → sessions.id ON DELETE SET NULL` so deleting a session doesn't orphan-error the alert row. State transitions, NOT deletion: alerts never disappear just because the underlying condition cleared, they flip to `resolved` with a timestamp and stick around so the user can review what happened while off the app. Retention: boot-time `pruneOld` removes rows where `resolved_at IS NOT NULL AND created_at < now - 30 days` | `server/src/db/index.ts` (migration 20), `server/src/alerts/store.ts` |
| ✅ | **Status-transition hook** — `createAlertHook` (in `server/src/alerts/events.ts`) is wired into `SessionManager.setAlertHook` at boot. Fires AFTER every `transitionStatus` so the post-transition DB row is visible. Rules: entering `awaiting` → insert `permission_pending` (deduped — no double row if one's already open for the session); leaving `awaiting` → auto-resolve open `permission_pending` alerts for the session; entering `error` → insert `session_error` + auto-resolve open `permission_pending`; leaving `error` → auto-resolve open `session_error`; `running → idle` → **hard-delete any existing `session_completed` for the session, then insert a fresh row** (per-session dedup: a long-running session that completes 10 turns produces 1 alert, not 10, and each new completion bumps the badge back up with a fresh unseen row). Side-chat children (`parentSessionId` set) are excluded from all alert generation — the parent already surfaces user attention. Hook is wrapped in try/catch at both the manager's call site and inside itself so a flaky alerts path can't crash the runner | `server/src/alerts/events.ts`, `server/src/sessions/manager.ts` |
| ✅ | **REST**: `GET /api/alerts` returns `{alerts: Alert[], unseenCount: number}` (newest first, no pagination — the 30-day retention keeps the row count small). `POST /api/alerts/seen-all` bulk-marks every unseen alert as seen, returns `{ok, touched}`. `POST /api/alerts/:id/seen` marks one alert seen. `POST /api/alerts/:id/dismiss` stamps both `resolved_at` AND `seen_at` and records `payload.dismissedByUser = true` so the UI can distinguish user-dismiss from auto-resolve. All JWT-gated. Every mutation path calls `SessionManager.notifyAlertsUpdate` which fans an `alerts_update` frame to every authenticated tab's global WS channel | `server/src/alerts/routes.ts`, `server/src/transport/app.ts` |
| ✅ | **WS `alerts_update` frame** — global-channel ping (added to `GLOBAL_FRAME_TYPES` in `server/src/transport/ws.ts`, cross-session by design like `queue_update`). Payload is just a server-side timestamp; clients re-fetch `GET /api/alerts` to reconcile. Web client listens in `state/sessions.ts` via a dynamic import of `state/alerts.ts` to avoid an import cycle | `shared/src/protocol.ts` (`ServerAlertsUpdate`), `server/src/transport/ws.ts`, `web/src/state/sessions.ts` |
| ✅ | **Web client: Alerts Zustand slice** (`web/src/state/alerts.ts`) — holds `alerts`, `unseenCount`, `loading` plus `fetchAlerts / markSeen / markAllSeen / dismiss`, plus an exported `dedupBySession(list)` helper that groups alerts by `sessionId` and keeps only the latest per session (alerts with null `sessionId` pass through). `unseenCount` is recomputed locally from the deduped list on every fetch/mutation — the server's raw `res.unseenCount` is ignored because it double-counts when e.g. a `permission_pending` and a `session_completed` overlap transiently on the same session. AppShell reads `unseenCount` for the red badge; the Alerts screen consumes the same `dedupBySession` helper so badge count = visible row count | `web/src/state/alerts.ts`, `web/src/components/AppShell.tsx` |
| ✅ | **Alerts screen** — three filter tabs **All / Unread / Read** (default: **Unread** — the badge count already told the user what's new, so landing on the matching subset is the most direct read; the other tabs are one tap away). Counts reflect the deduped view. Auto-mark-all-seen on mount was REMOVED — badge persists until the user actively acknowledges. Acknowledge flow: tapping a row navigates to the session AND calls `markSeen(id)` so the badge drains one row at a time as the user works through it; `Mark all seen` button handles bulk. Row layout: colored dot + kind icon + title + body/subtitle + relative time + per-row dismiss button (hidden for already-resolved rows). Seen rows get a "seen" chip; resolved rows dim and show a "resolved" chip and no dismiss button | `web/src/screens/Alerts.tsx` |


## Chat loop (Claude Agent SDK)

| Status | Feature | Where |
|---|---|---|
| ✅ | One `AgentRunner` per live session, spawned on first message. Spawns the user's local `claude` CLI via `@anthropic-ai/claude-agent-sdk` — no direct Anthropic API call | `server/src/sessions/agent-runner.ts` |
| ✅ | Inherits `~/.claude/` settings, MCP servers, skills, plugins, CLAUDE.md from the user's config | SDK default behavior |
| ✅ | `env` merged with `process.env` on spawn (workaround for SDK v0.2.113 breaking change that made env replace rather than extend) | `server/src/sessions/agent-runner.ts` |
| ✅ | Async-iterable input queue — follow-up user messages don't restart the subprocess | same |
| ✅ | Captures the SDK's `session_id` from the first `system/init` message (stored on the runner; used for resume design) | same |
| ✅ | Translates SDK `assistant` / `user` / `result` / `system` messages into typed `RunnerEvent`s (text, thinking, tool_use, tool_result, turn_end, error, status). Malformed blocks are silently skipped instead of crashing | same |
| ✅ | `SessionManager` persists every event into `session_events`, bumps stats & status on `turn_end`, and broadcasts to WS subscribers | `server/src/sessions/manager.ts` |
| ✅ | Permission mode selectable per session: `default` (ask), `acceptEdits`, `plan`, `auto`, `bypassPermissions`. `auto` is passed through to the SDK verbatim — claudex relays the choice and renders the prompt cards; whether auto is truly autonomous depends on the CLI/classifier at the configured `ANTHROPIC_BASE_URL` (a channel without a classifier degrades to asking on the CLI's side). Previously claudex downgraded `auto` to `default` (the SDK rejected the value at MVP time; SDK ≥0.2.132 types it). `allowDangerouslySkipPermissions: true` is always passed to the SDK so `bypassPermissions` works both at launch and when switched to mid-session via `setPermissionMode` | `server/src/sessions/agent-runner.ts` |
| ✅ | `interrupt()` supported at the Runner and manager layer | same |
| ✅ | `setPermissionMode` wired end-to-end: the session settings sheet PATCHes `/api/sessions/:id` with a new `mode`, the server updates the DB and calls `Query.setPermissionMode` on the live runner | same |
| ✅ | Session resume via `resumeSdkSessionId` — the SDK `session_id` from the first `system/init` is persisted to `sessions.sdk_session_id` (first-write-wins, SQLite migration id=2) and passed as `resume` on subsequent `getOrCreate`, so re-opening an old session after a server restart continues the same Agent SDK conversation | `server/src/sessions/manager.ts`, `server/src/sessions/store.ts`, `server/src/db/index.ts` |
| ✅ | **Per-session thinking-effort level** — each session row carries an `effort` column (migration 23, NOT NULL DEFAULT `'medium'`) alongside model + mode, matching the SDK's built-in `Options.effort` five-point scale: `low` / `medium` / `high` / `xhigh` / `max`. The runner passes `effort: session.effort` to `query({ options })` on every start — the SDK maps that onto its adaptive-thinking budget internally, so claudex doesn't hand-roll a `thinking.budgetTokens` mapping. `thinking: { type: "adaptive", display: "summarized" }` still rides along so Opus 4.7's default `"omitted"` display is overridden (the UI wants something renderable when it gets thinking blocks). **Three surfaces** expose it: (1) a 5-point segmented PillPicker in the Chat header next to the model + permission-mode pills (`Low / Medium / High / X-High / Max`), (2) the composer bottom meta line (`Opus 4.7 · Ask · Medium · 200k ctx` — effort sits between mode and context window), and (3) the New Session sheet in Home renders a 5-column segment so the choice is baked into the session on creation. `PATCH /api/sessions/:id` accepts `effort` and propagates through `SessionManager.applyEffort` → `AgentRunner.setEffort`, which stores the new value for the NEXT SDK turn (the SDK's `effort` option is start-time only; in-flight queries keep the budget they were launched with — same deferral pattern `model` uses). A `effort_change_applies_to_next_turn` warning surfaces in the SessionSettingsSheet when the runner is mid-turn so the user knows the swap is deferred. **Defaults + model gating** (shared helpers `defaultEffortForModel` / `clampEffortForModel` / `effortSupportedOnModel` in `shared/src/models.ts`): new sessions default to the highest level their model supports — `xhigh` for Opus 4.7 and `high` for Sonnet 4.6 / Haiku 4.5. `xhigh` is Opus 4.7 only; the Home "New Session" sheet, the Chat header pill, and the SessionSettingsSheet all hide or disable the X-High button when the current model isn't Opus 4.7, and the server clamps `xhigh` → `high` when a PATCH switches the model away (covered by tests in `server/tests/session-routes.test.ts`) | `shared/src/models.ts` (`EffortLevel`, `Session.effort`, `defaultEffortForModel`, `clampEffortForModel`), `server/src/db/index.ts` (migration 23), `server/src/sessions/store.ts`, `server/src/sessions/runner.ts`, `server/src/sessions/agent-runner.ts`, `server/src/sessions/manager.ts`, `server/src/sessions/routes.ts`, `server/src/backup/export.ts`, `server/src/backup/import.ts`, `web/src/screens/Chat.tsx`, `web/src/components/SessionSettingsSheet.tsx`, `web/src/screens/Home.tsx` |
| ✅ | Agent SDK spawned with `thinking: { type: "adaptive", display: "summarized" }` so Opus 4.7 forwards summarized thinking text to the runner (default display is `"omitted"`, which keeps the model thinking silently) | `server/src/sessions/agent-runner.ts` |
| ✅ | **Side-chat context injection** — when a session has `parent_session_id` set (a `/btw` child) and has never spawned an SDK conversation before, the manager synthesizes a context seed from the parent's `user_message` + `assistant_text` events (tool calls / thinking stripped so the child isn't drowned in noise) and pushes it to the SDK as a synthetic first user message. Seed never hits the child's `session_events` log — the transcript only shows what the user actually typed. Resumes skip the seed because the SDK already has the full history | `server/src/sessions/manager.ts` |
| ✅ | **User-message broadcast** — `SessionManager.sendUserMessage` broadcasts a manager-synthesized `user_message` RunnerEvent (translated to a `ServerUserMessage` WS frame as `{type, sessionId, content, createdAt, echoId?}`) so every tab subscribed to the session sees the user's turn the moment it lands, not after `turn_end`. The originating tab stamps a per-send `echoId` nonce (`crypto.randomUUID`) onto its local optimistic piece and the outgoing `ClientUserMessage` frame; the server relays it verbatim on the broadcast. The web reconciler matches on `echoId` first (stable, collision-free) and falls back to the legacy content + `createdAt`-within-3s heuristic for broadcasts from other tabs or legacy clients that didn't stamp one — flipping `serverAcked` rather than rendering a duplicate. Other tabs insert a fresh piece above any trailing `pending` placeholder | `server/src/sessions/manager.ts`, `server/src/transport/ws.ts`, `web/src/state/sessions.ts`, `shared/src/protocol.ts` |

## Permissions

| Status | Feature | Where |
|---|---|---|
| ✅ | SDK `canUseTool` callback is bridged to a Promise queue keyed by `toolUseID` — the request sits until the UI answers | `server/src/sessions/agent-runner.ts` |
| ✅ | Permission requests are enriched server-side with a human-friendly `summary` and a `blastRadius` hint per tool (Bash, Edit/Write/MultiEdit, Read, Glob/Grep, WebFetch, WebSearch) | `server/src/sessions/permission-summary.ts` |
| ✅ | Three-decision UX: **Allow once / Always / Deny** — permission card is mockup-s-05-accurate. Mobile (<md) renders as a bottom-sheet shape with drag handle, warn-tinted `permission · ask mode` pill, title ("Run a shell command?" / "Edit {file}?" / "Write to {file}?" / "Apply N edits to {file}?" / "Fetch a URL?" / "Use {tool}?"), subtitle "Claude wants to run this in `<cwd>`", dark terminal-style command block (Bash highlights the binary token in `text-klein-soft`, continuation lines in `text-canvas/60`; non-Bash pretty-prints the tool input in the same block with header `{toolName} · tool`). Long bodies (>12 lines OR >1200 chars — e.g. Edit/Write JSON blobs with full file content) default to a collapsed 8-line preview with an `…` truncation hint and a klein-soft underlined "Expand (N lines)" / "Collapse" toggle, mirroring the plan-accept fold so the Allow/Deny actions stay on-screen, a Blast radius tile (Bash: destructive / test-build / shell; Edit-family: `Edits {filename}` + `+N −M`; WebFetch: `Network read` + domain), and three stacked action buttons — Allow once / Always allow `{signature}` / Deny. Desktop (md+) renders as a 560px modal-card with warning icon, header pill, title, mono meta line (`{project} · {branch} · {cwd}`), close-X (maps to Deny), terminal block with `cwd = …` in the header, a 1-up Blast radius tile, a "Why Claude is asking" panel with a "Remember this decision" checkbox that upgrades the "Allow once" footer button to `allow_always`, and footer actions Deny / Always allow / Allow once ⏎ (Enter while card focused submits). Diff-producing tools (Edit/Write/MultiEdit) swap the terminal block for the inline DiffView plus "Review full diff →" link. Card outer wrappers carry `max-w-full min-w-0` so a long signature can't push the transcript column past viewport width and spawn a page-level scrollbar; desktop footer actions use `flex-wrap` with Deny/Allow once pinned `whitespace-nowrap shrink-0` while the middle `Always allow {signature}` button truncates its mono signature inline (mobile stacked variant truncates the same way) | `web/src/screens/Chat.tsx` (`PermissionCard`) |
| ✅ | "Always" records a `ToolGrant` scoped to the session; matching future requests auto-approve without prompting the user. Signature conventions: Bash→command, Edit-family→file_path, Glob/Grep→pattern. Auto-approvals are **not** silent: the manager appends a `permission_decision` event with `{approvalId, decision: "allow_always", automatic: true, matchedGrantId}` so the transcript shows which grant fired for which tool use, and emits a matching `audit.append({event: "permission_granted", target: sessionId, detail: "auto: <tool> matched <sig>"})` row so the Security audit log doesn't lose the trail | `server/src/sessions/grants.ts`, `server/src/sessions/manager.ts` |
| ✅ | Session status flips `awaiting` → `running` as permission requests come in and are resolved. Same treatment for `AskUserQuestion` and `ExitPlanMode` — both are blocking interactions, not security gates, so the session still lights up as `awaiting` on the Home status dot + Chat header + side rails. Every flip synthesizes a `session_update` frame explicitly (the SDK runner never emits one for `awaiting` — that's claudex-side). `sendUserMessage` also broadcasts `running` on every turn so the "thinking" dot lights up on second-and-later turns, not just the first — without this the runner's one-shot `status:running` at consume-loop top left follow-up turns stuck on `idle` until the next `turn_end` | `server/src/sessions/manager.ts` |
| ✅ | `GET /api/sessions/:id/grants` lists session + global grants (scope annotated); `DELETE /api/grants/:id` revokes one. The session settings sheet renders these under "Approved in this session" with a per-row Revoke button | same + `web/src/components/SessionSettingsSheet.tsx` |
| ✅ | `GET /api/grants` returns every tool grant on the machine (global first, then session, each group sorted by `createdAt` DESC; session rows carry `sessionId` + `sessionTitle`). Settings → Security renders a **Granted tools** card between Recovery codes and Trusted projects: scope pill · mono signature · owning session title · short relative time · per-row Revoke, plus a two-click **Revoke all** footer action that iterates the visible list and refetches | `server/src/sessions/routes.ts` + `server/src/sessions/grants.ts` (`listAllGrants`) + `web/src/screens/Settings.tsx` (`GrantedToolsCard`) |
| 🟡 | No "Always, globally" (cross-session) decision yet — `addGlobalGrant` exists but isn't wired to the UI | same |
| 🟡 | Permission card "Remember this decision" checkbox (desktop) upgrades an `allow_once` into an `allow_always` for the current session only — it does NOT yet persist a project-scoped grant across server restarts. Follow-up: wire the checkbox to a new project-scoped row in `tool_grants` (see `server/src/sessions/store.ts`). | `web/src/screens/Chat.tsx` (`PermissionCard`) |
| ⬜ | Permission card Duration / Network tiles (desktop mockup s-05) intentionally omitted — we don't track tool-call duration or sandbox network access, so the tiles would have to fake data. Will ship once there's real data to put in them. | — |
| ⬜ | Permission card "Review later" link (mobile mockup s-05 footer) intentionally omitted — no inbox concept exists today. | — |

## Diff rendering

| Status | Feature | Where |
|---|---|---|
| ✅ | Inline diff view for `Edit`, `Write`, and `MultiEdit` tool calls — the diff renders both inside the permission card (so you can decide on the spot) and inline in the transcript | `web/src/components/DiffView.tsx` |
| ✅ | Handles create (new file) / overwrite / edit hunks; contextless hunks for Edit since we don't have the full file | `web/src/lib/diff.ts` |
| ✅ | **Full-screen Diff Review page** (`/session/:id/diff`, mockup s-06) — aggregates every diff-producing tool call in the session that's awaiting attention (pending `permission_request` without a matching `permission_decision`, OR in-flight `tool_use` without a matching `tool_result`). Desktop layout is a 3-column `[260px files · fluid diff · 320px summary]`: left rail is the file list with per-file `+N −M` and a klein dot on rows pending approval; center is the sticky-header diff using the same `<DiffView>` component used inline; right rail shows the permission's summary/title and a "Checks" placeholder ("No CI configured for this project" — we don't read project CI config yet). Mobile hides the rails and replaces the file tree with a top-sheet triggered by a "N files" chip. Sticky footer has "Approve all (N)" (klein button, disabled when nothing's pending) and a Back-to-chat link. Approve/Reject on individual files route through the same `resolvePermission` WS path as inline decisions. Reached from (a) the PermissionCard's "Review full diff →" link — which deep-links via `?approvalId=<id>` to preselect that file — and (b) a klein "Review diff" chip in the desktop Chat header that appears when `session.status === "awaiting"` and there's at least one diff-producing pending permission | `web/src/screens/DiffReview.tsx`, `web/src/App.tsx`, `web/src/screens/Chat.tsx` (`PermissionCard`, header chip) |
| ✅ | `GET /api/sessions/:id/pending-diffs` — aggregates the session's event log into an array of `{toolUseId, filePath, kind: "edit" \| "write" \| "multiedit", addCount, delCount, hunks, approvalId?, title}`. Powers the full-screen review page without the client having to replay the transcript. Returns `[]` for sessions with no pending edits; `404` for unknown sessions. Ports `web/src/lib/diff.ts`'s hunk builder into a pure server-side module so the shape is computed once and shipped verbatim | `server/src/sessions/diffs.ts`, `server/src/sessions/routes.ts`, `shared/src/models.ts` (`FileDiff` / `PendingDiffEntry` / `PendingDiffsResponse`) |
| ✅ | **Session diff summary** (`/session/:id/session-diff`, mockup s-15) — a PR-shaped aggregation of every Edit / Write / MultiEdit in the entire session (not just pending). Per-file status (`A`/`M`), per-file +/- totals, stitched hunks, and a chronological timeline of each change with its approval state (`accepted` / `rejected` / `pending` / `auto`). Entered from a `GitCompareArrows` icon button in the Chat desktop header (always available when a session loads) and from a "Session diff" row in the mobile `ChatMoreSheet`. Mobile layout stacks a summary card + tappable file rows (expand inline to show the diff) + inline timeline; desktop uses the 3-col `[260 | 1fr | 280]` grid with file rail (clicking a row focuses that file in the center pane, first file selected by default, `aria-current` + klein left-border + canvas fill on the active row — GitHub-style one-file-at-a-time view rather than a stitched stack), sticky-header diff block for the focused file, and a right rail holding the timeline plus a placeholder AI-review card | `server/src/sessions/session-diff.ts`, `server/src/sessions/routes.ts`, `web/src/screens/SessionDiff.tsx`, `web/src/screens/Chat.tsx`, `shared/src/models.ts` (`SessionDiffResponse`) |
| ⬜ | Line-by-line commenting, "Review code" button (AI re-review of a pending diff) — mockup'd but not implemented |  |

## WebSocket transport

| Status | Feature | Where |
|---|---|---|
| ✅ | `GET /ws` — one socket per browser tab, multiplexed across any number of sessions the client subscribes to | `server/src/transport/ws.ts` |
| ✅ | Cookie-authenticated handshake. Unauthenticated sockets get a single error frame and are closed | same |
| ✅ | `hello` / `subscribe` / `unsubscribe` / `user_message` / `interrupt` / `permission_decision` — all validated by zod discriminated unions from `@claudex/shared` | same + `shared/src/protocol.ts` |
| ✅ | Broadcasts: `hello_ack`, `session_update`, `assistant_text_delta`, `thinking`, `tool_use`, `tool_result`, `permission_request`, `turn_end`, `error` | same |
| ✅ | **Outbound-path OOM guard**. Large `tool_result` content (multi-MB grep / file-read / image b64) is clipped to **512_000 chars** (`TOOL_RESULT_WS_LIMIT`) at the WS mapper before the frame ships; the suffix reads `… [truncated — N chars dropped. Refetch transcript to see full content.]` and the frame carries `truncated: true`. The DB row in `session_events` stays intact — manager persists the full payload before the WS mapper runs, so `GET /api/sessions/:id/events` returns the unclipped content for a refetch. **Per-connection backpressure**: every `conn.send` checks `socket.bufferedAmount` against a 2MB budget; frames that would overflow are dropped for *that* socket (others in the same broadcast still get them) and a `slowCount` is incremented. After 10 consecutive skipped frames the socket is closed with code `1013 "try again later"` and the client's ua/remote are logged — prevents a dead-but-connected mobile tab from pinning unbounded Node memory. Backpressure applies uniformly to both the per-session subscribers fan-out and the global sessions channel because both route through the same `ConnState.send` helper | `server/src/transport/ws.ts`, `shared/src/protocol.ts` (`truncated` flag on `ServerToolResult`) |
| ✅ | **Global sessions channel**: every authenticated socket automatically joins a global broadcast set on `hello_ack`. Cross-session frames (`session_update`, `user_message`) are delivered to both per-session subscribers *and* the global set, so Home/list screens get live status dots without per-session `subscribe` frames. Turn-scoped frames (`assistant_text_delta`, `thinking`, `tool_use`, `tool_result`, `permission_request`, `turn_end`) still go only to explicit subscribers — idle list tabs don't get firehosed with transcript content | `server/src/transport/ws.ts` |
| ✅ | Malformed frames return a typed error frame; the socket stays open | same |
| ✅ | Auto-reconnecting WS client on the web side. Backoff is `min(1000 + 500 * attempts, 5000)` with ±30% jitter (`base * (0.7 + Math.random() * 0.6)`) so concurrent tabs don't lock-step after a shared blip. On every `acked` transition (initial connect AND every reconnect) the sessions store re-sends `subscribe` for the `activeSessionId` and calls `refetchTail` to pull events that landed during downtime. Auth-failure detection: close code `4401` OR a `{type:"error", code:"unauthenticated"}` frame flips a `disabled` flag, halts reconnects, and dispatches a `claudex:auth-lost` window event so the UI can route to /login | `web/src/api/ws.ts`, `web/src/state/sessions.ts` |
| 🟡 | `hello` carries a per-session `resume: {sessionId: lastSeq}` map so the server can replay missed events on reconnect — schema and storage are in place but the server-side replay is not wired yet (reconnect today relies on the client refetching `/api/sessions/:id/events?limit=200` on every `acked`, which the store merges via `refetchTail`) | `server/src/transport/ws.ts` |

## Terminal

| Status | Feature | Where |
|---|---|---|
| ✅ | `GET /pty` WebSocket endpoint — authenticated via the session cookie, attaches a `node-pty` subprocess rooted in the session's cwd (worktree path if present, else project root). Shell is `$SHELL` with a `/bin/zsh` / `/bin/bash` fallback; the client cannot pick the shell or inject env (server-side only, plus `TERM=xterm-256color`). JSON frames: server → `{data}` / `{exit, exitCode, signal}` / `{error, code, message}`; client → `{data}` / `{resize, cols, rows}`. One PTY per session — a second concurrent attach for the same `sessionId` is refused with `{error: "busy"}`. Archived sessions refuse with `archived`, unknown sessions with `not_found`. PTY is killed on socket close / error; socket is closed on PTY exit | `server/src/transport/pty.ts` |
| ✅ | TerminalDrawer in the Chat header — button next to Settings. Mounts an `@xterm/xterm` terminal with fit-addon, JetBrains Mono 13px, light palette matching the app canvas. Opens as a full-screen bottom sheet on mobile and a lower-right panel on desktop. Header shows the cwd and a live/connecting/closed/error indicator. ResizeObserver drives `fit + resize` frames so the PTY honors the actual viewport size. No auto-reconnect — a PTY session is stateful and silent reconnects would be worse than visible breakage | `web/src/components/TerminalDrawer.tsx`, `web/src/screens/Chat.tsx` |

## Web UI

| Status | Feature | Where |
|---|---|---|
| ✅ | **Visual identity — cool neutral, sans-only, denser on desktop**. The earlier "warm paper" palette (`#faf9f5` cream canvas, `#1f1e1d` warm-brown ink, Fraunces serif headlines, `#e8e4d8` warm rules) read as editorial / newspaper and dated the UI. The shell now uses zinc-scale neutrals (`canvas #fafafa`, `paper #f4f4f5`, `ink #09090b/#27272a/#52525b/#a1a1aa`, `line #e4e4e7/#d4d4d8`); klein orange `#cc785c` is preserved as the single saturated accent (it actually pops harder against cool neutrals than against cream). The `.display` heading class — used by ~35 page headers / sheet titles / metric numbers — was Fraunces 600 with optical sizing; it's now Geist 600 with `letter-spacing: -0.018em` so the wider call-site list switches typography in one place. Two stray `font-serif` callsites (SessionDiff totals, KeyboardHelp title) repointed to `.display` for the same reason. Fraunces is dropped from the Google-Fonts `<link>` to save the round-trip; the Tailwind `font-serif` token still points at it as a graceful fallback should we want to put a serif back somewhere later. Hardcoded warm hexes scrubbed from chart strokes (UsagePage, UsagePanel, PlanSheet, ContextPanel, Home, ChatTasksRail), the xterm.js light theme inside TerminalDrawer, the Login background grid, the ContextPanel sm shadow, and the PlanPanel checkmark. **Density / font sizing**: every `text-[Npx]` literal across `web/src` (~760 call sites) was reduced once via a banded mapping — sizes ≥18 px shrank by 3, 13–17 px by 2, 12–12.5 px by 1, ≤11.5 px stayed put as the chrome-readability floor. The previous attempt to add desktop density via a root `font-size` shrink at `≥768px` was reverted: the codebase has no rem-based text utilities, only literal-px arbitrary values, so the root hack only ever shrank Tailwind's rem-based **spacing** scale (padding, gaps, widths) — which made the layout feel cramped without making any text smaller. Spacing is back at its mobile-equivalent rem values on desktop, font reduction is now visible everywhere instead. Phones inherit the same reduced sizes (it's a single set of utility-class changes, not viewport-gated) — acceptable because mobile started smaller and the floor (≤11.5 px) was preserved. Border radii and 1-px borders are unaffected because they're px-based. `theme-color` meta dropped from klein to canvas so the iOS / Chrome address-bar tint no longer announces "warm orange brand" before the page paints | `web/src/styles/globals.css`, `web/tailwind.config.js`, `web/index.html`, plus the in-component scrubs cited above |
| ✅ | Login screen polished to mockup s-01 with a 2-step flow (credentials → 6-digit TOTP, recovery-code fallback). Desktop (≥md) is a `grid-cols-[1.05fr_1fr]` split — decorative left panel with logo / self-hosted badge / "Remote control for the claude…" tagline / subtle grid texture, form on the right; mobile collapses to the form column only with a compact wordmark on top. Calm two-step stepper (1 Sign in · 2 Verify — no pair step, single-user). Credentials step carries a "Remember this browser for 30 days" checkbox (klein dot-in-box) and a `h-12 bg-ink` Continue button. TOTP step renders six separate `h-12 rounded-[8px]` slots fed by a single offscreen `autoComplete="one-time-code"` input; the next-empty slot gets a klein border + `0 0 0 3px rgba(204,120,92,0.18)` ring and a blinking caret. Klein focus ring on every input wrapper. Welcome line personalises to `Welcome back, {username}.` once the user starts typing, falls back to `Welcome back.`. Distinct copy for rate-limit (`尝试次数过多，请稍后再试`) vs invalid credentials. Auto-clears the TOTP input on wrong code so the next attempt doesn't concatenate | `web/src/screens/Login.tsx` |
| ✅ | **AppShell** (`components/AppShell.tsx`) — global navigation frame used by every non-chat, non-login screen. Mobile: fixed bottom tab bar (h-58px, `bg-canvas/95 backdrop-blur`, active = 3px klein underline pill) with **Sessions · Routines · Queue · Alerts · Settings**. Desktop (≥md): 260px left sidebar with logo, same nav items plus Usage (desktop-only), a Projects list sourced from `/api/projects` with per-project session counts, and a pinned user profile card at the bottom. The top logo+wordmark is a `<Link to="/sessions">` (`aria-label="Go to sessions"`, `hover:bg-canvas/60`, klein focus ring) so clicking the brand returns to the Sessions home from any shell screen. Chat and Login intentionally bypass AppShell — they need full viewport for the composer and pairing screen respectively. **Scroll containment**: outer frame is `h-[100dvh] overflow-hidden flex`, `<main>` is `flex-1 min-h-0 flex flex-col overflow-hidden`, and every shell-wrapped screen places its `<header shrink-0>` beside a `<section flex-1 min-h-0 overflow-y-auto>` that becomes the actual scroll surface. Result: desktop sidebar stays fixed and the mobile bottom tab bar stays fixed while only the list area scrolls underneath (no more whole-page scrolling past the sidebar or behind the tab bar). Settings splits its inner 240px rail + content area into two independently scrollable columns on desktop; on mobile the whole column scrolls as one unit | `web/src/components/AppShell.tsx`, `web/src/App.tsx` |
| ✅ | Routing: `/` redirects to `/sessions`; `/sessions`, `/routines`, `/alerts`, `/settings` each wrap their screen in AppShell with the matching tab highlighted; `/session/:id` is the chat; `/login` is standalone. Unknown paths redirect to `/sessions` | `web/src/App.tsx` |
| ✅ | Home (`/sessions`): session list **grouped by project** with sticky group headers (klein dot + mono project name + session count), matching mockup s-02. Inside each group, session cards show status dot (idle/running/awaiting/archived/error), title, branch/model/mode. Groups with zero sessions are hidden. Group ordering is newest-session-first; within a group, sessions are sorted **strictly by `updatedAt` desc** (with pinned rows floating to the top). Each group is **collapsible**: by default it shows every non-idle session (awaiting / running / error / cli_running — always visible regardless of count) plus the top 3 most-recent idle sessions; any remaining idle sessions hide behind a `Show N more idle sessions` toggle in the group footer. Expanding shows the whole list; collapsing (`Show less`) returns to the default view. Expansion state is per-project and held in a `Set<projectId>` on the Home component. Header buttons: live WS connection indicator (with `aria-label="WebSocket diagnostics"`), projects-management gear, New session. H1 is `display text-[1.25rem] md:text-[22px]` to match the mockup's desktop size. **Desktop header (≥md)** also carries an inline search input (placeholder `"Search sessions, projects, files…"`, ⌘K / Ctrl+K focuses it from anywhere on the page, `×` button appears while querying to clear) that substring-filters the list by session title / project name / branch — files are in the placeholder copy aspirationally, we don't index file content. **Mobile header** exposes a dedicated `Search` icon button (`md:hidden`, `aria-label="Search"`) as the leftmost action in the right cluster so GlobalSearchSheet is reachable on touch without ⌘K. The filter chip rail carries a right-aligned `caps` "Group by project" label as a visual confirmation of the current grouping mode (not a toggle), uses `py-2 md:py-3` (mockup desktop s-02 line 492), and the ProjectGroup sticky header offsets `top-[45px] md:top-[53px]` to stay pinned right beneath it. Group headers show the project path in mono `hidden md:inline` so it's present on desktop and hidden on small screens where the header would wrap. **Mobile session rows** now mirror mockup s-02 (lines 370–403): CAPS status label row (dot + `NEEDS YOU`/`RUNNING`/etc + relative time on the right) above the title, the title line carries a 20×20 SVG context ring on the right (empty ring when `contextPct` is 0/unknown), subtitle/meta below. **Desktop grid rows** render a `caps worktree` pill next to the title when `session.worktreePath` is set, per mockup line 517 | `web/src/screens/Home.tsx` |
| ✅ | **Home filter chip rail** (mockup s-02 lines 492–500) — sticky, horizontally scrollable row above the list: **All** *n* / **Running** (green dot) / **Needs approval** *n* (warn dot) / **Scheduled** / **Mine** / **Archived**, plus a right-aligned "Group by project" hint on ≥sm. Active chip fills ink; inactive chips are canvas w/ a thin border. Filter state is URL-backed (`?filter=running`), so back/forward and deep-links just work; clicking the active chip clears to "All". "Scheduled" and "Mine" render at 60% opacity with explanatory tooltips (routines don't yet link to sessions; single-user install). "Archived" swaps the data source to `listSessions({archived:true})` — the default chip set excludes archived rows | `web/src/screens/Home.tsx` |
| ✅ | **Project filter from the sidebar**: clicking a project row in the desktop sidebar navigates to `/sessions?project=<id>`, which filters the Home list down to that project's group and shows a "× clear filter" pill under the header. An "All projects" row at the top of the Projects list clears the filter (`/sessions` with no query string). The mobile tab-bar Sessions button also clears the filter because it navigates to `/sessions`. The sidebar project row for the currently filtered project renders in the active pill style | `web/src/screens/Home.tsx`, `web/src/components/AppShell.tsx` |
| ✅ | Routines (`/routines`): full-page version of the old RoutinesSheet, reachable from the tab bar. Same list + editor dialog for cron preset picking, model/mode selection, run now, pause/resume, delete | `web/src/screens/Routines.tsx` |
| ✅ | **Alerts (`/alerts`)** — list of sessions that need the user. Derived live from the `sessions` store (no separate backend alert log): rows for every non-side-chat session with `status === "awaiting"` (permission pending, warn icon) or `status === "error"` (error icon), sorted awaiting-first then newest activity. Each row links to `/session/:id`; desktop shows project name + relative time on the right, mobile keeps just the time. Empty state says "No alerts. You'll see sessions that need permission here, or errors." The tab-bar Alerts icon shows a red badge with the same count (mobile: absolute-positioned `9+`-capped pill; desktop sidebar: `bg-danger` pill next to the nav label) so missed attention is visible from every screen | `web/src/screens/Alerts.tsx`, `web/src/components/AppShell.tsx` |
| ✅ | New-session bottom sheet: existing-project picker **and** "+ add new project" row coexist (no more mutual-exclusion bug); title input, model pills (Opus 4.7 / Sonnet 4.6 / Haiku 4.5), 4-way permission mode selector. Name auto-defaults to the folder's last segment if left blank. **Project list scrolls within a bounded `max-h-[280px]` inner scroller** so Title / Model / Permission mode / Start session stay on-screen regardless of project count — on mount the currently-selected project row is `scrollIntoView({ block: 'nearest' })`ed into view, and selecting the "+ add new project" row scrolls the expanded name/path/Browse inputs into view for the same reason | `web/src/screens/Home.tsx` |
| ✅ | **Folder picker** (`FolderPicker.tsx`) behind the "Browse" button — full-screen on mobile, modal on desktop. Walks the host filesystem via `/api/browse`, with Home/Up buttons, dotfile toggle, dirs-first list, "Select this folder" confirms at the current path. **Header path is tap-to-edit** (pencil icon) so users can paste or type any absolute path — essential on Windows hosts with multiple drives (`D:\Code`, `E:\projects`) where "Up" never reaches another drive root | `web/src/components/FolderPicker.tsx` |
| ✅ | **Project management sheet** (gear button in the Home header) — lists every project, inline rename via a per-row pencil button (Enter saves, Escape cancels, empty name shakes the input + toasts "Name can't be empty"), delete with friendly 409 `has_sessions` handling that tells the user to archive/delete sessions first. The **desktop sidebar** project list also supports inline rename: double-click a project name (or long-press on touch-capable devices) flips the row into an `<input>` with a Check-icon commit button, same validation + PATCH `/api/projects/:id` path | `web/src/screens/Home.tsx`, `web/src/components/AppShell.tsx` |
| ✅ | **Statistics sheet** (chart icon in the Home header, between Download and Settings) — one-shot fetch of `GET /api/stats` which runs a single snapshot transaction over `sessions` + `session_events`: session status bucket counts (total / active = `running`+`awaiting` / archived), total `turn_end` events, avg turns per non-archived session (1 decimal), busiest project by session count, top 5 tools by `tool_use` count (pure-CSS bar rail, each bar `uses / topUses * 100%` wide, klein fill), total + avg tokens per turn (summed from `turn_end.usage.{inputTokens, outputTokens, cacheReadInputTokens, cacheCreationInputTokens}`), oldest/newest session refs. Every card is backed by a literal query — no vanity metrics (no "days streak", no leaderboard). Zeros / nulls / empty arrays on an empty DB rather than 404. Bottom sheet on mobile, centered modal (`max-w-2xl`) on desktop; Esc-to-close, backdrop-click-to-close | `server/src/stats/routes.ts`, `web/src/components/StatsSheet.tsx`, `web/src/screens/Home.tsx` |
| ✅ | **Collapsible tool groups in Chat** (mockup s-19) — a run of 2+ consecutive tool_use pieces inside one claude turn folds into a single summary pill so the thread reads as alternating claude prose + compact tool summaries instead of `Read → Edit → Bash → Bash → Bash` dominating every screen. The summary row is a **four-band layout** — chevron · tool-chip stack (up to 4 unique icons + "+N" bucket) · `COUNT` + uppercase tracking-wider "STEPS" label + deduplicated tool-name list · status pill (✓ done / spinner running / ✗ failed) + live-ticking elapsed (`1.2s` / `1m 42s`) — with each band carrying its own background tint so the eye resolves "count — which tools — outcome — how long" at a glance. **Width is content-driven**: `w-fit max-w-full` when collapsed (short pill under the claude message, not edge-to-edge), `w-full max-w-[min(80ch,100%)]` when expanded so diffs / long bash output have room. No left gutter line. **State machine**: `defaultOpen = anyRunning \|\| !finalized` — a group stays expanded while any of its tools is in flight, OR while the current "claude in tool mode" stretch hasn't been closed out yet. `finalized` is set by a second reverse pass over `renderEntries` that flips a group to "closed business" the moment a non-tool piece (assistant_text / thinking / user / permission_request / ask_user_question / plan_accept_request) lands after it. The practical effect: back-to-back tool runs with brief gaps between them (group A completes → 200 ms pause → group B starts) **do not** flicker open/closed/open — the whole tool-mode stretch stays expanded until claude actually writes prose. `anyError` is deliberately NOT in `defaultOpen`: the latest errored group stays open naturally because no text has landed after it yet (`!finalized`), and any historical errored group has a finalizer after it so it collapses like a normal group. Manual taps flip a per-group override so the user can fold an active stream or re-open a historical group. **Tone classes**: `klein` wash while any child is running, `danger` wash when any child errored, neutral `paper` for all-green. A live 1s `setInterval` drives the running-group elapsed counter; cleaned up the instant the group goes neutral. **Group-breakers**: a text chunk, user message, permission prompt, ask_user_question, plan accept, pending placeholder, orphan tool_result, or a `Task`/`Agent`/`Explore` subagent dispatch (those have the indigo pointer + dedicated drawer — folding them would hide the delegation moment) all terminate the current run. **Everything else groups**, including diff-producing Edit/Write/MultiEdit and the TodoWrite plan pointer — when expanded, each child still renders its existing rich surface (DiffView, "Plan updated → view" button, etc.) inside the group body. Singleton tool_uses (`count < 2`) stay inline — no summary chrome adds value for one call. **Edit error banner** — when an Edit/Write/MultiEdit tool_use renders via DiffView and its matched tool_result has `isError=true` (e.g. "File has been modified since last read", "String not unique"), a small `bg-danger-wash/60 border-danger/35` banner tucked under the diff says `Edit didn't apply — <summary>`. Without this the DiffView showed the *proposed* hunks with no error surface of its own, so a failed edit looked identical to an applied one and any containing ToolGroup seemed to be flagging nothing (header said ERRORED but no visible culprit). **Mode interactions**: Verbose skips grouping; Summary filters tool_use pieces upstream. Expanding a group calls back into the same `<Piece>` render path the inline loop uses, so the body of an expanded group is visually and behaviorally identical to inline rendering — the group is pure chrome around existing cards. **Matched-result timing** — `matchedResultByToolUseId` memo carries each tool_result's `createdAt` ISO string, consumed only by `ToolGroup` for its first-tool-start → last-result-end duration math. Backwards compatible — other `matchedResult` consumers ignore the new field | `web/src/screens/Chat.tsx` (`buildRenderEntries`, `isGroupableToolUse`, `isFinalizerPiece`, `pieceKey`, `ToolGroup`, DiffView error banner) |
| ✅ | Chat screen: user messages as ink bubbles, assistant as flowing prose, thinking in an italic left-rule block, **tool_use + its matching tool_result merged into a single collapsible `ToolCallBlock`** — one chevron toggles both the pretty-printed input JSON and the result body, and the folded chip shows `{chevron} {toolName} {summarizeInput}` on the left with a muted one-line `summarizeResult` (first line / `N lines, M chars` / `✗ <err>` / `image only`) on the right. Orphan tool_results (no matching tool_use in the visible list) fall back to the standalone bubble. Outer block open still keeps the inner per-result "show N more chars" affordance so huge results don't blow the viewport; errored calls get a danger dot + subtle `bg-danger-wash/40` tint so failed tool calls are visible at a glance even when folded. Long unbroken tokens (URLs, absolute paths, command args) wrap via `[overflow-wrap:anywhere] break-all` in tool-result / tool-input `<pre>` and the permission-card `CommandBlock`, so mobile transcripts can't be forced into horizontal scroll by a single long link. **Shell layout**: flex-column with `h-[100dvh]`, so messages scroll inside their own pane and the composer stays pinned to the bottom of the viewport (fixes the old bug where the composer scrolled off with the transcript). Chat intentionally does **not** render AppShell — every vertical pixel goes to the transcript + composer. **Archived read-only gate**: when `session.status === "archived"`, the composer textarea is `disabled` (placeholder "This session is archived — read-only", opacity 0.7), the Send / Stop buttons are hidden, every chip (Slash, File, Attach, /btw, /compact) is disabled, drag-drop + image paste are no-ops, and the edit pencil on the last user bubble is hidden (`canEdit` requires `status === "idle"`). A subtle `Archive` banner above the composer reads "Archived · read-only — open a new session to continue." so the UI matches the server's `409 archived` refusal instead of letting the user type into a dead session. **Errored lockout**: when `session.status === "error"` (hard runner error from the SDK) the composer is locked out identically — same disables on textarea + chips + Send/Stop + drag/paste — but with a danger-styled banner "Session errored — start a new session to continue." so the user can tell the two terminal states apart. `running` and `awaiting` deliberately stay interactive: typing while busy queues per the intentional queue-while-busy behavior. **Image tool_results**: `extractImagesFromText` recognizes three shapes — inline `data:image/*;base64,...` URLs, `/api/attachments/:id/raw` references, AND the stringified Anthropic SDK image block `{"type":"image","source":{"type":"base64","media_type":"image/*","data":"..."}}` that `agent-runner.ts` emits when a Read tool reads a PNG/JPEG — so screenshots surface as clickable ImageThumbs (lightbox-ready) instead of a mono base64 dump. **Truncated tool_results**: when the WS back-pressure mapper clips a `tool_result` (`truncated: true`, see outbound-path OOM guard row), `ToolResultBlock` detects the sentinel suffix (`… [truncated — N chars dropped. Refetch transcript to see full content.]`), strips it from the inline `<pre>`, and renders a klein-ink underlined "Refetch full content (N chars clipped)" button under the block that calls `useSessions().refetchTail(sessionId)` to re-fetch the last 200 events from `/api/sessions/:id/events` (reads full payloads directly from `session_events` — WS clipping never touched the persisted row) and replaces the truncated piece with the untruncated one. Works for truncated results inside the refetched tail window; older truncated results outside the window keep the sentinel (the button still fires but only the tail is replaced) | `web/src/screens/Chat.tsx`, `web/src/lib/images.ts` |
| ✅ | **Desktop three-column Chat layout (≥ md)** — the Chat screen spreads across a 220px sessions rail · fluid center · 300px tasks rail. Left rail header is a **`Sessions {n}` title + black `+ New ⌘N` pill button** (the pill toggles the inline quick-create form below); a `<Link to="/sessions">` wraps the title so it doubles as the escape back to the global Home. Below the header sit two filter affordances: a **search input** (substring match across title / branch / projectId / `lastUserMessage`, with a decorative `⌘K` chip) and a **status segmented row** (`All / Active / Waiting / Idle`, counts updated live off the project-scoped list). **Rows** are flex containers with the **status dot vertically centered** across the row so it stays aligned regardless of how many text lines render; the right column shows title + `projectId · branch` (mono muted) on the left and a stacked timestamp + diff-stat block (`+148 −21` from `session.stats.linesAdded/linesRemoved`) on the right — when the session is `awaiting`, the diff stats are replaced with a two-line `awaiting / you` warn-orange badge so the call-to-action reads instantly. Active row gets a 2px klein left bar + canvas card lift + a klein ring around the status dot so "you are here" is anchored both at the row edge and at the dot itself. Rows are bucketed into **Now / Today / Earlier** (last hour or pinned · last 24h · everything else) with right-aligned counts; a rolling 60s tick re-buckets rows whose "Now" / "Today" labels just expired. Compact `railTime` formatter (`now / 2m / 5h / yest. / 2d / MM-DD`) replaces the global `timeAgoShort` "ago" suffix that ate space without adding meaning when every row is past tense. Rail is hidden below `md` so the mobile Chat layout is unchanged. Footer reports live WS connection (`connected` / `offline`). **Inline "+ New session" quick-create** still spawns a session in the current project without leaving the Chat view; the inline form is now toggled by the header's black pill instead of the previous dashed-border affordance. Form carries a first-prompt textarea (optional, Cmd/Ctrl+Enter submits, Esc cancels), a 4-way permission-mode segmented control (Ask / Accept / Plan / Bypass — defaults to the current session's mode), and a **Use git worktree** checkbox (resolved from the project's `isGitRepo` on mount — defaults ON for git repos, disabled-off for non-git, kept enabled while probing). Title is deliberately omitted from this form — the server derives it from the first prompt or "Untitled" | `web/src/components/ChatSessionsRail.tsx`, `web/src/screens/Chat.tsx` |
| ✅ | **Desktop tasks rail** — right-hand 320px panel listing in-flight and recently-completed tool calls as cards (status dot: running = green/pulse, awaiting = warn, done = ink-faint; body shows the tool's most relevant input field — command / file_path / pattern / url / query — or a truncated JSON fallback). Pending `permission_request` pieces surface as "awaiting you" cards. Footer renders a Context-window donut + `lastTurnInput / contextWindow(model)` using the same math as the Usage panel. Opened from the ⋯ overflow menu's "Tasks" row; state persists in `localStorage` under `claudex.chat.tasksRail`. **Closed by default** — the Settings rail (next row) is the desktop right-rail default; Tasks is on-demand so the user doesn't see two rails on first load. When both are open, Tasks slots second-from-right and Settings stays pinned rightmost — the render order in `Chat.tsx` puts Tasks before the Settings sibling so the anchor side is stable. No dedicated "task" abstraction server-side — purely a live view derived from existing `tool_use` / `tool_result` / `permission_request` pieces | `web/src/components/ChatTasksRail.tsx`, `web/src/screens/Chat.tsx`, `web/src/lib/usage.ts` |
| ✅ | **Desktop session-settings rail** — the Session Settings sheet (mockup s-10) doubles as the persistent right-rail on desktop instead of only opening as an overlay. `SessionSettingsSheet` accepts `variant="rail"`: when set, it renders as a flex-sibling `<aside>` (380px, `hidden md:flex`) with no scrim or `fixed` positioning, no Escape-to-close, and `useFocusReturn` disabled so the rail doesn't steal focus back when idle. Toggled via a `Settings2` (gear) button in the desktop chat header and opens by default on md+ (`matchMedia("(min-width: 768px)")`); state persists in `localStorage` under `claudex.chat.settingsRail`. **Shares the right-rail slot with Plan and Subagents via state-level mutex**: `openPlanRail` / `openSubagentsRail` / `toggleSettingsRail` / `toggleSubagentsRail` helpers in `Chat.tsx` clear the other two flags when opening, so at most one of the three `show*` booleans is ever true after any user interaction. Last-click-wins — clicking Settings while Subagents is open closes Subagents and opens Settings, and vice versa. Tasks is still a distinct sibling rail — only Settings/Plan/Subagent share the slot. Mobile still uses the overlay variant | `web/src/components/SessionSettingsSheet.tsx`, `web/src/screens/Chat.tsx` |
| ✅ | **Plan surface** (mockup s-16) — Claude's `TodoWrite` output is promoted out of the chat transcript into two persistent surfaces so the user can always glance at "where are we." (1) A sticky **PlanStrip** below the session header (both mobile and desktop) shows `[Plan] ●●⬤○○ 2/5 › Pinning server render locale to en-US ▾` — one dot per item when the list is ≤8 items, collapses to a single progress bar above that. The strip hides entirely when no plan exists so it never eats space on sessions that never use TodoWrite. (2) A full **PlanSheet** — bottom sheet on mobile, right slide-over on desktop (400px) — with the checklist grouped Done / Now / Next. In-progress items render with the activeForm in bold klein-wash + pulsing dot (not the imperative content), completed items as strikethrough with green check, pending items with hollow bullets. A 36px progress ring in the sheet header mirrors the strip counter. (3) An inline **lightweight pointer** replaces the old generic `TodoWrite` tool-chip in the chat (`N items · K done → view`) so the full list is never re-dumped as the agent rewrites it — each pointer opens the same sheet. No server changes, no migration: the snapshot is derived from the latest `tool_use` piece with `name === "TodoWrite"` via `selectLatestTodos(pieces)`, O(n) walk from the tail, cached with `useMemo`. Normalizes unknown statuses to `pending` and drops rows without a `content` string so malformed agent output doesn't crash the panel | `web/src/lib/todos.ts`, `web/src/components/PlanStrip.tsx`, `web/src/components/PlanPanel.tsx`, `web/src/components/PlanSheet.tsx`, `web/src/screens/Chat.tsx` |
| ✅ | **Timestamps across the UI** — every UIPiece carries the underlying event's `createdAt`, surfaced as a muted `mono text-[10px]` relative caption (`timeAgoShort`) with hover tooltip showing the absolute local timestamp. Chat bubbles render the time below user messages (right-aligned) and below assistant_text / tool_use / permission_request blocks. SessionSettingsSheet's Workspace card adds **Created** and **Last activity** rows (`timeAgoLong`). ChatSessionsRail shows last activity (`lastMessageAt ?? updatedAt`) on every row. ChatTasksRail task cards add a `started {rel}` caption. Queue rows add `queued / started / finished` captions based on `createdAt / startedAt / finishedAt`. Agents rows add `started {rel}` alongside the existing duration column | `web/src/lib/format.ts`, `web/src/state/sessions.ts`, `web/src/screens/Chat.tsx`, `web/src/components/SessionSettingsSheet.tsx`, `web/src/components/ChatSessionsRail.tsx`, `web/src/components/ChatTasksRail.tsx`, `web/src/screens/Queue.tsx`, `web/src/screens/Agents.tsx` |
| ✅ | **Chat header (mobile)**: back chevron → `/sessions`, title + meta line (status dot · project · model · mode), context ring → Usage panel, `Settings2` gear → overlay `SessionSettingsSheet`, three-dot `MoreVertical` → bottom sheet with `Tasks` (`ListChecks`), session diff, `/btw` side chat, and Open terminal. Subagents button (`Bot`) appears between the context ring and Settings when the session has any `Task` tool calls. Mirrors the desktop header layout — Settings lives in the header, Tasks lives inside the ⋯ dropdown — so the two surfaces expose the same entry points in the same shape. Replaces the old stacked row of ViewMode / ContextRing / Stop / /btw / Settings2 / Terminal buttons | same |
| ✅ | **Chat header (desktop ≥md)**: status dot + title/meta on the left (title block is `flex-1 min-w-0` so it truncates first); right rail is Model pill, Permission-mode pill, Thinking-effort pill (5-point: Low / Medium / High / X-High / Max), context ring, a `MoreVertical ⋯` overflow popover, and the `Settings2` (gear) settings-rail toggle. The overflow popover folds session diff, /btw side chat, session settings, and terminal behind a single button so the header stays breathable at tablet widths (≈ 768–1100px) where the sessions + tasks rails both compete for space — previously the Model pill would line-wrap ("Opus" + "4.7") because the flex row had 10 children with no shrink control. Model, permission-mode, and effort pills are real dropdowns bound to `PATCH /api/sessions/:id` — no round-trip through the session settings sheet for the common case. All pill labels now carry `whitespace-nowrap + shrink-0` so text can't split mid-word. Desktop drops the back chevron (navigation lives in the sidebar) per mockup s-04 line 967 | same |
| ✅ | **Desktop Chat polish (mockup s-04)**: thread scroller uses `md:px-6 md:py-6 md:space-y-6`; user bubbles expand to `md:max-w-[75%] md:text-[15px] md:px-4 md:py-3`; assistant markdown bumps to `md:text-[15px] md:leading-[1.65]`; composer outer padding `md:px-5 md:pt-3 md:pb-4` and the composer card is `md:rounded-[10px] md:p-2.5 md:bg-paper/50`; the circular Send swaps to a `Send + arrow` pill (`md:h-8 md:px-3 md:rounded-[8px]`), with a matching pill-shaped Stop when busy; composer foot appends a mono `{ctx}k ctx` suffix alongside model · mode. Mobile layout is unchanged | `web/src/screens/Chat.tsx` |
| ✅ | **Composer chip rail** above the composer (mockup 918-924): horizontally scrolling row of five chips — `/Slash`, `@File`, `Attach`, `/btw`, `/compact`. Slash/File open the same pickers as typing the sigils; /btw opens the side-chat drawer; /compact injects the literal token into the textarea. **Attach** opens the native file picker (mobile) and also accepts drag-and-drop onto the composer card (desktop) — uploads route through `POST /api/sessions/:id/attachments` and render as chips in a new row above the composer; image attachments show a 40×40 thumbnail from `previewUrl`, non-images show a filename + KB size chip. **Clipboard paste** of an image (⌘V / Ctrl-V with an image in the clipboard, including mobile browsers' long-press-paste after a screenshot) runs through the same `onFilesPicked` upload path as the button and drop zone — `onPaste` reads `ClipboardEvent.clipboardData.items`, filters `kind === "file" && type.startsWith("image/")`, and only `preventDefault`s when an image is actually present so plain-text pastes insert normally. The handler deliberately uses `clipboardData.items` (available on plain HTTP) rather than `navigator.clipboard.read()` (secure-context-only, would silently fail behind the frpc HTTP tunnel). × on a chip removes the unlinked attachment (DELETE). **Click-to-preview**: the 40×40 image thumbnail in the staged chip row is a button — clicking it opens the same `ImageLightbox` the transcript uses, seeded with every staged image attachment in display order so the user can arrow-nav through the whole batch before sending (catches "did I attach the right screenshot?" without having to send + un-send). Non-image chips stay inert. On Send the attachment ids ride along in the WS `user_message` frame; the server stamps `message_event_seq` so linked rows become immutable and the SDK prompt is prefixed with `@<abs-path>` tokens so Claude's Read tool can pick up the file contents. 5MB per file / 10MB per request / allowlist: `image/png,image/jpeg,image/gif,image/webp,text/plain,text/markdown,application/json,application/xml`; anything else returns 415. **In-transcript rendering**: once a user message carries attachments, the `UserBubble` splits them by MIME. Non-images render inside the dark bubble as filename+KB chips that open the raw file in a new tab. Images render as iMessage/ChatGPT-style **capsule pills BELOW the bubble** (`ImageAttachmentCapsules`) — each capsule is a paper-toned rounded card with a 40×40 thumbnail + filename + size, right-aligned under the bubble, clickable to open the same lightbox wired up for inline base64/`@path` image refs. Text-extracted image refs (base64 blocks emitted by Read on a PNG, or `@path` mentions) also flow through the capsule strip so every image attached to a user turn renders with the same visual language. When a user message is image-only (no remaining text, no file chips), the dark bubble is suppressed entirely and only the capsule strip renders — matching iMessage's "photo-only" message treatment. Image attachments sent via the composer are therefore visible inline in chat history, not just during the fleeting "staged" phase above the composer | `web/src/screens/Chat.tsx`, `web/src/api/client.ts`, `server/src/uploads/routes.ts`, `server/src/uploads/store.ts`, `server/src/sessions/manager.ts`, `shared/src/models.ts` (`Attachment`), `shared/src/protocol.ts` (`attachmentIds`) |
| ✅ | `attachments` table — SQLite migration id=7 creates `attachments(id, session_id REFERENCES sessions(id) ON DELETE CASCADE, message_event_seq INTEGER NULL, filename, mime, size_bytes, path, created_at)` with an index on `session_id`. Two-phase lifecycle: upload writes the file under `~/.claudex/uploads/<session-id>/` and inserts a row with `message_event_seq = NULL` (unlinked, user-deletable); send-time link stamps the row with the `user_message` event seq just appended, flipping it immutable (linked rows refuse DELETE with 404). Cascade-delete on the session wipes every attached file row | `server/src/db/index.ts` (migration 7), `server/src/uploads/store.ts`, `server/src/uploads/routes.ts` |
| ✅ | **Send ↔ Stop button swap**: the single circular action button in the composer swaps between an arrow Send (idle) and a red `StopCircle` (when `session.status` is `running` or `awaiting`). Interrupt flows through the same `ClientInterrupt` WS frame as before. Evicts the previous red Stop square from the chat header | same |
| ✅ | Permission card in-thread with Allow-once / Always / Deny buttons, diff preview for Edit/Write/MultiEdit | same |
| ✅ | **AskUserQuestion tool — in-transcript multiple-choice cards replacing the generic permission prompt for this tool.** When the SDK's built-in `AskUserQuestion` tool fires, `agent-runner.ts` detects `toolName === "AskUserQuestion"` and branches out of the `permission_request` flow; the manager persists the interaction as a new event kind `ask_user_question` (append-only — user answers land as a sibling `ask_user_answer` event). The WS layer ships a `ServerAskUserQuestion` frame; `web/src/components/AskUserQuestionCard.tsx` renders it as a klein-wash card with radios (single-select) or checkboxes (multi-select), per-option description + optional mono `<pre>` preview, and an auto-appended "Other" free-text row. **Multi-question asks render one-at-a-time with a `Q1/N` tab strip across the top** (shrink-0 chips, done-dot on answered tabs, active tab klein-filled) — the previous flat stack pushed the submit button off-screen on mobile when there were 3+ questions. Single-question asks keep the original no-tabs layout. Single-select picks auto-advance to the next unanswered tab via `queueMicrotask` (skipped for "Other" since the user still needs the free-text box). Submit stays globally gated on every tab being ready. **After submit the card auto-collapses (mockup s-20) into a compact "receipt" view** — one row per question, `Q{i}·{total} | question text | • chosen answer | caret` — replacing the full tab-strip + option-cards with a single scannable ledger so a session that asks 3–6 questions doesn't leave six 400-px tab cards choking the thread. The header keeps the same klein-wash chip; a muted `{N} answered` meta appears on the right. Per-row caret peeks the chosen option's description + `preview` inline (border box, mono preview unchanged) without opening sibling rows — no accordion-of-accordions. Free-text Other answers render as `Other → <mono chip>` in-row (not just the literal word "Other" which would lose the user's text). Multi-select answers comma-join their labels on one row. Footer adds an `Expand all` toggle (klein-outline pill, mono) that flips the card back to the full tab-strip read-only view (current behavior before this collapse) for anyone who wants to re-study the raw options; "Collapse" flips back. Peek + expand state resets on each transition into the answered state via `useEffect`. `canUseTool` resolves with `{behavior:"allow", updatedInput:{answers, annotations?}}` matching the SDK's `AskUserQuestionOutput`. Double-submit is idempotent (the pending map is keyed on `askId` and deleted before the resolver fires). Card is wrapped in an `ErrorBoundary` in the Chat transcript with a "Skip question" fallback so a render crash in the inner subtree can't white-screen the whole transcript (mirrors the PermissionCard guard); the on-change handler also hoists `e.currentTarget.checked` out of the `setSelections` updater because React can invoke the updater after the handler returns, nulling `currentTarget` and throwing `null.checked` | `server/src/sessions/agent-runner.ts`, `server/src/sessions/runner.ts`, `server/src/sessions/manager.ts`, `server/src/transport/ws.ts`, `shared/src/models.ts`, `shared/src/protocol.ts`, `web/src/components/AskUserQuestionCard.tsx`, `web/src/screens/Chat.tsx`, `web/src/state/sessions.ts` |
| ✅ | **ExitPlanMode tool — dedicated "commit to this plan?" card instead of the generic permission prompt.** The SDK's built-in `ExitPlanMode` tool fires at the end of a planning pass when the model thinks it's ready to start executing. `agent-runner.ts` detects `toolName === "ExitPlanMode"` and branches out of the `permission_request` flow (mirroring AskUserQuestion's carve-out); the manager persists the interaction as a new event kind `plan_accept_request` (append-only — the user's accept/reject lands as a sibling `plan_accept_decision` event). The WS layer ships a `ServerPlanAcceptRequest` frame; `web/src/components/PlanAcceptCard.tsx` renders it as a klein-wash card with a "plan · ready to execute" caps header, the plan text rendered as markdown inside a canvas-bordered box, and two buttons: klein-filled **Accept & execute** and outlined **Send back for revisions**. The plan body is collapsed by default to a short preview (first 10 lines or 400 chars, whichever clips more) with a klein-ink underlined "Expand (N lines)" / "Collapse" toggle so a long plan doesn't push the rest of the transcript off-screen; short plans render inline without the toggle. After decision the card stays in the transcript read-only with either a klein "✓ accepted" pill or an outline "✗ rejected" pill (expand/collapse remains available so the plan can be re-read). On accept `canUseTool` resolves `{behavior:"allow"}` — the SDK then transitions out of `plan` permission mode on its own for subsequent tool calls. On reject it resolves `{behavior:"deny", message:"plan not accepted — please revise"}` so the model sees a tool error it can recover from by regenerating the plan. Double-submit is idempotent (pending map keyed on `planId`, deleted before the resolver fires). **No permission_request / permission_decision event leakage** — covered by an explicit test in `server/tests/plan-accept.test.ts` | `server/src/sessions/agent-runner.ts`, `server/src/sessions/runner.ts`, `server/src/sessions/manager.ts`, `server/src/transport/ws.ts`, `shared/src/models.ts`, `shared/src/protocol.ts`, `web/src/components/PlanAcceptCard.tsx`, `web/src/screens/Chat.tsx`, `web/src/state/sessions.ts` |
| ✅ | Optimistic echo of user messages (shown before the WS ack) | `web/src/state/sessions.ts` |
| ✅ | **Inline "claude is thinking" placeholder** — after each user send the transcript gets a `pending` UI piece with three bouncing dots so the user can tell the request is alive. Cleared by the first substantive WS frame (`assistant_text_delta` / `thinking` / `tool_use` / `tool_result` / `permission_request` / `turn_end` / `error`) or a `session_update` back to `idle` / `error` / `archived`. After 90s of silence it appends a muted informational hint ("still working — no output yet. hit Stop above to cancel.") next to the dots — not an error, because claude is almost always still running (long thinking / slow tool), so the styling is deliberately low-key. Refresh-resilient: entering a Chat page whose `session.status` is `running` or `awaiting` seeds the same placeholder without waiting for a user send. Paired with a second-line note that the reply won't stream word-by-word (SDK limitation, see `memory/project_streaming_deferred.md`) | `web/src/screens/Chat.tsx` (`PendingBlock`), `web/src/state/sessions.ts` |
| ✅ | **Stop button in the composer** — when `session.status` is `running` or `awaiting` the round Send arrow in the composer flips to a red `StopCircle` icon. Sends a `ClientInterrupt` WS frame which the server routes to `SessionManager.interrupt` → `AgentRunner.interrupt` → `Query.interrupt` on the SDK. Single state-swapped button — no twin buttons side-by-side | `web/src/screens/Chat.tsx`, `server/src/transport/ws.ts`, `server/src/sessions/agent-runner.ts` |
| ✅ | **Queue-while-busy composer** — the send button only requires non-empty text (never gated on session state). Messages typed while claude is still processing are handed to the SDK's async-iterable input queue and processed after the current turn; the placeholder in the textarea reads "Type while claude thinks — will queue…" to set expectations | `web/src/screens/Chat.tsx`, `server/src/sessions/agent-runner.ts` |
| ✅ | **Prompt history recall in the composer** — `↑` / `↓` step through previously-sent prompts like bash/readline. Per-session: entries are stored under `localStorage['claudex.prompt-history.<sessionId>']` as a JSON array, most-recent-LAST, capped at 30; adjacent duplicates are deduped so spam-retrying the same prompt doesn't pad history. `↑` activates only when no `/` / `@` picker is open (that guard runs first and keeps priority), and the caret is at `0,0` or the textarea is empty; first `↑` stashes the current draft, subsequent `↑` walk backwards. `↓` walks forward and restores the stashed draft at `historyIndex === 0`; requires the caret to be at `value.length`. `Escape` while recalling cancels and restores the draft. Editing the recalled text exits recall without restoring — the user has taken ownership. Caret jumps to end-of-text on every recall so typing continues naturally. Silent no-op for sessions with no prior history | `web/src/screens/Chat.tsx` (`Composer`) |
| ✅ | **Edit last user message (typo recovery)** — hovering the newest user bubble while `session.status === "idle"` surfaces a small Pencil affordance at the bubble's top-left. Clicking swaps the bubble for an inline textarea pre-filled with the current text plus Cancel / Save & resend. Save hits `POST /api/sessions/:id/edit-last-user-message`, which locates the last `user_message` event via `SessionStore.findLastEventByKind`, deletes every event with `seq > that.seq` through `deleteEventsAboveSeq` (so the obsolete assistant reply + tool calls + permission prompts disappear, with FTS rows cleared in the same pass), rewrites the user_message payload via `updateEventPayload` with the new text + an `editedAt` ISO stamp, bumps `last_message_at`, then calls `SessionManager.rerunFromEditedMessage` which flips status → `running`, broadcasts a `refresh_transcript` frame, and pushes the new text into the SDK's input queue for a fresh assistant turn. Error codes: `409 not_idle` (turn in flight — would race the truncation), `400 no_user_message` (session has no user turn yet), `400 has_attachments` (message carried file attachments — not yet editable because we don't have a re-link/delete story for those rows), `409 archived`. Bubbles whose last message has attachments render a small "attachments can't be edited yet" hint next to the (affordance-less) bubble. **CLI-parity caveat**: for sessions adopted from `claude` CLI the underlying JSONL at `~/.claude/projects/<slug>/<uuid>.jsonl` is NOT rewritten, and the Agent SDK's in-memory conversation history also still contains the pre-edit message + any prior reply — it will replay both before the edited message when formulating the next turn. We ship it anyway: the web UX win (fix a typo without resending) outweighs the CLI-side divergence; users who switch back to CLI on the same session will see an inconsistent transcript from that point forward | `server/src/sessions/routes.ts`, `server/src/sessions/store.ts`, `server/src/sessions/manager.ts`, `server/src/search/store.ts`, `web/src/screens/Chat.tsx` (`UserBubble`), `web/src/api/client.ts`, `web/src/state/sessions.ts` |
| ✅ | Transcript is reconstructed from both persisted events (initial load via `/api/sessions/:id/events?limit=200`) and live WS frames, unified into a single UI piece list. Initial load fetches the last 200 events; older history is lazy-loaded in 200-event pages as the user scrolls to the top of the message list (`loadOlderTranscript` → `?beforeSeq=<lowestSeq>&limit=200`), with scroll position anchored so prepended pieces don't jump the viewport. A small "Loading older messages…" loader shows while the fetch is in flight, a "Loading transcript…" skeleton fills the initial-load blank, and `refresh_transcript` WS frames (emitted by CLI resync) trigger a tail refetch without discarding already-loaded older pages | same |
| ✅ | Sign out clears the session cookie and returns to the login screen | `web/src/screens/Home.tsx` |
| ✅ | **Composer pickers** — typing `@` after whitespace pops a file-mention sheet (reuses `/api/browse`, defaults to the session's project root, inserts `@<relative>` or `@<abs>` fallback outside the root); typing `/` at the start of a line pops a slash-command sheet populated at mount from `GET /api/slash-commands?projectId=<id>` — merges the CLI built-ins, the user's `~/.claude/commands/*.md`, and the active project's `.claude/commands/*.md`, each entry tagged with its `kind` shown as a badge (plugin entries render with an `sk` icon + klein `skill` badge, matching mockup s-09). Network/auth failure falls back to a tiny built-in list (`help / clear / compact / review`) so the picker is never empty. Both sheets share the s-09 bottom-sheet language on mobile; the `@` picker renders as an anchored popover above the composer on desktop (≥md) with a compact header strip (`@` + folder prefix + matches · ↑↓). Keyboard navigation: ↑/↓ move the highlighted row, Enter inserts it, Escape closes — forwarded from the composer textarea via an imperative `PickerHandle` ref so focus stays in the textarea. The matched substring of the query is wrapped in `text-klein` on each row (first-occurrence substring highlight, not fuzzy). The slash sheet also shows a `Recent` section above the full list (last 6 picks, persisted in localStorage under `claudex.slash.recents`) — shown only when the query is empty. **Pinned favorites**: each row carries a trailing star (outline = unpinned, filled klein = pinned); clicking toggles the command name in `claudex.slash.favorites` (localStorage, cap 10, oldest evicted on overflow) without firing the insert. When the query is empty and favorites exist, a `Pinned` caps header renders FIRST above Recent (saved pin order, not alphabetical), and the Recent group filters out anything already pinned so rows don't duplicate. **Slash behavior triage**: each slash row carries a `behavior` from the server; `claudex-action` rows intercept on select and open the mapped UI instead of inserting the token (e.g. `/model` opens the session settings, `/cost` opens Usage, `/mcp` jumps to Settings → Plugins); `unsupported` rows render with `opacity-60` and a `REPL only` badge, a tap doesn't insert but surfaces a short reason in the footer slot, and sending a message whose first token is an unsupported command is blocked with a one-line danger hint under the composer (`/doctor is CLI-only — can't send from claudex.`). Only `native` picks and unknown (custom/user/project/plugin) rows actually insert `/name`. Side-rail icons also open the pickers explicitly. **Pull-to-dismiss**: on mobile the drag handle at the top of each sheet is wired — pointerdown grabs, pointermove translates the sheet 1:1, release past 80px closes the sheet, otherwise it springs back with a 200ms transition (tracking stays direct during drag, transition only applied on release). Mouse pointers are ignored so desktop popover / modal layouts are unaffected. Shared via `web/src/hooks/usePullToDismiss.ts`. **Omitted from the mockup**: per-file `TypeScript · 42 lines` metadata and `edited 2m ago` freshness — `/api/browse` returns name/kind/hidden only, so adding language sniffing + mtime would need new server fields (out of scope for this polish pass) | `web/src/screens/Chat.tsx`, `web/src/components/SlashCommandSheet.tsx`, `web/src/components/FileMentionSheet.tsx`, `web/src/hooks/usePullToDismiss.ts`, `web/src/lib/slash-commands.ts`, `web/src/api/client.ts` |
| ✅ | **Conversation compaction (`/compact` lifecycle)** — when the user types `/compact` (or the SDK auto-compacts as the context fills) the Agent SDK fires three messages we now translate end-to-end: `system/status` with `status:"compacting"` (start), `system/compact_boundary` with `compact_metadata` (the actual divider — trigger=manual/auto, pre_tokens, post_tokens, duration_ms), and `system/status` with `status:null` + `compact_result:"success"\|"failed"` (end, with optional `compact_error`). `agent-runner.ts` emits two new RunnerEvents: a transient `compact_status` (drives a "Compacting…" pill next to the chat title via `useSessions().compacting[id]`) and a persisted `compact_boundary` event. The boundary lands in `session_events` under a new `compact_boundary` EventKind so the transcript still shows the divider on reload + survives export. The chat transcript renders it as a hairline rule with a center chip (`compacted · manual · 178k → 12k · 4.2s`); it's also marked as a finalizer for tool groups so any preceding `ToolGroup` collapses post-compaction. Defensive: a `session_update` to `idle` / `error` / `archived` clears the compacting flag in case the matching `end` frame is missed (server restart mid-compaction). The runner's per-call `lastAssistantUsage` snapshot is dropped on `compact_boundary` so the next `turn_end` doesn't bake the pre-compaction prompt size into the context-ring %. Failed compaction toasts the SDK's `compact_error` once, then clears the pill. **Compaction summary surfaced (new):** the SDK writes the post-compaction summary as an `isCompactSummary:true` user message in its own transcript JSONL with `isVisibleInTranscriptOnly:true`, which means it never reaches us through the streamed SDKMessage interface. After every boundary, `agent-runner.ts` fire-and-forgets a `harvestCompactSummary` task that resolves the SDK transcript path (`~/.claude/projects/<cwd-slug>/<sdkSessionId>.jsonl`, slug = `cwd.replace(/[/.]/g, "-")`), tail-reads the last 256 KB on a 250 ms poll for up to ~12 s, parses from the end for the first `isCompactSummary:true` row whose timestamp ≥ the boundary's `at` (1 s skew tolerance), and emits a separate `compact_summary` RunnerEvent carrying `{ boundaryAt, content }`. Manager persists it as a new `compact_summary` EventKind row (not folded into the boundary row — append-only), and the WS layer broadcasts a `ServerCompactSummary` frame. The web store folds the summary onto the matching `compact_boundary` UIPiece by `at === boundaryAt` for both live frames and replay (events → pieces builds a `boundaryAt → idx` map). `CompactBoundaryDivider` only renders the "Show summary" toggle once `summary` lands; expanding drops a mono panel under the divider with the SDK's full summary text (`whitespace-pre-wrap`, scrollable `max-h-[60vh]`, copy button via `copyText` so it works behind the HTTP frpc tunnel). Wire shape: `ServerCompactStatus` (transient, no seq), `ServerCompactBoundary` (seq-bearing), and `ServerCompactSummary` (seq-bearing, carries `boundaryAt` + `content`) in `shared/src/protocol.ts`; all session-scoped. The composer chip rail's `/compact` button still just inserts the literal `/compact ` token — the SDK does the actual work | `shared/src/models.ts` (`EventKind.compact_boundary` + `compact_summary`, `CompactStatusPayload`, `CompactBoundaryPayload`, `CompactSummaryPayload`), `shared/src/protocol.ts`, `server/src/sessions/runner.ts`, `server/src/sessions/agent-runner.ts` (`harvestCompactSummary`, `findCompactSummary`), `server/src/sessions/manager.ts`, `server/src/transport/ws.ts`, `web/src/state/sessions.ts`, `web/src/screens/Chat.tsx` (`CompactingPill`, `CompactBoundaryDivider` + summary toggle) |
| ✅ | **Quick "+ New" button in the composer chip rail** — leftmost pill in the row above the textarea (`Plus` icon + `New` label) that spawns a sibling session in the current project in one tap, no dialog, no title prompt. Inherits `projectId` + `model` + `mode` + `effort` from the current session, and defaults `worktree` to whether the current session itself runs in a worktree (`session.worktreePath !== null`). While the `POST /api/sessions` is in flight the icon swaps to a spinning `Loader2` and the button disables to block double-taps; on success the screen navigates to the new session, on failure a toast surfaces the server error code. Replaces the previous mobile-only "New session here" entry in `ChatMoreSheet` (removed along with `MobileQuickCreateSheet` + the `showQuickCreate` state); desktop still has the rail's inline `+ New session` form for cases where the user wants to pick a permission mode / write a first prompt before spawning | `web/src/screens/Chat.tsx` |
| ✅ | Session settings sheet (gear in the desktop Chat header, or three-dot menu on mobile) — rebuilt to mockup s-10. Mobile: X + "Session / Settings" header. Desktop: 380px right rail, "Settings / This session" header. Sections shipped: **Model** 3-segment (Opus 4.7 / Sonnet 4.6 / Haiku 4.5, `PATCH /api/sessions/:id` `{model}`, mid-run warning banner), **Permission mode** 5-segment (Ask / Accept / Plan / Auto / Bypass, `PATCH /api/sessions/:id` `{mode}`; Bypass dimmed at 60% when inactive; `auto` is accepted by the server and currently maps to `default` in the Agent SDK layer), **Workspace** read-only (branch + worktree path, falling back to the project's root path), **Memory** read-only CLAUDE.md preview (see next row), **Approved in this session** (scope badge + tool + signature, per-row Revoke via `DELETE /api/grants/:id`), and a footer row carrying **Archive** (posts `/api/sessions/:id/archive` — label auto-switches to `Archive session & remove worktree` when `session.worktreePath` is set) alongside **Delete session** (destructive; two-step confirm — first click flips the button to "Click again to confirm" for 3s, second click calls `DELETE /api/sessions/:id`, then `forgetSession(id)` in the Zustand store and navigate to `/sessions`). Escape key and backdrop-click both close. Sections intentionally omitted because we can't back them today: the **Effort** slider (SDK accepts `thinking.budget_tokens` but claudex doesn't persist or expose a setter), **Workspace Status** (`clean · N staged` — no git-status reader), **Worktree Relocate…** (no backend), **Duplicate session** (no clone endpoint), **editable title** (the old sheet had one; removed because it isn't in the mockup — title auto-derives from the first user message, see auto-title row), and the `⌘⇧M` helper text (no shortcut bound yet). Desktop chat header still exposes model + mode as inline pill dropdowns for the common case | `web/src/components/SessionSettingsSheet.tsx` + `web/src/screens/Chat.tsx` |
| ✅ | **Memory preview** in the session settings sheet — read-only surfacing of the CLAUDE.md files the `claude` CLI treats as ambient memory, so the user can see what's actually layered into context without guessing. Backed by `GET /api/projects/:id/memory` (login-gated; 404 on unknown project). Probes three paths and returns up to two entries: project scope is the first match of `<project>/CLAUDE.md` or `<project>/.claude/CLAUDE.md`, user scope is `~/.claude/CLAUDE.md`; both can appear together (project first — precedence in the UI, with the user-global shown underneath). Content capped at 64 KB per file server-side; larger files are truncated and flagged with `truncated: true` so the UI appends a `· truncated` note. Symlinks are refused outright (`lstat` + `isSymbolicLink()` check) to prevent a `CLAUDE.md → /etc/passwd` from exfiltrating arbitrary host files. Fetched on sheet open, not on Chat mount — shows `Reading memory…` while loading, an empty-state pointing to `<project>/CLAUDE.md` when none is found, and otherwise one card per file (`<scope>` badge + shortened path + `formatBytes` + rendered `<Markdown>` body clamped at `max-h-[240px]` with internal scroll). Editing / creating / deleting CLAUDE.md is out of scope here — that's a separate P8 surface | `server/src/sessions/memory.ts`, `server/src/sessions/memory-routes.ts`, `web/src/components/SessionSettingsSheet.tsx`, `web/src/api/client.ts`, `shared/src/models.ts` |
| ✅ | **`/btw` side chat** — reachable from the composer chip rail and the desktop Chat header (and the mobile three-dot menu). Opens a lateral conversation that reads the parent thread but never writes back. Mobile: bottom sheet covering ~65% of the viewport so the main thread stays peekable above; desktop: right-rail panel with an orange left border and "side chat · /btw" badge per mockup s-11. One drawer per main session; re-opening `/btw` reuses the existing child session (conversation is preserved). "Archive & start new" button wipes the current child and creates a fresh one. Composer is deliberately lean — no `/` or `@` triggers, because /btw is for quick questions, not actions. Defaults to `plan` mode so the model can't accidentally edit files from the side lane | `web/src/components/SideChatDrawer.tsx`, `web/src/screens/Chat.tsx` |
| ⬜ | **View modes (Normal / Verbose / Summary)** — removed. The transcript always renders in what used to be "Normal" shape: subagent lifecycle + subagent-child pieces filtered out (they have their own rail + sheet), thinking blocks hidden from the main thread, tool_use pieces fold into collapsible `ToolGroup` summaries. The old ViewModePicker component, the `viewMode` store field, the `summary`-only `SummaryCards` card, and the mobile ChatMoreSheet's "Transcript view" block are all gone. If a verbose / summary mode comes back later it would need to be re-designed — `applyViewMode` / `ChangeEntry` are deleted | removed in the effort-level pass |
| ✅ | Markdown rendering for assistant text via `react-markdown` + `remark-gfm` — headings, bold/italic, strikethrough, inline & fenced code (Tailwind-styled, language tag top-left, per-block Copy button top-right — desktop reveals on hover via `md:opacity-0 md:group-hover:opacity-100`, mobile shows at `opacity-60` always; clicks fire `navigator.clipboard.writeText` with a `toast("Copied")` / `toast("Copy failed")` result, no syntax highlighter), lists, task lists (GFM `- [ ]` / `- [x]` → disabled checkboxes), blockquotes, tables, links (external `href` opens in a new tab with `rel="noopener noreferrer"`). User messages are kept verbatim — no markdown processing — so literal `**` or backticks the user typed render as entered. `tool_result` blocks are also verbatim (mono/pre-wrap), because tool output is usually command stdout, not prose | `web/src/components/Markdown.tsx`, `web/src/screens/Chat.tsx` |
| ✅ | **Link preview cards** below user/assistant message bubbles — after the rendered markdown the first http(s) URL in the raw source gets a small OG-style card rendered via `<LinkPreview url={…} />`. The card is a `border border-line rounded-[10px] bg-paper/50` row with an optional 48×48 thumbnail, site name in mono-muted, a single-line title, and a two-line clamped description; clicking opens the URL in a new tab (`rel="noopener noreferrer"`). Deferred fetch via IntersectionObserver (100px rootMargin) so offscreen bubbles don't burn the user's rate budget on mount; a skeleton placeholder holds the card shape while loading. Only the FIRST URL per bubble renders — not every URL — matching Slack/iMessage behavior. Server-side the `GET /api/link-preview?url=…` endpoint is login-gated, rejects non-http schemes + literal private/loopback/metadata IPs (127/8, 10/8, 172.16/12, 192.168/16, 169.254/16, ::1, fc00::/7, fe80::/10, `localhost`, `*.local`), **and DNS-resolves every hostname before fetching — any A/AAAA record that falls in the private blocklist returns `400 private_or_invalid_host`, closing the DNS-rebinding hole where `evil.example.com` resolves to 127.0.0.1 at fetch time**. **Redirects are followed MANUALLY up to 3 hops** with the same literal + DNS-resolved host check re-applied on each `Location`, so an open redirector on a public site cannot bounce us at an intra-network target on hop 2. 5s upstream timeout, 512KB body cap, minimal regex parse of `<title>` + `og:*` / `twitter:*` / `name="description"` meta tags (no DOM parser pulled in), cached in the `link_previews` SQLite table (migration id=13) — 24h TTL for success, 1h TTL for negative cache (upstream 4xx/5xx/network error / blocked redirect → route returns 502, client renders nothing). Per-user rate limit of 60 previews / hour via the same `SlidingWindowLimiter` the auth surface uses, keyed on `req.userId`; `429 Retry-After` when tripped | `server/src/link-preview/routes.ts`, `server/src/link-preview/fetch.ts`, `server/src/link-preview/store.ts`, `server/src/db/index.ts` (migration 13), `web/src/components/LinkPreview.tsx`, `web/src/screens/Chat.tsx`, `shared/src/models.ts` (`LinkPreview`) |
| ✅ | **Per-message actions row** — every user / assistant_text / tool_result bubble in Chat grows an inline action row that's hidden by default. Reveal model is the same on both platforms: **click/tap the bubble** to show the row; click another bubble (or an action) to dismiss. Hover does NOT reveal the row on either platform — previous iterations tried `md:group-hover` (plain, then with a 500ms delay) but both felt noisy when the mouse tracked across messages during scroll, so reveal is now strictly click/tap-intent. Keyboard focus still reveals the row via `md:focus-within:*` so tabbing through actions works. Actions render as rounded-full pill chips (`h-7 px-2.5 rounded-full border border-line bg-canvas text-[12px]`) in a row BELOW the bubble as a normal-flow sibling (no absolute positioning overlapping the bubble), right-aligned under user bubbles and left-aligned under assistant / tool_result. Actions: **Copy text** (everywhere), **Copy as markdown** (assistant_text only — copies the raw `source` fed to `<Markdown>`, which is already the authored markdown), **Copy permalink** (`${origin}/session/<sessionId>#seq-<seq>`; the hash anchor is honored by Chat's scroll-to-event code and by global-search message-hit rows), **Branch** (assistant_text + tool_result with a persisted seq — forks the session at that seq into a new top-level session via `POST /api/sessions/:id/fork` and navigates into it). Reveal state is a single `revealedSeq: number | null` held at `ChatScreen` scope and threaded through `Piece` → `MessageActions` / `UserBubble` — only one bubble shows its chips at a time, so clicking a different bubble trivially dismisses the previous. Copy is done via `copyText` (async Clipboard API with a hidden-textarea `execCommand` fallback — claudex runs over HTTP through an frpc tunnel where `navigator.clipboard` is unavailable); failure surfaces as a "Copy failed" toast. Tool_use chips, thinking blocks, and permission_request cards intentionally skip the action row — those aren't user-addressable messages. The pencil-edit affordance on the newest user bubble stays at top-left (`-top-2 -left-2`) and shares the same click-to-reveal rule — no hover reveal — so the two affordances never disagree. Permalinks carry a `seq` only for persisted events; optimistic echoes (not-yet-acked) fall back to a session-level link without an anchor. Backed by a tiny toast singleton (one `toast(msg)` function + one `<ToastHost />` mount in Chat) | `web/src/components/MessageActions.tsx`, `web/src/lib/toast.tsx`, `web/src/screens/Chat.tsx`, `web/src/state/sessions.ts` (`UIPiece.seq`) |
| ✅ | Inline image rendering in `tool_result` and `user_message` with lightbox preview — base64 `data:image/(png|jpe?g|gif|webp)` URLs and `/api/attachments/:id/raw` refs embedded in piece content are extracted into 120×120 `cursor-zoom-in` thumbnails above the remaining text (which is now image-token-stripped, so a screenshot no longer shoves the real output past the 1200-char truncation budget). Clicking a thumb opens a full-viewport lightbox (`z-60`, `bg-ink/80 backdrop-blur-sm`) with `max-w-[90vw] max-h-[85vh] object-contain` rendering; closes on backdrop click / X button / Escape; left-right arrows (keyboard and on-screen) step through all images in the clicked piece; top-right Download button is a plain `<a download>` so data URLs flow through the browser's native save dialog. Purely a render-side change — the persisted event shape is unchanged | `web/src/lib/images.ts`, `web/src/components/ImageLightbox.tsx`, `web/src/screens/Chat.tsx` |
| ✅ | Context ring in the chat header opens a Usage panel with current-session token counts (input/output/total) and a cost estimate using a front-end pricing table. Aggregated by model when a session spanned multiple models. The big ring shows a client-side context % estimate computed as `lastTurnInput / contextWindowTokens(model)` — the sum of `inputTokens + cacheReadInputTokens + cacheCreationInputTokens` from the most recent `turn_end` over the known per-model window (every 4.x model is 200k today). Summing all three is required because with prompt caching on, `input_tokens` alone only counts *new* uncached input and would report ~0 on every warm-cache turn. The chat-header mini-ring shares the same computation and refetches `/events` on piece-length changes. **Per-call vs cumulative split** (fix for the "1.14M / 1M" / 100%+ ring on multi-tool-use turns): the SDK's `result.usage` is **cumulative across every API sub-call** the agent made during the turn — its `cache_read_input_tokens` sums each sub-call's repeated read of the warm prefix and easily exceeds the actual context window on long turns. The live runner now snapshots the last top-level `assistant` message's `message.usage` (the FINAL sub-call's per-call prompt size — what the model actually saw) and emits it as `turn_end.usage`; the cumulative `result.usage` rides alongside on a new `billingUsage` field for billing-style totals. `scanTurnEnds` reads per-call `usage` for `lastTurn` (drives the ring), and `billingUsage ?? usage` for `totalInput` / `perModel` (drives "Tokens · this session" — true billable breakdown when present, falls back to per-call summed for CLI imports / pre-fix live rows). Same fallback in the stats SQL (`/api/stats`), `/api/usage/today` aggregator, and Markdown export. Subagent assistant messages (with `parent_tool_use_id`) are skipped — their per-call usage describes the subagent's prompt, not the parent's, and would otherwise jump the parent ring to the subagent's context size. **Historical-turn guard**: `turn_end` rows persisted before agent-runner started emitting cache fields carry only `inputTokens` + `outputTokens` (a few dozen tokens on warm cache) and would misread as "0 %"; `computeSessionUsage` flips `lastTurnContextKnown = false` when `lastTurnInput < HISTORICAL_TURN_THRESHOLD` (500 tokens), and the ring/mini-ring/tasks-rail donut all render `—` with a tooltip "historical turn — cache fields not persisted; next turn will reflect real context" instead of a misleading percentage. The server also `.info()` logs the cumulative + per-call usage on every `turn_end` ("turn_end usage" → `{billingUsage, perCallUsage}`) so future context-ring blind spots can be diagnosed from `~/.claudex/logs/server.log` alone. On mobile the context ring opens the bottom-sheet `UsagePanel`; on desktop it opens the slide-in `ContextPanel` (model, context %, token stats, cost, skills, plugins, CLAUDE.md memory) with a "Full usage analytics →" deep link at the bottom. **Both panels refetch usage on transcript growth** — Chat threads `pieceCount={pieces.length}` into the panels and the usage-summary effect lists it as a dependency, so the ring + token stats + cost track in-flight turns instead of snapshotting on open. Without this dependency the panels would only update after you closed and reopened them, which made the user-facing surface feel "stuck during streaming" | `web/src/components/UsagePanel.tsx`, `web/src/components/ContextPanel.tsx`, `web/src/lib/usage.ts`, `web/src/lib/pricing.ts`, `web/src/screens/Chat.tsx`, `server/src/sessions/agent-runner.ts`, `server/src/sessions/runner.ts`, `shared/src/usage.ts`, `shared/src/models.ts` |
| ✅ | **Full-screen Usage page (`/usage`, mockup s-08)** — cross-session analytics dashboard reached from the desktop sidebar "Usage" nav item, a "Full usage analytics →" link at the bottom of the mobile `UsagePanel` bottom sheet and the desktop `ContextPanel` slide-in, and direct links from other surfaces (e.g. the Home header's chart icon). **Mobile (<md)** renders a real compact stacked layout from mockup s-08 iPhone (back-chevron header, big 140×140 context ring with `lastTurnInput / contextWindow(model)` caption, honest "No plan data" plan-period card, per-model rows with fading klein dots, and a front-end cost-estimate card from `/api/usage/today` × published per-token rates). The 7-day bar chart + top-sessions list are desktop-only — a 700px SVG doesn't read at 390px. **Desktop** tiles shipped: (1) **Current session** — context % donut + last-turn `lastTurnInput / contextWindow(model)` + aggregate Prompt/Output + last-turn cache-hit ratio (`cacheRead / (cacheRead + input)`); empty state when no active session is available. (2) **Today · tokens** — sum of every `turn_end` since local midnight, session count, and a per-model mini-row (dots fade with rank). (3) **Last 7 days · tokens / day** — self-contained SVG stacked bar chart, klein-gradient segments ranked by model, nice-ceiling y-axis, weekday x-labels; empty state when no tokens recorded yet. (4) **Top sessions** — up to 5, ranked by today's tokens, each row links to the session. The row-1 grid is `md:grid-cols-2 lg:grid-cols-4` so the md viewport (768–1280) drops to 2-col instead of compressing 4 tiles into a too-narrow row. Intentionally **omitted** (honest empty states instead of fake data): **Plan period** — we don't have plan/quota info; rendered as a "No plan data" card pointing to CLI `/cost`. **Cowork / Code split**, **"Resets Mon 16:00"** — omitted, no data. Time-range chips render Today as active; 7/30 days are disabled with `cursor-not-allowed` (the wire-up for those would need a separate `days` query string and a refactored tile, not shipped yet). Backed by `GET /api/usage/today` (`{windowStart, totalTokens, sessionCount, perModel, topSessions}`) and `GET /api/usage/range?days=N` (zero-padded N-day buckets, N clamped to 1..90). Both endpoints walk `session_events WHERE kind = 'turn_end'` and sum `inputTokens + outputTokens + cacheReadInputTokens + cacheCreationInputTokens` per payload — side chats and archived sessions are included on purpose (tokens spent still count). Day buckets use local-date keys so the x-axis matches what the user sees | `server/src/sessions/usage-routes.ts`, `web/src/screens/UsagePage.tsx`, `web/src/api/client.ts`, `web/src/components/AppShell.tsx`, `web/src/components/UsagePanel.tsx`, `web/src/components/ContextPanel.tsx`, `web/src/screens/Chat.tsx`, `shared/src/models.ts` |
| ✅ | **Import existing CLI sessions** — Home header "download" button opens a sheet that lists sessions under `~/.claude/projects/<cwd-slug>/*.jsonl` (already-adopted sessions are hidden). `GET /api/cli/sessions` streams each file's first few lines to extract the first user message as a title; `POST /api/cli/sessions/import` idempotently creates `sessions` rows with `sdk_session_id` set so the existing resume path picks up from where CLI left off, AND seeds `session_events` from the JSONL transcript so the imported session opens with its full history (user turns, assistant text/thinking/tool_use, tool_result, per-turn usage for the context ring). Import also stamps `cli_jsonl_seq` with the number of JSONL lines consumed so the **resync-on-open** path (triggered whenever the adopted session is re-opened) knows where to pick up when the user has kept chatting in the CLI since import — new lines get mapped and appended, and subscribers receive a `refresh_transcript` frame to re-fetch the tail. The slug↔cwd decoding is the CLI's own `/` ↔ `-` scheme; Windows slugs are detected by the `X--` drive-letter prefix (the CLI collapses both `:` and `\` into `-`) and restored as `X:\<body>` with the body kept verbatim — real dashes and separators are indistinguishable post-encode, so we show the user a readable path rather than guessing | `server/src/sessions/cli-discovery.ts`, `server/src/sessions/cli-import.ts`, `server/src/sessions/cli-events-import.ts`, `server/src/sessions/cli-resync.ts`, `server/src/sessions/cli-routes.ts`, `web/src/components/ImportSessionsSheet.tsx`, `web/src/screens/Home.tsx` |
| ✅ | **Terminal drawer** — reachable from the desktop Chat header and the mobile three-dot menu. Not in the mockup, kept as an extra affordance for power users; PTY attached to the session cwd | `web/src/components/TerminalDrawer.tsx`, `web/src/screens/Chat.tsx` |
| ✅ | **Keyboard help overlay** — press `?` from any screen to see wired bindings. Ignored while focus is in an input / textarea / select / contenteditable. Lists only bindings that actually exist today: Global (`?`, `⌘K` / `Ctrl+K` to focus Home search, `Esc` to close modals) and Composer (`Enter` send, `Shift+Enter` newline, `↑` / `↓` picker nav, `Enter` picker insert, `Esc` picker close). `?` toggles, `Esc` closes, backdrop click closes. Mac vs. non-Mac modifier pill detected via `navigator.platform` / userAgent | `web/src/components/KeyboardHelp.tsx`, `web/src/App.tsx` |
| ✅ | **Mobile iOS viewport polish** — Chat root switches from `h-full` to `h-[100dvh]` and Login's `min-h-screen` becomes `min-h-[100dvh]` so the layout tracks the *dynamic* viewport (iOS Safari shrinks `100vh` when the URL bar is visible, which used to clip the composer off-screen); `html/body/#root` get a matching `min-height: 100dvh` anchor in `globals.css`. New `useVisualViewport` hook exposes `{height, offsetBottom}` where `offsetBottom = window.innerHeight - visualViewport.height - visualViewport.offsetTop`, i.e. how many CSS pixels are currently hidden under the software keyboard. Chat passes that into the Composer and lifts the composer wrapper via `transform: translateY(-offsetBottom)` so it floats above the keyboard instead of disappearing beneath it (transform, not padding, so the thread above keeps its scroll geometry); an effect scrolls the transcript to the tail when the keyboard first opens so the user sees what they're replying to. Desktop keeps `offsetBottom = 0` and the transform is omitted. AppShell's mobile content padding becomes `pb-[calc(58px+env(safe-area-inset-bottom))]` so the iOS home indicator no longer eats the last row above the tab bar. Login's TOTP input gains `autoComplete="one-time-code"` for iOS SMS/Authenticator autofill | `web/src/hooks/useVisualViewport.ts`, `web/src/screens/Chat.tsx`, `web/src/screens/Login.tsx`, `web/src/components/AppShell.tsx`, `web/src/styles/globals.css` |
| 🟡 | Settings page shipped to mockup s-12 structure — eight subtabs, six with real data (Account / Security / Appearance / Plugins / Environment / Advanced; Security now carries a server-backed Audit log card with icon-tile header, Recovery-codes card with Regenerate + one-time plaintext modal, and Trusted-projects card; Advanced carries an **App version** card that reads the currently-loaded `/assets/index-*.js` filename directly off the live `<script>` tag and a "Force reload (clear cache)" button — opportunistically purges Cache API stores then `location.replace()`s the current URL with a `_r=<timestamp>` query param to bypass the browser's HTTP cache for index.html, preserving pathname + hash so deep links survive; the query string is a no-op server-side because the SPA fallback ignores query params. Advanced also carries the Worktrees prune panel, falling back to `EmptyCard` when no worktrees exist), two empty states (Notifications / MCP servers). 2FA card hero uses the mockup's borderless `h-10 w-10 rounded-[10px] bg-klein-wash` tile and a 3-col Issuer / Recovery codes / Last used grid (last two as honest `—` placeholders); profile card adds a `since <createdAt>` subline; mobile chip-row active state uses `bg-klein`; desktop rail wordmark reads `claudex`. Still partial at the page level because Disable-2FA, paired-browsers, exposure panel, MCP-server parsing, and dark-mode all remain planned | `web/src/screens/Settings.tsx`, `server/src/auth/routes.ts`, `server/src/audit/routes.ts`, `server/src/sessions/user-env.ts`, `server/src/sessions/worktree-manage.ts` |
| ✅ | **Honest loading/error states** across data-fetching screens — a caught API rejection used to look identical to "no data yet", so a broken endpoint rendered as an empty list or a permanently-spinning card. Each of `Routines`, `UsagePage`, `ChatTasksRail`, `Home`, and three Settings cards (`AdvancedWorktreesCard` / `GrantedToolsCard` / `TrustedProjectsCard`) now tracks a separate `err` state alongside its existing `loading`/`data==null` shape and renders with priority `err > loading > empty > data`. Load failures show a `bg-danger-wash` banner with a Retry button (or a dismissible banner in Home's case); Home and UsagePage replace the first-paint "loading…" mono text with a 3-row `bg-paper animate-pulse` skeleton so a fresh install with no sessions is no longer mistaken for a broken fetch; ChatTasksRail splits "historical turn unknown" from "usage endpoint failed" by adding a small red dot + "Failed to load usage" tooltip on the Context-window footer when the `/api/usage/summary` fetch rejects | `web/src/screens/Routines.tsx`, `web/src/screens/UsagePage.tsx`, `web/src/components/ChatTasksRail.tsx`, `web/src/screens/Home.tsx`, `web/src/screens/Settings.tsx` |
| ✅ | **ToolGroup sticky header on expand** — when the combined "N steps · …" tool-group card is expanded, its header pins to the top of the Chat scroller so a long body (diffs, bash output) can be collapsed from the current scroll position without scrolling back to the chevron. Header button gains `sticky top-0 z-20` plus an opaque tone-matched background (`bg-paper` / `bg-indigo-wash` / `bg-danger-wash` — NOT the `/NN` alpha variants, which let body content bleed through while scrolling). Outer card keeps `overflow-hidden` so body color bands stay clipped to the border radius. Only the outermost ToolGroup header is sticky; inner DiffView / ToolCallBlock / ToolCallCard headers stay non-sticky because they sit inside the ToolGroup's clipping ancestor and nested sticky layers compete for the same scroll anchor | `web/src/screens/Chat.tsx` (`ToolGroup`) |

## Tests

| Status | Feature | Where |
|---|---|---|
| ✅ | vitest backend suite, all green — run `pnpm --filter @claudex/server test`; current count tracked in git log rather than pinned in this doc (drifts every commit) | `server/tests/` |
| ✅ | Bind-safety, DB migration + FK cascade | `tests/config.test.ts`, `tests/db.test.ts` |
| ✅ | Password/TOTP/JWT edge cases (tampering, cross-secret, wrong audience, expiry, file-mode 0600) | `tests/auth.test.ts` |
| ✅ | Auth HTTP routes including peek-retry TOTP, replay rejection, cookie attributes, user enumeration parity | `tests/auth-routes.test.ts` |
| ✅ | Session + project stores: stats, archive filtering, per-session event seq isolation, payload JSON roundtrip, FK cascade | `tests/sessions-store.test.ts` |
| ✅ | Deterministic Agent SDK → RunnerEvent translation (15 cases covering every block kind + malformed input) | `tests/agent-runner.test.ts` |
| ✅ | SessionManager lifecycle, status transitions, grant-based auto-approval | `tests/session-manager.test.ts` |
| ✅ | Session REST routes (path validation, duplicate path 409, archive, events, project rename + delete with sessions-FK guard, PATCH session title/model/mode with live-runner mode propagation, archived 409, empty-body 400, running-model warning, grants list + revoke with scope + 404, pending-diffs aggregation over event log) | `tests/session-routes.test.ts`, `tests/diffs.test.ts` |
| ✅ | Filesystem browse routes: auth gate, abs-path validation, 404 / 403 error paths, sort + flags, parent-null at root, symlinks are not followed. `/api/browse/raw` covered by auth gate, not-absolute / not-found / is-a-directory rejections, extension → Content-Type mapping (png), octet-stream fallback for unknown extensions, `download=1` → `Content-Disposition: attachment` | `tests/browse.test.ts` |
| ✅ | Slash-commands scanner + route: auth gate, built-ins-only when `~/.claude/commands` missing, user-command parsing (frontmatter / heading / plain line / null), project-command opt-in via `projectId`, unknown `projectId` soft-ignored. Uses an injected `userClaudeDir` tmp to avoid touching the host user's real `~/.claude/` | `tests/slash-commands.test.ts` |
| ✅ | WebSocket end-to-end over a real port (auth gate, hello_ack, broadcast isolation, permission decision round-trip, interrupt round-trip, bad-frame recovery, global sessions channel delivers cross-session frames to unsubscribed tabs while turn frames stay gated) | `tests/ws.test.ts` |
| ✅ | Tool grants: signature conventions, session-vs-global scope, idempotent insert, revoke, FK cascade | `tests/grants.test.ts` |
| ✅ | Permission summary content for every supported tool + missing-field edge cases | `tests/permission-summary.test.ts` |
| ✅ | Static web serving: index at /, immutable asset cache, SPA fallback for GET, /api 404s stay JSON, non-GET doesn't fall back, /api/health works alongside static | `tests/static.test.ts` |
| ✅ | Worktree lifecycle: `isGitRepo` classification (dir / file / missing), `createWorktree` branch collision fallback, `removeWorktree` cleanup, POST /api/sessions `worktree: true` round-trip on a tmp git repo, 400 `not_a_git_repo` on non-git project, archive path removes the worktree dir, archive tolerates a pre-deleted worktree dir | `tests/worktree.test.ts` |
| ✅ | `/btw` side chat: POST + GET routes (create copies parent project/model and defaults to `plan`, persists `parent_session_id`, cascades on parent delete, 404 on unknown parent, idempotent re-POST reuses the active child), default session list hides children, SessionManager context seed injection (strips tool/thinking noise, seeds only once on first spawn, top-level sessions unaffected, seed never written to the child's event log) | `tests/side-sessions.test.ts` |
| ✅ | Routines CRUD + scheduler: REST round-trip (POST / GET / PATCH / DELETE), `invalid_cron` on bad expression, `project_not_found` on unknown project, `POST /:id/run` spawns a session and delivers the prompt. Scheduler fires on elapsed `next_run_at` (capturing fake timer — no `vitest.useFakeTimers()` needed), paused routines skip, deleted routines stop firing, `reload()` rearms after cron edits, missed fires are logged and skipped (no catch-up), `fire()` direct-call advances `last_run_at` + `next_run_at`. `cron-parser` wrapper helpers (`isValidCron` / `computeNextRun`) | `tests/routines.test.ts` |
| ✅ | Usage analytics routes: `/api/usage/today` (auth gate, empty-DB zeros, three-session multi-model aggregation with tokens correctly summed from `inputTokens + outputTokens + cacheReadInputTokens + cacheCreationInputTokens`, session-count match, exclusion of turn_end events older than local midnight, tolerance for malformed JSON payloads) and `/api/usage/range` (auth gate, 7-day default with zero-padded buckets, three-day placement across the window, `days` param clamped to 1..90 and coerced from bad input to 7) | `tests/usage-routes.test.ts` |
| ⬜ | Frontend unit/component tests — none yet (mockup + visual review cover this for MVP) |  |

## Routines

| Status | Feature | Where |
|---|---|---|
| ✅ | `routines` table (SQLite migration id=4) keyed on `id` with FK to `projects` (`ON DELETE RESTRICT`). Columns: `name`, `prompt`, `cron_expr`, `model`, `mode`, `status` (`active` / `paused`), `last_run_at`, `next_run_at`, `created_at`, `updated_at`. Two indexes: `status`, `project_id` | `server/src/db/index.ts` |
| ✅ | `RoutineStore` — same-shape API as `ProjectStore` / `SessionStore`: `create` / `list` / `listActive` / `findById` / `update` / `setStatus` / `setSchedule` / `setLastRun` / `delete` | `server/src/routines/store.ts` |
| ✅ | `RoutineScheduler` — single chained `setTimeout` anchored to the next-due active routine. On fire: creates a fresh session titled `"<name> · <timestamp>"` with the routine's project/model/mode, delivers `prompt` via `SessionManager.sendUserMessage`, rolls `last_run_at` + `next_run_at` forward, and rearms. `reload()` on any CRUD change. No catch-up: missed fires (server was down) are logged and the schedule is rolled forward to the next future slot. **Respects project trust**: before spawning, the scheduler re-checks `project.trusted` (parity with `POST /api/sessions`) — if the user revoked trust after scheduling, the fire is logged (`routine_skipped_untrusted`), the schedule rolls forward, and no session is created. `lastRunAt` stays `null` so the skipped fire isn't misreported as a successful run. Uses `cron-parser@5` (`CronExpressionParser.parse`) to evaluate 5-field expressions in the host's local timezone. Injectable `now` / `setTimeout` / `clearTimeout` for deterministic tests | `server/src/routines/scheduler.ts` |
| ✅ | REST (login-gated): `GET /api/routines`, `GET /api/routines/:id`, `POST /api/routines` (400 `invalid_cron` on parse failure, 400 `project_not_found` on unknown project), `PATCH /api/routines/:id` (partial — same invalid-cron check), `DELETE /api/routines/:id`, `POST /api/routines/:id/run` ("Run now" — invokes `scheduler.fire()` and returns `sessionId`). Every mutating route calls `scheduler.reload()` | `server/src/routines/routes.ts` |
| ✅ | Routines reached from the bottom tab bar on mobile / sidebar nav on desktop (`/routines`). List with status dot, human-readable cron ("Every day at 9:00"), next-fire relative time; per-row Run now / Pause-Resume / Edit / Delete. Editor dialog has name, project picker (locked after create), prompt textarea, five cron presets (hourly / daily 9 / weekdays 9 / Mondays 9 / every 30m) + Custom cron field, model pills, 4-way permission mode selector. Invalid cron surfaces the server's `invalid_cron` as a friendly message. The old `RoutinesSheet` component is retired in favor of this full-page screen | `web/src/screens/Routines.tsx`, `web/src/App.tsx` |

## Batch queue

| Status | Feature | Where |
|---|---|---|
| ✅ | `queued_prompts` table (SQLite migration id=10) keyed on `id` with FK to `projects` (`ON DELETE CASCADE`). Columns: `prompt`, `title`, `model` (nullable — defaults to `claude-opus-4-8`), `mode` (nullable — defaults to `default`), `worktree`, `status` (`queued` / `running` / `done` / `cancelled` / `failed`), `session_id`, `created_at`, `started_at`, `finished_at`, `seq`. One index on `(status, seq)` for the runner's pick-next query | `server/src/db/index.ts` |
| ✅ | `QueueStore` — CRUD + ordering helpers: `list` / `findById` / `findRunning` / `pickNextQueued` / `maxSeq` / `create` (auto-assigns `seq = max+1`) / `update` (free-form but callers gatekeep on status) / `setStatus` (with optional `sessionId` / `startedAt` / `finishedAt` stamps) / `swapNeighbor` (reorder within queued set) / `reorderTo` (absolute-index drag reorder; clamps out-of-range target for UX-tolerant drops) / `delete`. Fires a shared `onChange` hook on every mutation so the HTTP/runner broadcast paths can emit `queue_update` WS frames without each call site remembering to | `server/src/queue/store.ts` |
| ✅ | `QueueRunner` — polls every 2s (`QUEUE_TICK_INTERVAL_MS`). Each tick: reconciles any `running` rows against their session's status (session `idle` → queue row `done`, `error` → `failed`); if nothing running, picks lowest-seq `queued`, creates a fresh session (pre-stamping it `running` so the next poll doesn't race), and fires `manager.sendUserMessage(sessionId, prompt)` fire-and-forget. **Respects project trust**: before dispatch, the runner re-checks `project.trusted` (parity with `POST /api/sessions`) — if the user revoked trust after queueing, the row is flipped to `failed` with a `queue_skipped_untrusted` warn log rather than left queued (leaving it queued would wedge every row behind it). One-at-a-time dispatch survives server restarts because the state is persisted. Auto-started on boot except when `NODE_ENV === 'test'` (tests drive `tick()` directly). Timer `.unref()`'d so shutdown isn't blocked. **Watchdog**: a dispatched row that stays `running` past `QUEUE_DISPATCH_TIMEOUT_MS` (default 30 min, overridable via env) without its session settling is assumed wedged — the runner fires `manager.interrupt(sessionId)` fire-and-forget and flips the queue row to `failed` with a warn log, so a single hung SDK spawn can't wedge the whole queue. Every status mutation goes through `QueueStore.setStatus` → `onChange` → `manager.notifyQueueUpdate()` so the web Queue screen sees dispatch/settle transitions in real time without polling | `server/src/queue/runner.ts`, `server/src/transport/app.ts` |
| ✅ | REST (login-gated): `GET /api/queue` (list), `POST /api/queue` (400 `project_not_found` on unknown project, returns row with computed `seq`), `PATCH /api/queue/:id` (409 `not_editable` when status ≠ `queued`), `DELETE /api/queue/:id` (cancels queued, interrupts + cancels running, 409 `not_cancellable` on terminal states), `POST /api/queue/:id/up` and `POST /api/queue/:id/down` (swap seq with adjacent queued neighbour; 409 `not_reorderable` when the row itself isn't queued), `POST /api/queue/:id/move {seq}` (reorder to an absolute index within the queued sub-list; target clamped server-side so "drop past the end" is a tolerated no-op rather than 400; 409 `not_reorderable` on non-queued rows). Every mutation fans out a `queue_update` WS frame via the global channel. Covered by `tests/queue-routes.test.ts` (18 tests) | `server/src/queue/routes.ts`, `server/tests/queue-routes.test.ts` |
| ✅ | `/queue` screen wrapped in AppShell with a new **Queue** nav entry (between Routines and Alerts on desktop sidebar + mobile tab bar, uses the `ListOrdered` lucide icon). List rows show a status dot (queued → ink-faint, running → pulsing success, done → ink-faint, cancelled → line-strong, failed → danger), title (or first-line of prompt), project name + model + mode + worktree badges, and per-status actions (queued → Edit / Remove; running → Open session / Stop; done/cancelled/failed → Open session). Editor sheet mirrors the Routines pattern: project select (locked after queue), model + mode + worktree toggle, prompt textarea, optional title. **Desktop (md+): HTML5 drag-and-drop reorder** with a `GripVertical` handle, a thin klein indicator line rendered above/below the hover target based on cursor Y position, optimistic local splice, and a `POST /api/queue/:id/move` round-trip. **Mobile (<md): up/down chevron buttons untouched** so thumb reorder still works without a fiddly grab-and-drag. Only `queued` rows are draggable; running/done/failed/cancelled rows stay pinned. **Live updates via WS** (`claudex:queue_update` window event, fanned out from the global `queue_update` frame) replace the previous 5s poll — an idle tab stays truthful without polling the server. **A11y**: queued rows are `role="button" tabIndex={0}` with `aria-label="Queue item N of M — press space then arrow keys to reorder"`; Space/Enter arms reorder mode (visual klein ring + `aria-grabbed="true"`), Arrow Up/Down while armed calls the same move API as the chevrons and keeps focus on the moved row via `requestAnimationFrame` re-focus, Escape exits. Editor modal is a proper `role="dialog" aria-modal="true" aria-labelledby="queue-editor-title"` with autofocus on the first editable input (project select on create, title input on edit), Tab focus-trap, Escape close, and scrim click-out (mousedown-on-scrim only, so a drag ending outside the card doesn't dismiss). Destructive cancel replaces the old native `window.confirm()` with an inline two-click confirm ("Click again to cancel", 3s reset) matching the Session Settings Delete / Trust pattern | `web/src/screens/Queue.tsx`, `web/src/state/sessions.ts`, `web/src/api/client.ts`, `shared/src/protocol.ts` (`ServerQueueUpdate`), `server/src/sessions/manager.ts` (`notifyQueueUpdate`), `server/src/transport/ws.ts` |

## Subagents

| Status | Feature | Where |
|---|---|---|
| ✅ | `GET /api/agents?status=<active\|done\|all>&limit=<N>` (login-gated) — read-only observability over the SDK's subagent-family tool invocations. Aggregates every `session_events` row where `kind='tool_use'` and the payload's `name` is in `SUBAGENT_TOOL_NAMES = ['Task', 'Agent', 'Explore']`, JOINed in JS with the matching `tool_result` row by `(sessionId, toolUseId)` so we avoid a brittle SQL-side JSON join. Each item carries `{id: toolUseId, sessionId, sessionTitle, projectName, toolName, description, input, seq, startedAt, finishedAt, durationMs, status, isError, resultPreview}` — `input` is the tool_use's full parsed input object (empty `{}` when missing or non-object) so the UI can pretty-print it in the expanded-row view. Status rule: `running` when no matching `tool_result`; `failed` when `isError=true`; `done` otherwise. Description is `summarizeInput` on the tool_use payload — prefers `description` / `title` / `subagent_type` / `prompt` / `query` / `task`, whichever lands first, trimmed to 200 chars. `resultPreview` is the first 200 chars of the tool_result's content (null while running). `stats` block reports `activeCount`, `completedToday`, `avgDurationMs`, `failureRate` (failed / (done+failed) today); "today" is UTC midnight-anchored. `?status=active` returns only `running`; `?status=done` returns done+failed (the UI narrows to `failed` client-side). `limit` defaults to 100, capped at 500. The stats block is computed before filtering so the four cards stay truthful on every tab. **Scope limit**: only the SDK's built-in subagent tool names are recognized today — user-defined prompt-template subagents that show up under a different tool name would need to be added to `SUBAGENT_TOOL_NAMES`. Covered by `tests/agents-routes.test.ts` (4 tests: 401, empty, three-run aggregation across two sessions, `?status=active` filter) | `server/src/agents/routes.ts`, `server/src/transport/app.ts`, `server/tests/agents-routes.test.ts`, `shared/src/models.ts` (`SubagentSummary`, `SubagentStats`, `ListSubagentsResponse`) |
| ✅ | `/agents` screen wrapped in AppShell with a new **Subagents** nav entry (between Queue and Alerts on desktop; dropped from the mobile tab bar alongside Usage since observability screens are desktop-focused). Uses the lucide `Bot` icon. Layout: header caps "Subagents" + display "What claude is delegating.", a four-card stat row (Active / Done today / Avg duration / Failure rate — Failure rate flips red at ≥50%), a URL-backed filter chip rail (All / Running / Done / Failed, stored in `?status=`), then a **grouped** row list — one section per tool name with a sticky caps header + run count (`Agent · 4 runs`), group order `Agent → Task → Explore → <rest alphabetical>`. Rows are tight single-line (`py-2`): a 3px status-colored left-border strip (running → klein + animate-pulse, done → success/50, failed → danger/70), a matching background wash, a bordered tool-name chip, a spinning `Loader2` for running rows, the description truncated, the parent session title as a deep link to `/session/:id#seq-<n>`, a duration / `running` marker, and a `timeAgoShort` relative caption. Tap a row to expand an inline panel with **three sections**: (1) **Input** — pretty-printed JSON of the tool_use's full `input` payload in a scrollable `<pre>` (max-h 40), with a "Copy" button that uses `copyText` from `@/lib/clipboard` (hidden-textarea fallback for non-secure contexts); (2) **Result** / **Error output** — the 200-char `resultPreview` in a max-h-48 `<pre>` with a note pointing the user to `/session/:id#seq-<n>` for the full transcript; for running rows a "polling every 2 s" loader replaces this; (3) meta row with session / project / started + finished clocks. **Live updates** layer two sources: (a) subscribes to `claudex:refresh_transcript` window events with a 500 ms debounce so tool events in a single turn produce at most one HTTP roundtrip; (b) while an expanded row is still `running`, a 2 s `setInterval` re-polls `/api/agents` so the result surfaces promptly once `tool_result` lands — cross-session view only; in-session live state now ships over WS via the `subagent_*` frames (see row below). Mobile stays single-column; no special override. Read-only — there is no affordance to cancel / retry a subagent run from here; the parent session drives that | `web/src/screens/Agents.tsx`, `web/src/App.tsx`, `web/src/components/AppShell.tsx`, `web/src/api/client.ts` (`listAgents`), `web/src/state/sessions.ts` (`claudex:refresh_transcript` forwarder), `web/src/lib/clipboard.ts` |
| ✅ | **Live subagents (s-17)** — in-session subagent monitor that streams every Task/Agent/Explore run's inner tool calls, text chunks, and periodic AI-generated progress description to the parent session's WS. Opt-in to the SDK side via `forwardSubagentText: true` + `agentProgressSummaries: true` in `agent-runner.ts`. `translate()` fans the SDK's `system/task_started | task_progress | task_updated | task_notification` subtypes + top-level `tool_progress` into five new `RunnerEvent` variants (`subagent_start / progress / update / end / tool_progress`); main-thread `assistant_text`/`thinking`/`tool_use`/`tool_result` also thread through `parent_tool_use_id` so nested child output is attributable. Persisted as five new `EventKind` values in `session_events` (migration id=21 is a marker — no schema change since `kind` is open TEXT; `tool_progress` is broadcast-only since each tick supersedes the previous). WS bridge emits five matching `ServerFrame` variants + `parentToolUseId` on the four existing text/tool frames. Web store exposes `useSubagentRuns(sessionId)` which assembles one `SubagentRun` per taskId — latest description ("activeForm"), status, usage rollup, nested child stream (text + tool chips in order) — memoized against the transcript array reference via WeakMap | `server/src/sessions/agent-runner.ts`, `server/src/sessions/runner.ts`, `server/src/sessions/manager.ts`, `server/src/transport/ws.ts`, `server/src/db/index.ts` (migration 21), `shared/src/models.ts` (`Subagent*Payload`, `SubagentLifecycleStatus`, new `EventKind`s), `shared/src/protocol.ts` (`ServerSubagent*`), `server/tests/agent-runner.test.ts`, `web/src/state/sessions.ts` (`UIPiece`, `SubagentRun`, `computeSubagentRuns`, `useSubagentRuns`) |
| ✅ | **In-session s-18 UI** — four surfaces, all wired off `useSubagentRuns(sessionId)`, **all in the indigo palette** so delegated agent activity reads distinctly from main-agent (klein) work. (1) `SubagentsStrip` — persistent sticky strip under the session header (next to `PlanStrip`); `Agents` pill + status dots one-per-run in indigo (running pill glows, stopped pill dimmer, done/failed fall back to success/danger dots), most-recent `description` with a blinking indigo caret on live runs, optional `bg` badge when `is_backgrounded: true`, hides entirely when the session has zero runs. (2) **`SubagentsSheet`** — dedicated drawer twin to `PlanSheet`: mobile bottom sheet, desktop `md:w-[420px]` right slide-over (`fixed inset-0 z-40 bg-ink/30 backdrop-blur-[2px] flex items-end md:items-stretch md:justify-end`). Header has the indigo Agents chip + "N live · N total" count + a `follow` toggle stub + × close; body is `<SubagentsPanel variant="embedded" />` wrapped in `flex-1 min-h-0 overflow-y-auto`. Empty state (zero runs) renders a dashed `bg-paper/40` card ("No delegated runs yet — …") so the drawer is always reachable from the header **Bot** icon. Closes on backdrop tap / Esc / × / revealing a tool-use (which auto-dismisses so the transcript scroll is visible). Three open triggers all target the same sheet: the strip, the indigo `Task started → view` pointer in the chat thread, and a `Bot` icon button in the chat header (mobile + desktop, visible only when `subagentRuns.length > 0`, pulsing indigo dot when anything is running, active state switches to `bg-indigo-wash/60 border-indigo/40 text-indigo`). The Tasks drawer button (tools-only) swapped to the `Wrench` icon to free up `Bot` for the subagents button — delegated-agent activity is the visual anchor the robot glyph reads as. (3) `SubagentsPanel` — grouped Running / Failed / Stopped / Done list. Running rows auto-expand with a collapsible Input preview + a timeline-style live stream (text chunks, thinking blocks, tool chips with paired tool_result status inline, trailing **indigo caret** on the last partial chunk while streaming) + a footer with a (disabled) stop button, the SDK `output_file` as a truncated mono path when present, and the short taskId. Each run card also carries an **"Open"** (ExternalLink icon) deep-link to `/session/:id/subagent/:taskId` — the standalone full-page view; clicking it closes the sheet on the way out so the back button returns cleanly. Legacy-synthesized runs (no `subagent_start` event, so no real `taskId`) hide the Open link since the full-page route keys on taskId. The row-level tint (strip, bg wash, border, chip) for a running run is **indigo**; tool chips *inside* the live stream keep the main-thread klein "running" palette so tool rows read the same as anywhere else. One panel-level 1s ticker keeps every expanded run's mm:ss elapsed counter fresh. A `variant: "inline" \| "embedded"` prop suppresses the panel's own "Agents" chip header when embedded in `SubagentsSheet` (which supplies its own). **The panel is no longer mounted inside `ChatTasksRail` / `TasksDrawer`** — they are strictly tools-only surfaces now. (4) Chat thread — Task/Agent/Explore `tool_use` pieces render as a one-line `Agent started → view` / `Agent done` / `Agent failed` **indigo pointer** (`bg-indigo-wash/60 border-indigo/40`, `text-indigo`) instead of a fat JSON chip, mirroring the existing `Plan updated` pointer pattern; failed runs fall back to the danger wash. Nested child pieces (any with `parentToolUseId` set) are filtered out of the main thread in `applyViewMode` **and out of the right-rail `TasksList` via `buildTaskRows`** so sync-subagent Bash/Read/Grep calls are owned solely by `SubagentsSheet` and never double up in the main tasks rail. Stop button + background-agent output-file auto-render are stubs — clicking stop today is a no-op (TODO: wire `SDKControlStopTaskRequest`). `follow` toggle in the sheet header is also a visual-only stub for now (TODO: wire to the timeline scroller) | `web/src/components/SubagentsStrip.tsx`, `web/src/components/SubagentsSheet.tsx`, `web/src/components/SubagentsPanel.tsx` (`variant` prop), `web/src/components/ChatTasksRail.tsx` (tools-only), `web/src/components/TasksDrawer.tsx` (tools-only), `web/src/components/TasksList.tsx` (`buildTaskRows` parent-filter), `web/src/screens/Chat.tsx` (`applyViewMode` parent-filter, Task/Agent/Explore indigo pointer branch, `Bot` header icon in both mobile+desktop headers, Tasks drawer button now `Wrench`, sheet mount + state), `mockup/index.html#s-18` |
| ✅ | **Subagent standalone page** (`/session/:id/subagent/:taskId`) — full-viewport twin of the 420px `SubagentsSheet` drawer so a long subagent run can be read without the narrow-sheet constraint. Reached from the `ExternalLink` button on each drawer RunCard. Layout: sticky header (back button → parent session, `Agent` chip tinted by status, agent-type + description, status/elapsed + clickable parent-session title), then a body with the original prompt in a scrollable mono block with Copy affordance, then the run's stream rendered chat-style — assistant_text in `Markdown`-rendered bubbles, thinking in a dashed muted block, tool calls as expandable cards that show pretty-printed JSON input and the paired tool_result output inline (danger tint when `isError`), orphan tool_results as standalone cards. A footer card surfaces token/tool-use/duration usage and the SDK `outputFile` path when present. Live: the screen subscribes to the parent session's WS so running runs stream into the page the same way they do in the drawer; a 1 s ticker keeps the elapsed counter fresh. **Auto-scroll**: the scroll container smooth-scrolls to the bottom whenever a new stream event is appended (tail-append pattern cribbed from `Chat.tsx`), plus a one-shot instant jump to the tail on first load so big completed transcripts open at the latest activity instead of the prompt. **iPhone safe-area**: the inner `RunView` carries `pb-[calc(env(safe-area-inset-bottom)+40px)]` so the last message clears the rounded screen corners + home bar on iOS Safari (where the route renders as a raw viewport, no AppShell tab bar to inherit clearance from). **Header wrap fix**: the `StatusLabel` span is `whitespace-nowrap shrink-0` so `running · 0:21` stays on one line even when the parent session title is long — previously the status clause broke mid-word on narrow mobile widths. Hidden for legacy-synthesized runs (`taskId` starts with `legacy-`) because there's no real task_id to key on | `web/src/screens/SubagentRun.tsx`, `web/src/App.tsx` (route), `web/src/components/SubagentsPanel.tsx` (Open link + `sessionId` + `onNavigate` props), `web/src/components/SubagentsSheet.tsx` (`sessionId` prop threaded through) |
| ✅ | **Main-thread plan isolated from subagent TodoWrite** — `selectLatestTodos`, `countTodoWriteRevisions`, and `todoRevisionAt` now skip any `tool_use` piece whose `parentToolUseId` is set, so a subagent's own TodoWrite call can't override the PlanStrip / PlanSheet with the subagent's plan. The subagent's TodoWrite still lives in the run's stream (same as its other tool calls) and will surface inside the subagent's own future plan surface if we ship one | `web/src/lib/todos.ts` |
| ✅ | **Desktop push-mode rails for Plan + Subagents** — on ≥md viewports, `PlanSheet` and `SubagentsSheet` now render as in-flow right-rail `<aside>` siblings of the center `<main>` column (400px / 420px, `hidden md:flex`, no backdrop, no modal semantics, Esc ignored) instead of `fixed inset-0` slide-overs that blurred the live chat behind a dimmed sheet. Mobile (<md) keeps the existing bottom-sheet overlay (now `md:hidden` so desktop never double-renders). Both components accept a `variant: "overlay" \| "rail"` prop modeled after `SessionSettingsSheet`; `Chat.tsx` mounts the rail variant at the outer flex container alongside `ChatTasksRail` / `SessionSettingsSheet(variant="rail")`, and keeps the overlay variant inside `<main>` for mobile. Stacks with the other rails so users can have Settings + Plan + Subagents open side-by-side if they want — each rail still closes independently via its header × | `web/src/components/PlanSheet.tsx` (`variant` prop), `web/src/components/SubagentsSheet.tsx` (`variant` prop), `web/src/screens/Chat.tsx` (dual mount: overlay inside `<main>` for mobile, rail at outer flex for desktop) |
| ✅ | **Subagent timestamp hygiene** — `eventToPiece` validates the `at` field on every `subagent_*` lifecycle event: if the value isn't a parseable ISO string with a positive epoch (e.g. a legacy row with a numeric seconds-stringified `at`), we fall back to the event's own `createdAt`. And the legacy-synthesis fallback in `computeSubagentRuns` no longer emits `new Date(0).toISOString()` (which would render as `1970-01-01` in the drawer's `timeAgoShort` date fallback and looked like "the drawer's clock is broken"); it now prefers `tool_use.createdAt`, then the paired `tool_result.createdAt`, then `new Date()` so fresh runs read "now" instead of a 1970-epoch stamp | `web/src/state/sessions.ts` (`eventToPiece` `subagentAt()` helper, `computeSubagentRuns` legacy fallback) |
| ✅ | **Live subagent WS frames no longer dropped** — the WS-store merger in `sessions.ts` had an allow-list that only accepted seven frame types; the five `subagent_start / progress / update / end / tool_progress` frames were computed into valid `UIPiece`s by `frameToPiece` and then silently filtered out before reaching `transcripts`. Persisted rows went through fine (`manager.ts` appends every type), so a page refresh always showed the full run — but the live drawer sat on "0 events / Waiting for the first tool call…" until completion because `computeSubagentRuns` found no live `subagent_start`, fell through to legacy synthesis, and only picked up the parent tool_result's summary at the end. Also caused the legacy run's `startedAt` to be recomputed to `new Date()` on every WS frame (WeakMap cache miss), making the mm:ss counter flicker up a few seconds then reset to 0:00. Allow-list extended to include all five subagent frames so the live stream renders in real time instead of materializing only on refresh | `web/src/state/sessions.ts` (WS `frameToPiece` allow-list) |

## Settings

| Status | Feature | Where |
|---|---|---|
| ✅ | `/settings` route wrapped in AppShell (260px global sidebar). Rebuilt to match mockup s-12: screen header (caps "Settings" + display `{activeTab}` + mobile back chevron to `/sessions`), profile card (avatar = first char of username, username, "self-hosted" tagline, permanent "2FA on" pill), then an **inner 240px left rail** on md+ with eight entries (Account / Security / Notifications / Appearance / MCP servers / Plugins / Environment / Advanced; active row = `bg-canvas border border-line shadow-card`), and a horizontal filter-chip row on mobile. Content area uses the mockup's caps + display + lede header pattern on every subtab. Selected tab persists in `?tab=security` via `useSearchParams` so refresh/back-forward preserve state; Account is the default and clears the query string | `web/src/screens/Settings.tsx`, `web/src/components/AppShell.tsx` |
| ✅ | **Account** tab — real data. Rows: username, createdAt, 2FA-enabled badge, plus a "Change password" button that opens a modal. Modal enforces current password, new-password ≥ 8, and non-identity; surfaces server error codes (`invalid_credentials`, `same_password`, `bad_request`) in human words. On success shows a "password updated — other tabs keep working until cookie exp" note | `web/src/screens/Settings.tsx` (`AccountPanel`, `ChangePasswordModal`) |
| ✅ | **Security** tab — real data, honest. Three cards: the **2FA card** (klein-wash icon tile + status pill with full state machine — Enable / Rebind / Disable buttons; Enable opens a modal that pulls a fresh `secret + qrSvg` from `POST /api/auth/totp/begin` and confirms via `POST /api/auth/totp/confirm` with the new pairing code + current password; Rebind uses the same modal but gates on a code from the **current** authenticator instead of password; Disable opens a separate modal that wipes secret + every recovery code via `POST /api/auth/totp/disable`, password-gated), the **Recovery codes** card (only rendered when 2FA is on; shows `{remaining} of 10 unused` + relative `generated` timestamp, two-click-armed Regenerate that opens a one-time modal with the 10 plaintext codes + Copy all + Download as .txt; warning pill when `remaining === 0`), and an **Audit log** card backed by `GET /api/audit`. Audit card shows `{totalCount} events · past 30 days` and up to 6 rows inline with 12-char relative-time column (`5m`, `2h`, `3d`, `1w`) + human sentence per event kind; "Open full log" expands and paginates via a `before=<iso>` cursor — 50 rows per page, "Show more" appends the next page and disables when `rows.length >= totalCount` or a short page arrives, so the card can reach past the legacy 200-row ceiling. Server writes fire-and-forget from auth (`login` / `login_failed` / `totp_failed` / `totp_enabled` / `totp_rebound` / `totp_disabled` / `totp_enable_failed` / `totp_rebind_failed` / `totp_disable_failed` / `logout` / `password_changed` / `recovery_codes_regenerated` / `recovery_code_used` / `recovery_code_failed`), session delete (`session_deleted`), manager permission decisions (`permission_granted` / `permission_denied`), and push routes (`push_subscribed` / `push_revoked`). Still intentionally omitted: Last-used tile, Paired browsers card, Exposure panel | same (`SecurityPanel`, `TwoFactorCard`, `TwoFactorEnrollModal`, `TwoFactorDisableModal`, `RecoveryCodesCard`, `AuditLogCard`), `server/src/audit/store.ts`, `server/src/audit/routes.ts`, `server/src/auth/recovery-codes.ts` |
| ✅ | `audit_events` table — SQLite migration id=8 creates `audit_events(id, user_id NULL, event, target NULL, detail NULL, ip NULL, user_agent NULL, created_at)` with indexes on `created_at DESC` (powers the Security card's "past 30 days" query) and `event`. `event` is a short free-form identifier rather than an enum column so new audit kinds can land without a migration; `user_id` has no FK so rows survive credential rotations and pre-login events (failed logins, TOTP failures without a valid challenge) can still be recorded with `user_id = NULL`. `ip` / `user_agent` are best-effort and may be NULL when the call site can't thread a request through (e.g. manager permission decisions). **Login device audit**: `audit_events` gained `device_id` (from the `claudex_device_id` cookie) / `device_is_new` (marker for a device's first successful login) / `port` (source port) columns. A successful login signs a device number (`claudex_device_id`) when none is present and flags it as a new device; failed-login and rate-limit events carry `device_id` too. The Audit log sentence renders `IP:port` alongside a new-device marker | `server/src/db/index.ts` (migration 8, plus login-device-audit migration), `server/src/audit/store.ts`, `server/src/audit/routes.ts` |
| ✅ | **Notifications** tab — empty state only ("No notifications stream yet.") with a "not tracked yet" pill. Will light up once permission queues, routine outcomes, and pairing alerts have a notification channel to flow through | same (`NotificationsPanel`) |
| ✅ | **Appearance** tab — partly real. Theme row with the current "Light" segment active and a dimmed "Dark (soon)" placeholder; disabled text-size slider with an explanatory note. **Output-language picker**: 8 options (Auto + Chinese / English / Japanese / Korean / Spanish / French / German). Auto defers to the user's own `~/.claude/settings.json` `language` field; picking a specific language appends "Please respond in <lang>." to the Claude Code system preset on every NEW/resumed session's SDK start. Live sessions keep their current language until next resume. State lives in claudex's own SQLite (`app_settings` KV table, migration 24) — never writes to `~/.claude/`. Backed by `GET`/`PATCH /api/app-settings` | same (`AppearancePanel`), `server/src/settings/store.ts`, `server/src/settings/routes.ts`, `server/src/sessions/agent-runner.ts`, `shared/src/models.ts` (`AppSettings`, `SUPPORTED_LANGUAGES`) |
| ✅ | **Models** tab — add custom model IDs from self-hosted API proxies (OneAPI, New API, etc.) alongside the built-in Claude models. Custom models are persisted in the `app_settings` KV table as a JSON array under `custom_models`. Each entry has `id` (the model string passed verbatim to the Agent SDK), `label` (human-readable display name), and optional `contextWindow` (token count for the context-percentage ring; defaults to 1M). The tab shows built-in models (read-only) and custom models (add/edit/delete). All model selectors throughout the app (Chat header pill, New Session sheet, Session Settings, Queue, Routines) merge built-in + custom models dynamically | same (`ModelsPanel`), `shared/src/models.ts` (`ModelId`, `BUILTIN_MODELS`, `CustomModel`, `AppSettings`), `server/src/settings/store.ts`, `web/src/lib/pricing.ts` (`getModelLabel`, `getAllModelEntries`), `web/src/state/app-settings.ts` |
| ✅ | **MCP servers** tab — empty state. "Once we parse `~/.claude/settings.json#mcpServers` this pane will list each configured server…" with an inline link that jumps to the Plugins tab (closest adjacent concept we actually render). No fake server list | same (`McpPanel`) |
| ✅ | **Plugins** tab — real data. Read-only reflection of `~/.claude/settings.json` `enabledPlugins` merged with `~/.claude/plugins/installed_plugins.json`. Shows `{enabledCount} enabled · {total} installed` header, then per-plugin name / marketplace / version / enabled badge. Empty state explicitly says claudex does not install plugins itself. Zero mutation — fetched via `GET /api/user/env` | same (`PluginsPanel`) |
| ✅ | **Environment** tab — real data. Read-only dump of a few safe fields from `GET /api/user/env`: session user, `claudeDir`, and whether `settings.json` is readable. Footer reminds the user claudex only writes to `~/.claudex/`, never `~/.claude/` | same (`EnvironmentPanel`) |
| ✅ | **Advanced** tab — **Worktrees** section. Backed by `GET /api/worktrees` and `POST /api/worktrees/prune`: lists every `claude/*` git worktree git knows about across the user's registered projects, classified as **linked** (a session row still references the branch or path — green dot, no action) or **orphaned** (nothing references it — red dot + per-row Remove button + bulk "Prune N orphans" footer). Prune path calls `git worktree remove --force` in the project's checkout, then `git branch -D <branch>`, then defensively `rm -rf` the dir if it remains; refuses to touch any branch that doesn't start with `claude/`. **The target path is additionally bounded to inside the project directory** (compared via `realpathSync` so /var ↔ /private/var matches) — a forged request pairing a legit `claude/*` branch with an arbitrary absolute `path` returns `{removed:false, error:"path_out_of_project"}` and never reaches the `rm -rf`. Per-project errors (path missing, git unavailable) are swallowed so one broken project can't hide orphans elsewhere. Still planned here: JWT secret rotation, worktree defaults, cookie lifetime, exposure diagnostics | `web/src/screens/Settings.tsx` (`AdvancedPanel`), `server/src/sessions/worktree-manage.ts`, `server/src/sessions/worktree-routes.ts` |
| ✅ | **Advanced** tab — **Backup & restore** section. `GET /api/export/all` (login-gated) streams a JSON `BackupBundle` covering every project, session (archived + side-chats included), event, routine, queued prompt, grant metadata, attachment metadata (no file bytes), and the last 1000 audit rows; password hashes, TOTP / recovery-code hashes, push subscription keys, and VAPID / JWT secrets are never included. `POST /api/import/all` (multipart, field `bundle`) merges a bundle into the current database inside a single transaction: projects dedupe on `path`, sessions are always inserted under fresh ids (skipped when `sdk_session_id` already exists locally to avoid double-adopting a CLI conversation), events are re-sequenced 1..N per session **and mirrored into the full-text search index in the same pass** (imported sessions/messages are searchable immediately, not only after the next write), routines land as `paused` (no surprise cron fires on the new install), queued prompts get appended at the tail and `running` rows reset to `queued`. **Audit rows are never imported** — the bundle controls `event`/`ip`/`user_agent` strings, so accepting them would let a malicious bundle forge `login` / `import.success` entries into the local Security card. Count is surfaced under `skipped.audit = {count, reason}`. Grants, attachments, push subscriptions, users, and recovery codes are skipped by policy — device-bound or security-sensitive rows should not travel across installs. Web surface in the Advanced tab has a Download-bundle anchor (browser-native download) and a file-picker + drag-drop zone that POSTs the bundle and shows an honest "imported N … skipped M …" summary | `server/src/backup/export.ts`, `server/src/backup/import.ts`, `server/src/backup/routes.ts`, `web/src/screens/Settings.tsx` (`BackupCard`), `web/src/api/client.ts` |
| ✅ | **About** screen at `/about` (standalone route, not a Settings subtab; reached from an "About" entry below "Advanced" on the Settings sidebar + mobile chip row). Backed by login-gated `GET /api/meta` which returns `{version, commit, commitShort, buildTime, nodeVersion, sqliteVersion, platform, uptimeSec}` — static fields sampled once at server boot (package.json version, `git rev-parse HEAD` with null fallback when not a git checkout, `os.platform() + " " + os.arch()`, `SELECT sqlite_version()`), uptime live per request. UI renders a label/value card (Version, Commit as clickable GitHub link when known, Built with relative + absolute timestamp, Node, SQLite, Platform, Uptime with `Xd Yh` formatting) and a footer linking the GitHub repo + FEATURES.md (pinned to the build's commit when known, else `main`). **Update check**: a second login-gated `GET /api/meta/latest-release` lazily hits `api.github.com/repos/aaravarr/claudex/releases/latest` (5s timeout, 1h in-process TTL on success / 5min on failure, single-flight via shared promise) and returns a discriminated union — `ok:true` with `{tag, name, version, htmlUrl, publishedAt, body, currentVersion, updateAvailable, fetchedAt}` or `ok:false` with `{error, currentVersion, fetchedAt}` (`network`, `timeout`, `no_release`, `http_<code>`). The About screen renders an extra "Latest release" row with the tag (linked to the release page) + relative published time + an `up to date` / `update` badge, and when `updateAvailable` is true also shows a top banner with the tag, current version, the first 280 chars of release notes, and a CTA linking to the release page. **"Update now" button** calls `POST /api/admin/update-and-restart` `{tag}` — the server spawns a detached `scripts/update-restart-worker.mjs` that git-fetches origin, checks out the exact tag, runs pnpm install, waits for the old port to drain, then relaunches the server. The page polls `/api/health` and auto-reloads when the new server is up. Button has four states: idle ("Update to vX.Y.Z") → updating → waiting-for-server → (auto-reload) or failed with retry. Version comparison uses a small dotted-numeric comparator (semver-ish, prereleases lose to their release counterparts). Tested: 401 gates on both endpoints, shape assertions on `/api/meta`, `ok:true` / `ok:false` paths for `/latest-release` (200 happy + 404 → `no_release` + thrown `fetch` → `network`), single-flight cache shares one upstream call across two requests, plus a `compareVersions` unit suite | `server/src/meta/routes.ts`, `server/src/admin/routes.ts`, `scripts/update-restart-worker.mjs`, `server/src/transport/app.ts`, `shared/src/models.ts` (`LatestReleaseResponse`, `UpdateAndRestartRequest`), `web/src/screens/About.tsx`, `web/src/lib/admin-actions.ts`, `web/src/screens/Settings.tsx`, `web/src/App.tsx`, `web/src/api/client.ts`, `server/tests/meta-routes.test.ts` |
| ✅ | **Client errors screen** at `/errors` (standalone route under Settings; reached from a "Client errors" entry beside About in the sidebar + mobile chip row, and from the red RootErrorBoundary fallback's "Open /errors" button). Surfaces every browser-side crash captured by the frontend error reporter. Frontend wiring: `web/src/lib/error-reporter.ts` is imported first in `main.tsx` and installs `window.error`, `unhandledrejection`, and a `console.error` monkey-patch that each POST to `/api/client-errors` (cookie-auth, `keepalive: true`) with 500ms in-process coalescing so a render loop doesn't flood the wire. `RootErrorBoundary` wraps `<App />` and converts React render crashes into a full-screen red page (inline styles only — does not depend on Tailwind or any user-land component, so a CSS / shared-component regression still renders the error instead of going white) with the message, stack, component stack, Reload, and "Open /errors" buttons, and also reports the error with `kind: "render"`. Server side: migration 18 creates `client_errors(id, kind, message, stack, component_stack, url, user_agent, fingerprint UNIQUE, count, first_seen_at, last_seen_at, resolved_at)` — dedup on a sha1 fingerprint of `kind + first-line message + first stack frame`, so repeat firings bump `count` + `last_seen_at` instead of spawning rows, and a fresh occurrence on a resolved fingerprint auto-reopens it (clears `resolved_at`) for regression detection. Login-gated REST: `POST /api/client-errors` (ingest), `GET /api/client-errors?status=open|resolved|all&limit=50&before=<iso>`, `POST /api/client-errors/:id/resolve`, `POST /api/client-errors/:id/reopen`, `DELETE /api/client-errors/:id`, `POST /api/client-errors/resolve-all`, `DELETE /api/client-errors/resolved`. Screen shows `{openCount} open · {resolvedCount} resolved` header, three-tab filter (open / resolved / all), per-row collapse with kind badge + `×N` count + relative `lastSeenAt` + message + url; expanded view surfaces full message / stack / componentStack / first-seen / last-seen / userAgent and Mark resolved · Reopen · Delete actions. Two bulk actions at the top: "Resolve all open" and "Clear resolved". Auto-refreshes every 10s while the tab is visible so a fresh crash surfaces without manual reload. Accepted gap: errors that fire before the JWT cookie lands (pre-login) aren't persisted. **Unhandled-rejection messages are prefixed with the rejection's constructor name** (e.g. `TypeError: Load failed`) so iOS Safari network-layer rejections don't fingerprint identically to other `Error` rejections with the same message — this makes bare `fetch()` failures distinguishable in the log. Also, `api.refreshSessions()` now swallows non-`ApiError` rejections (iOS backgrounded-tab resume → WS reconnect → stale fetch → `TypeError: Load failed`) instead of letting them escape as unhandled rejections; real API errors still propagate | `shared/src/models.ts` (`ClientErrorReport`, `ClientError`, `ClientErrorListResponse`), `server/src/db/index.ts` (migration 18), `server/src/client-errors/store.ts`, `server/src/client-errors/routes.ts`, `server/src/transport/app.ts`, `web/src/lib/error-reporter.ts`, `web/src/components/RootErrorBoundary.tsx`, `web/src/main.tsx`, `web/src/screens/ClientErrors.tsx`, `web/src/screens/Settings.tsx`, `web/src/App.tsx`, `web/src/api/client.ts` |

## Notifications (PWA + Web Push)

| Status | Feature | Where |
|---|---|---|
| ✅ | **PWA install** — `web/public/manifest.webmanifest` declares name, `start_url: /sessions`, `display: standalone`, theme/background colors matching the app, and three icons (192×192, 512×512 standard, 512×512 maskable). `web/index.html` links the manifest, `apple-touch-icon`, and `apple-mobile-web-app-*` meta tags so iOS Safari treats it as a first-class home-screen app. Required for Web Push on iOS 16.4+ (iOS only grants `pushManager.subscribe` inside a home-screen-installed PWA). Icons rendered from `web/public/icon.svg` via `web/scripts/gen-icons.mjs` (uses `sharp`); regenerate manually after logo changes — not wired into `pnpm build` | `web/public/manifest.webmanifest`, `web/index.html`, `web/public/icon*.png`, `web/scripts/gen-icons.mjs` |
| ✅ | **Service worker** at `web/public/sw.js` — minimal, no caching strategy (claudex is a thin live-server shell). Receives `push` events, shows a notification with title/body/data derived from the server's JSON payload, coalesces by `tag = sessionId` so repeated asks for the same session don't stack. `notificationclick` finds an existing tab whose pathname starts with `/session/<id>` and focuses it, otherwise opens the URL in a new window | `web/public/sw.js` |
| ✅ | **VAPID keys** generated on first boot and persisted at `~/.claudex/vapid.json` (mode 0600). Public half is served via `GET /api/push/vapid-public` for `PushManager.subscribe({applicationServerKey})`. Rotation would invalidate every subscription, same shape of damage as rotating the JWT secret — we keep it stable across restarts | `server/src/push/vapid.ts`, `server/src/index.ts` |
| ✅ | **Push subscription store** — SQLite migration id=6 creates `push_subscriptions(id, endpoint UNIQUE, p256dh, auth, user_agent, created_at, last_used_at)`. Upsert-by-endpoint semantics so re-subscribing from the same browser refreshes keys rather than duplicating. Single-user install → no `user_id` column | `server/src/db/index.ts`, `server/src/push/store.ts` |
| ✅ | **Push REST** (login-gated): `GET /api/push/vapid-public`, `POST /api/push/subscriptions` (body: serialized `PushSubscription` + optional userAgent; server falls back to the request header UA), `DELETE /api/push/subscriptions/:id` (per-device revoke, 404 on unknown id), `DELETE /api/push/subscriptions` (revoke all), `GET /api/push/state` (`{enabled, devices: [{id, userAgent, createdAt, lastUsedAt}]}`), `POST /api/push/test` (fire a test push to every device; returns `{sent, pruned}`) | `server/src/push/routes.ts` |
| ✅ | **Permission-request push trigger** — `SessionManager` fires a Web Push every time a session flips to `awaiting` because the SDK's `canUseTool` bridge is holding a pending request. Payload: `title = session.title`, `body = "<toolName> · <truncated primary input>"` (command / file_path / pattern / url / query, 60-char cap), `data = {sessionId, url: "/session/<id>"}`. Fire-and-forget with a `.catch` so push-delivery failures never crash runtime. Subscriptions that return 404/410 Gone from the push service are pruned on the spot so the device list stays honest | `server/src/sessions/manager.ts`, `server/src/push/routes.ts` (`createPushSender`) |
| ✅ | **Notifications settings tab** (`/settings?tab=notifications`) — replaces the old empty state. Three UI states: (1) capability-lost card when the browser can't SW/push (no SW support) OR the origin is insecure (frpc-over-HTTP) — surfaces the real constraint so the user doesn't wonder why the button is dead; (2) "Enable on this device" primary klein button when supported but not yet subscribed, with `Notification.permission === "denied"` handled as an explicit message; (3) when enabled: Disable button + "Send test" + device list with per-device Revoke. Device labels are derived client-side from the stored UA (Safari on iOS / Chrome on Android / etc). Honest footnote spells out the HTTPS requirement and the iOS-Safari Home Screen caveat | `web/src/screens/Settings.tsx` (`NotificationsPanel`), `web/src/lib/push.ts` |
| ✅ | **Push routes tested** — 11 tests covering 401 gate, VAPID public endpoint, subscribe → state round-trip, idempotent upsert on duplicate endpoint, DELETE by id (and unknown-id 404), DELETE all, test push with 0 devices, test push with 1 device actually invokes `webpush.sendNotification`, 410 Gone response prunes the stale subscription. `web-push.sendNotification` is stubbed via `vi.spyOn` so tests don't contact real push services | `server/tests/push-routes.test.ts` |

## Search

| Status | Feature | Where |
|---|---|---|
| ✅ | **SQLite FTS5 full-text index** — migration id=9 creates two virtual tables: `session_search(session_id UNINDEXED, event_seq UNINDEXED, kind UNINDEXED, body)` for message bodies and `session_title_search(session_id UNINDEXED, title)` for session titles. Both use `tokenize=unicode61` (covers CJK + accented scripts). Index is backfilled at migration time from existing `sessions` rows and text-bearing `session_events` rows (`user_message` / `assistant_text` / `assistant_thinking`) via `json_extract(payload, '$.text')` so users who upgrade get history searchable immediately | `server/src/db/index.ts` |
| ✅ | **Live sync on writes** — `SessionStore.appendEvent` indexes into `session_search` for text-bearing kinds (other kinds skipped honestly); `SessionStore.create` and `.setTitle` upsert into `session_title_search` (delete-then-insert since FTS5 lacks a natural upsert); `SessionStore.deleteById` removes every FTS row for the session. All three paths wrap FTS mutations in best-effort try/catch so an index failure never blocks the primary write | `server/src/sessions/store.ts`, `server/src/search/store.ts` |
| ✅ | **`GET /api/search?q=<query>&limit=<n>`** — login-gated. 400 on empty/whitespace `q`. Query sanitizer tokenizes on whitespace, strips FTS5 syntax chars (`"`, `(`, `)`, `-`, `^`, `*`, `:`) per token, keeps only tokens with at least one letter/digit, and wraps each survivor in double quotes so a literal phrase is searched — "AND"/"OR"/"NOT"/"NEAR/" are neutered as a side-effect of this wrapping. Returns `{ titleHits: [{sessionId, title, snippet?}], messageHits: [{sessionId, title, eventSeq, kind, snippet, createdAt}] }`. Snippets are generated via FTS5's `snippet(session_search, 3, '<mark>', '</mark>', '…', 32)` — 32-token window around the match with `<mark>` tags on hits. Both buckets capped at `limit` (default 20, max 50) | `server/src/search/routes.ts`, `server/src/search/store.ts` |
| ✅ | **GlobalSearchSheet UX** — mobile bottom sheet / desktop centered overlay (`max-w-[640px]`). ⌘K on the Home screen opens the sheet from anywhere; the existing inline Home input stays live as a cheap substring filter over the already-loaded session list. Typing is debounced 250ms; empty state shows "Type to search your sessions and messages.", loading shows "Searching…". Results render in two sections ("Sessions" / "Messages") with counts, kind badges (`you` / `claude` / `thought`), relative time, and snippets whose `<mark>` tags are parsed to styled `<span>` elements in a small tokenizer rather than piped through `dangerouslySetInnerHTML`. Clicking a row navigates to `/session/<id>` (for messages, appends `#seq-<eventSeq>`); Escape and outside-click close the sheet, focus returns to the inline input on close | `web/src/components/GlobalSearchSheet.tsx`, `web/src/screens/Home.tsx` |
| ✅ | **Search routes tested** — 401 gate, 400 on empty/whitespace q, title + message hits on a 3-session / 10-event corpus, empty response on nonexistent word, FTS5 special chars (`"`, `-`, `:`, `AND`/`OR`/`NOT`/`NEAR/`, `(`, `*`) don't crash the route, live-sync through `setTitle` (PATCH a session → new title is searchable, old title isn't) | `server/tests/search-routes.test.ts` |
| ✅ | **Scroll-to-event** — permalinks like `/session/<id>#seq-42` (from MessageActions' "Copy permalink" and GlobalSearchSheet message-hit rows) now resolve on Chat mount and on `hashchange`. The ChatScreen effect parses the `#seq-N` anchor, polls the scroll container for `[data-event-seq="N"]` every 120ms up to 2s, and if not yet rendered walks `loadOlderTranscript` up to 10 pages to pull older history in. On hit: `scrollIntoView({behavior:"smooth", block:"center"})` + a 1.2s klein ring flash (`ring-2 ring-klein/60 rounded-[10px]`). A `hashScrollActiveRef` suppresses the tail-autoscroll-to-bottom effect while the target is being resolved so the viewport doesn't race past it; tail autoscroll on *new* assistant frames is unaffected. The initial-load jump-to-bottom is also skipped when a `#seq-N` hash is present. Listening for `hashchange` makes in-session jumps from GlobalSearchSheet work without a reload | `web/src/screens/Chat.tsx` |

## Operational details

| Status | Feature | Where |
|---|---|---|
| ✅ | Typed shared contract in `@claudex/shared` — WS frames, HTTP DTOs, enums; both sides import from there | `shared/src/` |
| ✅ | Repo pushes to `https://github.com/aaravarr/claudex.git` (main); commits co-sign Claude | git log |
| ✅ | One-liner installer — `install.sh` (macOS + Linux, bash) and `install.ps1` (Windows, PowerShell 5.1+). `curl -fsSL .../install.sh \| bash` / `irm .../install.ps1 \| iex`. Detects git + Node 20 + pnpm 9 + `claude` CLI and offers to install each missing piece with a prompt (nvm-based Node install on *nix = no sudo; winget on Windows; `corepack enable` for pnpm; `npm install -g @anthropic-ai/claude-code` for the CLI). Clones to `~/claudex` (override via `--dir` / `CLAUDEX_HOME`), runs `pnpm install` + `pnpm --filter @claudex/web build`, and drives `pnpm run init` via `CLAUDEX_INIT_USERNAME` / `CLAUDEX_INIT_PASSWORD` env vars so the password never lands in argv — the init child prints the TOTP QR + recovery codes in its non-interactive branch, and the wrapper holds the terminal on a "save them now, press Enter to continue" banner so nobody misses the one-shot secrets. Idempotent: re-running pulls latest, re-installs deps, and skips init when `~/.claudex/claudex.db` already has a user. Flags `--yes` / `--skip-init` / `--skip-build` / `--skip-daemon`; env `CLAUDEX_HOME`, `CLAUDEX_BRANCH`, `CLAUDEX_REPO`, `CLAUDEX_ASSUME_YES`, `CLAUDEX_SKIP_DAEMON` | `install.sh`, `install.ps1`, `README.md`, `README_CN.md` |
| ✅ | Daemon registration baked into the installer (interactive prompt, default yes, `--skip-daemon` / `-SkipDaemon` to opt out). Platform-native + user-scoped, no sudo: **macOS** writes `~/Library/LaunchAgents/com.claudex.server.plist` and `launchctl bootstrap`s it into `gui/$UID` (falls back to `load -w`); **Linux** writes `~/.config/systemd/user/claudex.service` and `systemctl --user enable --now`s it (hints at `loginctl enable-linger` for run-without-login); **Windows** `npm i -g pm2 pm2-windows-startup`, `pm2 startOrReload ecosystem.config.cjs`, `pm2 save`, `pm2-startup install`. Logs land in `~/.claudex/logs/claudex.{out,err}.log` on all platforms. Idempotent (rerun == refresh the unit / plist / pm2 entry) | `install.sh`, `install.ps1`, `ecosystem.config.cjs` |
| ⬜ | Dockerfile | — |
| ⬜ | Telemetry / metrics endpoint | — (intentionally not doing this for MVP) |

---

## Not started (candidates for P4+)

- **P4** — parallel sessions per project surfaced in the UI, branch pickers, PR link-out
- **P5** — usage/context panel, sticky virtual-keyboard row for the composer pickers
- **P7** — PR monitoring, auto-fix / auto-merge, preview (embedded browser)
- **P8** — Skills / Plugins / Connectors management UI, CLAUDE.md editor, env-var editor, global settings (2FA + paired browsers)
- **P9** — Docker image, signed release binaries, dashboards

---

## How to keep this file honest

- Every commit that adds a user-visible behavior, a new API surface, or moves
  an item between the three tiers **must** update the corresponding row in this
  file in the same commit.
- Every commit that deletes a feature must remove (or downgrade) its row.
- If you introduce a capability on the backend but intentionally don't expose
  it yet, add it as 🟡 with a one-line note on what's missing. That way nobody
  re-implements it or misses the last 10%.
- Don't reorganize the doc without a reason — stable anchors let humans and
  agents reference specific rows in review.
